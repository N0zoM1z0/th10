# TH10 verified knowledge

This file stores durable TH10-scoped facts and important negative results.
Cross-game promotion is a later Factory review, never an automatic game action.

| ID | State | Fact | Evidence |
| --- | --- | --- | --- |
| TARGET-001 | observed / corroborated | The supported executable is original Japanese TH10 v1.00a, size 487,936, SHA-256 `2f14760b...dc9040`. | Direct hash/PE inspection plus pinned thcrap version database |
| PE-001 | observed | The target is relocation-stripped PE32 i386 at image base `0x00400000`, entry `0x004537DC`, with four sections. | `scripts/verify-target.py`; `config/target.toml` |
| TOOLCHAIN-001 | observed / corroborated | Rich product IDs 95, 96, and 100 at build 6030 record 131 normal C, 15 normal C++, and 52 LTCG C++ inputs. A separately pinned VC7.1 SP1 candidate reports compiler/linker build 6030. | Direct target Rich decoding; pinned `archaic-msvc/msvc710_sp1` commit; `scripts/verify-toolchain.py --execute` |
| TOOLCHAIN-002 | observed | The pinned candidate executes normal C/C++ COFF, C++ LTCG, resource compilation, and PE32 i386 linking under a game-bound headless Wine prefix. | `scripts/verify-toolchain.py --execute`; component hashes in `config/tools.lock.toml` |
| TOOLCHAIN-003 | unknown | Per-unit normal-COFF versus LTCG ownership, flags, TU partition, libraries, resources, and link order are not established. | Explicit open fields in `config/build.toml`; empty `config/match-units.toml` |
| TOOLCHAIN-004 | observed | A pinned VC7.1 build-6030 `/GL` diagnostic linked with `/LTCG` naturally eliminates an unused source parameter and converts surviving internal callee inputs from the normal-COFF stack ABI to private EAX/ECX register inputs. This demonstrates that the private-register/signature-shrink class seen in TH10 is a legitimate LTCG transformation, but provides no per-function TH10 ownership or exactness credit. | `.analysis/gpt-web/20260912-startup-owner/compiler-shape-report.txt`; pinned compiler/linker execution |
| ANALYSIS-001 | observed | The private Ghidra project is bound to the target by SHA-256/MD5, image layout, entry function, and distributed mapped-byte samples. | `python3 scripts/ghidra.py check`; Factory `th10-ghidra` check |
| INVENTORY-001 | observed / inferred | Ghidra supplied the initial navigation inventory, which has since been augmented by target review. Candidate entry, physical boundary, origin, source presence and exactness are independent claims recorded in separate ledgers; no Ghidra name or dense span alone establishes any of them. | `config/functions.csv`; `config/function-boundaries.csv`; `config/function-origins.csv`; `config/sources.csv`; `config/matches.csv` |
| ANALYSIS-002 | observed | The Factory-native `th10-ghidra` provider binds the registered implementation files by aggregate SHA-256. Editing `scripts/ghidra.py` or a registered Java script deliberately makes provider discovery/check fail with “native Ghidra implementation differs from its operator binding” until the private operator binding is reviewed and refreshed. After adding address-set range export, the reviewed private binding became `c001f8ef3d9a64e96664d999c14891d9c92fa12f393bca64cc77bb087c0e2143`, and the Factory-native `check {}` again passed with `provider_transport=factory-native-command`. | Factory provider renderer and validator; private ignored `service.toml`; Factory-native `th10-ghidra` check |
| INVENTORY-002 | observed | The target-bound boundary audit contains 1,312 tracked candidates: 1,173 reviewed, 88 provisional and 51 requiring focused review. Their union covers 340,260 `.text` bytes. The remaining 72,029 bytes split into 507 pure alignment gaps totaling 4,090 bytes and 445 unresolved gaps totaling 67,939 bytes. The audit reports 26 overlapping extent pairs involving 37 candidates, four direct branches to untracked addresses and 97 aligned non-text pointers into untracked `.text`; those reference lists are triage evidence and do not create candidates automatically. | Exact target plus pinned Capstone; `scripts/report-boundary-inventory.py --check-ledger`; `config/function-boundaries.csv` |
| INVENTORY-003 | observed | The attested Ghidra project currently exports 1,419 body ranges for 1,195 functions. Of those functions, 122 have multiple ranges and two own ranges below their entry: `0x004451C0` has remote ranges at `0x004436C0-0x0044390E` and `0x00443910-0x00443B5E`, while `0x0046227B` owns `0x00452720-0x00452724`. Collapsing a Ghidra address set to `entry..maximum-address` therefore loses physical structure and may absorb unrelated code or data. | Factory-native Ghidra architecture export; `.analysis/boundaries/function-body-ranges.csv`; `scripts/report-boundary-inventory.py` |
| FRONTEND-001 | observed / inferred | Target review adds three Ghidra-missed front-end entries. `0x0042D260-0x0042D2D3` ends executable code at `0x0042D2B3` and owns an eight-entry absolute switch table through `0x0042D2D3`. `0x0042D2E0-0x0042D2E6` is `mov eax,ecx; jmp 0x0042CDF0`, and `0x0042D2F0-0x0042D2F6` is `mov eax,ecx; jmp 0x0042D260`; both are followed by CC padding and have target data references. The reviewed title/front-end chain identified `0x0042CDF0`, `0x0042D920` and `0x0042E5A0` as one coherent core recovery batch, subsequently completed in `FRONTEND-002`; that prioritization was inferred from target call structure and size rather than origin or exactness. | Direct target bytes/xrefs; Factory-native Ghidra callers/decompile; `scripts/rank-core-backlog.py` |
| INVENTORY-004 | observed / review-queue | The boundary audit extracts indexed indirect-jump bases that point into `.text`. It currently finds 100 such target references: 54 resolve inside the recorded span of their source candidate and 46 resolve outside it. The latter list exposes probable compiler-owned switch tables that terminal-instruction boundary heuristics left in gaps, but each table length and physical ownership still requires direct review before changing a candidate extent. | Hash-attested target plus pinned Capstone; `scripts/report-boundary-inventory.py --check-ledger` |
| FRONTEND-002 | observed / source-present | Three authored front-end owners now have maintained source across 8,315 target bytes. `FrontEndControllerView::Update @ 0x0042CDF0` dispatches all 17 screen values, advances its timer, rotates demo replays after 900 idle frames and performs the observed ANM cleanup/title-entry branches. `UpdateOptions @ 0x0042D920` covers all five state values and the six cursor rows for color depth, BGM/SFX volume, key configuration, defaults and return. `RefreshOptionsDisplay @ 0x0042E5A0` updates sound globals, queues `SetVol`, applies the observed quartic BGM attenuation curve, assigns twelve digit sprites and controls normal/selected leading-digit visibility. Descriptive identifiers do not claim original symbols or TU ownership. | Hash-attested target disassembly/tables/calls; Factory-native Ghidra decompilation; maintained `src/FrontEnd.cpp`/`src/FrontEnd.hpp`; `scripts/report-frontend-core.py --check` |
| FRONTEND-003 | observed / physical-table-ownership | Physical review extends the title owner from its RET at `0x0042D212` through one NOP and its directly indexed 17-entry absolute table at `0x0042D214-0x0042D257`, for a 1,128-byte owner. It extends the options controller from its final `RET 4` at `0x0042E57D-0x0042E57F` through its directly indexed five-entry table at `0x0042E580-0x0042E593`, for a 3,188-byte owner. Eight and twelve `CC` bytes respectively separate the next entries. Both tables were previously untracked. | Direct target branch operands, table destinations and padding; target-bound front-end report; boundary ledger |
| FRONTEND-004 | observed / corrected | Repeated front-end and GUI child-VM traversals compare the signed word at ANM VM `+0x38A`, which the independently maintained VM layout identifies as `scriptIndex`; `activeSpriteIndex` is the distinct word at `+0x384`. The GUI helper's former `activeSpriteIndex` comparison was therefore a semantic field error and is corrected to `scriptIndex`. The cursor helpers establish a complete `0xD8` cursor layout, while the current partial front-end controller binds screen/state/cursor at `+0x1C/+0x20/+0x24`, timer at `+0x2B0` and VM-id storage at `+0x2C4`. | Target compare operands and cursor helper bodies; maintained ANM VM layout; VC7 layout assertions |
| TOOLCHAIN-076 | observed / normal-and-ltcg-nonexact | Pinned VC7.1 SP1 build6030 compiles `src/FrontEnd.cpp` under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc`, the same profile plus `/GL`, and `/W4`. The three target-bound diagnostics are mismatches with `acceptance_authority=none`. Normal object sizes are 324/928/203 bytes and selected linked `/GL` contributions are 368/884/199 bytes for title/options/display, versus target physical owners of 1,128/3,188/3,999 bytes. The target repeatedly inlines ANM lookup and helper paths; successful compilation establishes source viability only, so exactness remains unknown and no match row is added. Repository totals become 266 source mappings and remain 110 exact functions / 12,166 bytes. | `.analysis/gpt-5.6-sol/20260915-frontend-options-core/normal-probe.json`; linked `/GL` diagnostic in the same campaign; pinned compiler probes; unchanged canonical exact ledger |
| FRONTEND-005 | observed / source-present | Three additional authored front-end owners now have maintained source across 3,532 target bytes. `FrontEndControllerView::UpdateKeyConfig @ 0x0042F540` implements the five-state/seven-row controller, scans 31 signed controller-button transition bytes, edits five action bindings, handles reset/cancel/commit and drives root VM slot 2 interrupts. `RefreshKeyConfigDisplay @ 0x0042F8B0` resolves twenty child digit VMs and writes tens/ones sprites for both normal and selected rows. `AssignKeyConfigBinding @ 0x00430250` swaps a displaced duplicate binding before refreshing the display and playing sound 10. Descriptive identifiers do not claim original symbols or TU ownership. | Hash-attested target disassembly/calls; Factory-native Ghidra decompilation/xrefs; maintained `src/FrontEnd.cpp`/`src/FrontEnd.hpp`; `scripts/report-frontend-key-config.py --check` |
| FRONTEND-006 | observed / corroborated / physical-table-and-data-layout | The key-config update owner is physically `0x0042F540-0x0042F8AF` = 880 bytes: executable code ends at `0x0042F89A`, one NOP follows, and its directly indexed five-entry state table occupies `0x0042F89C-0x0042F8AF`; the independent display owner begins immediately at `0x0042F8B0`. Display ends at `0x00430243` before twelve CC bytes, and assignment ends at `0x00430317` before eight CC bytes. Runtime controller mapping storage at `0x00474E88` is nine adjacent signed shorts with target defaults `(0,1,2,3,-1,-1,-1,-1,4)`. TH10 input masks identify the first four entries as shot/bomb/focus/menu and the last as skip; up/down/left/right names for the four preserved middle entries are adjacent-supported and remain provisional because the reviewed TH10 input path derives direction bits from controller axes. Key-config scratch bindings are five shorts at controller `+0x59CC`; the UI edits the first four runtime actions plus skip, while commit preserves and copies the complete 18-byte runtime mapping to persisted storage at `0x00491D4C`. | Direct target table/tails/default data and copy instructions; input-consumer masks/axis paths in `0x0044A190`; Factory-native xrefs; committed TH08 mapping declaration used after TH10 layout recovery; compile-time layout assertions; focused report |
| TOOLCHAIN-077 | observed / normal-and-ltcg-nonexact | Pinned VC7.1 SP1 build6030 compiles the expanded `src/FrontEnd.cpp` under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc`, the same profile plus `/GL`, and `/W4`; `/W4` adds only the pre-existing anonymous-union extension warnings. The three new normal COMDATs are 624/113/171 bytes versus target owners 880/2,452/200. Strict diagnostics are real mismatches at 15/700, 9/2,432 and 19/192 comparable bytes respectively, all with `acceptance_authority=none`. The 2,452-byte display owner contains fully expanded child lookup paths that the standalone normal build retains behind helpers; successful `/GL` compilation keeps interprocedural reconstruction open but provides no linked-image extent or exactness credit. No match row is added. Repository totals become 269 source mappings and remain 110 exact functions / 12,166 bytes. | `build/probe-backlog/frontend-key-config-probe.json`; pinned normal, `/GL` and `/W4` compiler probes; unchanged canonical exact ledger |
| MAIN-001 | observed / corroborated | The reviewed application entry at `0x00438AD0` spans through `0x0043903C`, has a unique CRT caller, consumes the four WinMain stack arguments, and returns with `ret 0x10`. The next three bytes are `0xCC` padding. | Attested Ghidra caller/disassembly queries; direct target disassembly; pinned VC7.1 ABI probe |
| MAIN-002 | observed | The `0x00438AD0` application entry owns the D3D9 cooperative-level/reset message loop, restart path, window teardown, configuration writeback, and process-level cleanup visible in the target. | Attested Ghidra decompile/callees; direct target disassembly and target strings |
| MAIN-003 | observed / inferred | `0x00438A30` is a reviewed 43-byte authored resource-release loop used by two target-local callers. Both callers pass the object in EAX; the function releases and clears 32 COM resource pointers. This is evidence for an interprocedural/private-ABI optimization context, but does not by itself prove which Rich-record input or TU owns it. | Attested Ghidra callers/disassembly; direct target boundary inspection |
| MAIN-004 | unknown | Code at `0x00439040`, `0x00439060`, `0x00439080`, and `0x004390A0` follows WinMain padding but is not represented as Ghidra functions and has no direct Ghidra or literal-VA xrefs. Its cleanup-funclet/compiler ownership remains unresolved, so it is not added to the function denominator yet. | Attested Ghidra function/xref queries; bounded raw target VA scan |
| MAIN-005 | observed / corroborated | `0x004391F0-0x004392DB` is a reviewed authored present/reset/screenshot function. It calls the D3D9 device `Present` method, releases manager resources and resets/reinitializes render state on presentation failure, and implements the `snapshot/th%.3d.bmp` capture loop. `0x004392DC-0x004392DF` is `0xCC` padding before independent `0x004392E0`. `GameWindow::Present` is retained only as a proposed adjacent-supported name. | Attested TH10 Ghidra callers/decompile/disassembly; direct target bytes; committed TH08/TH095 source used only for naming/source-shape corroboration |
| MAIN-006 | observed / corroborated | `0x00439890-0x00439D16` is a reviewed authored D3D9 initialization function called directly by WinMain. It constructs presentation parameters, retries HAL hardware/software and REF device creation, installs view/projection matrices, queries capabilities, and invokes the render-state initializer before returning success/failure. `0x00439D17-0x00439D1F` is `0xCC` padding before independent `0x00439D20`. `GameWindow::InitD3DRendering` is a proposed adjacent-supported name, not an original-symbol claim. | Attested TH10 Ghidra caller/decompile/disassembly; direct target bytes; committed TH08/TH095 source used only for naming/source-shape corroboration |
| MAIN-007 | observed / corroborated | `0x00439D20-0x00439FE5` is a reviewed authored no-explicit-argument D3D9 render-state initializer used after initial device creation and reset. Ghidra's provisional fastcall argument is contradicted by the target: the entry `push ecx` reserves one dword reused for `1.0f`, `1000.0f`, and `5000.0f`, and the exit `pop ecx; ret` releases it. A pinned VC7.1 `/O2` normal-COFF diagnostic naturally emits the same scratch pattern for a no-argument C++ function, but this does not establish the target function's COFF/LTCG owner or exactness. | Direct target disassembly; three attested TH10 call sites; pinned VC7.1 diagnostic probe under `.analysis/gpt-web/20260912-d3d-owner-seam/` |
| MAIN-008 | resolved / source-present | The D3D/present seam is now maintained as target-bound `GameWindowView` source. TH10-local offsets bind the five trailing writes at `0x00439FBF-0x00439FDE` to the active ANM render manager's blend mode, color operation, vertex shader, texture and camera-mode caches. The `GameWindowView` name remains descriptive and physical TU/original identifier ownership remains unresolved. | TH10 target control flow and manager offsets; maintained `src/Main.cpp`/`src/Main.hpp`; adjacent source used only after target-local recovery for source-shape corroboration |
| MAIN-009 | observed / corroborated | `0x00439390-0x00439538` is a reviewed authored frame/render dispatcher. WinMain is its unique caller and pushes the address `0x004924F0`; the callee loads that one stack pointer and all return paths use `ret 4`. The body advances target-local timing fields, runs gameplay/draw work, and invokes reviewed `0x004391F0`. Normal VC7.1 COFF diagnostics exclude an ordinary C++ member-thiscall shape: ordinary members use ECX/`ret`, while explicit `__stdcall` member and static/free `__stdcall(pointer)` forms produce the observed stack-pointer/`ret 4` ABI. Source-level owner and LTCG/private-ABI status remain unknown. `GameWindow::Render` is only a proposed adjacent-supported name. | Attested TH10 caller/decompile/disassembly; direct boundary bytes; pinned VC7.1 ABI diagnostic |
| MAIN-010 | observed / source-present | `0x00439540-0x00439659` is a reviewed authored high-resolution/fallback timestamp routine. It enters the game critical section, uses `QueryPerformanceCounter` divided by the stored frequency when available, otherwise brackets `timeGetTime` with 1 ms timer-period calls, maintains a target-local time origin, and returns the resulting x87 scalar. Current natural source produces the same 282-byte VC7.1 `/O2 /GS` normal and selected `/GL` candidate. Both match 159/162 comparable bytes; the only differences are three occurrences of stack-frame constant `0x10` versus target `0x14`, so exactness remains unknown. | Attested TH10 decompile/disassembly; direct target bytes/constants; maintained `src/Main.cpp`; strict normal and linked diagnostics below `build/main/` |
| MAIN-011 | observed / corroborated | Target storage beginning at `0x004924F0` is a coherent window/timing object-like region used by the reviewed Main seam. Observed fields include HWND `+0x00`, closing state `+0x04`, WinMain HINSTANCE `+0x08`, active/inactive state `+0x0C/+0x10`, signed frame counter `+0x14`, performance frequency/start QWORDs `+0x18/+0x20`, shortcut/relative-launch state `+0x28`, three saved `SystemParametersInfoA` values `+0x2C/+0x30/+0x34`, timing doubles `+0x38/+0x40/+0x48`, and time origin `+0x50`. These establish the field usage through `+0x57`; `MAIN-015` subsequently closes the physical storage extent at `0x58` bytes from TH10-local ownership evidence. The original type name remains unknown and is not inferred from adjacent games. | TH10 WinMain/window-proc/create-window/timing target accesses; attested xrefs/decompilation; committed TH095 layout used only as cross-game corroboration after TH10 offsets were established |
| MAIN-012 | observed / corroborated | `0x004390E0-0x004391E8` is a reviewed authored Win32 window procedure. `0x00439730` stores its address directly in `WNDCLASSA.lpfnWndProc`; the body consumes the four standard callback stack arguments and every exit uses `ret 0x10`. Ghidra's provisional extra `this` parameter is contradicted by the target. `GameWindow::WindowProc` is only an adjacent-supported proposed name. | Attested TH10 function/data-xref/disassembly; direct target boundary bytes; committed TH08/TH095 source used only for naming corroboration |
| MAIN-013 | observed / corroborated | `0x004392E0-0x00439343` is a reviewed authored system/timing initializer: it saves and disables three `SystemParametersInfoA` power states and initializes the performance frequency/start fields. Its historical pre-strict probe has now been superseded by the two cold exact normal-COFF replays in `MAIN-026`; production normal-COFF versus LTCG ownership remains unresolved. | Direct target disassembly; attested TH10 caller/callee evidence; historical probe under `.analysis/gpt-web/20260912-main-window-owner/` |
| MAIN-014 | observed / corroborated | `0x00439730-0x00439884` is a reviewed authored create-window routine. WinMain copies its HINSTANCE into EBX immediately before the unique call, and the callee consumes live-in EBX for both `WNDCLASSA.hInstance` and `CreateWindowExA` without a stack argument. A pinned normal-COFF explicit-HINSTANCE probe instead reads `[esp+4]`, so the target machine ABI is private/interprocedural rather than an ordinary standalone normal-COFF call. `GameWindow::CreateGameWindow` is only an adjacent-supported proposed name. | Direct TH10 caller/callee disassembly; pinned VC7.1 ABI diagnostic; committed TH08/TH095 source used only for naming/source-shape corroboration |
| MAIN-015 | observed | The target-local window/timing storage is physically bounded to exactly `0x58` bytes at `0x004924F0-0x00492547`. Its last observed `double` occupies `0x00492540-0x00492547`; an independent static object begins exactly at `0x00492548`, where startup code clears 17 dwords, sets its first-word flags, and registers a destructor wrapper that passes `0x00492548` to a separate cleanup routine. A default-layout VC7.1 struct using only TH10-observed field widths naturally has size `0x58` and the target offsets. The original type identifier remains unproven. | TH10 field-width accesses and xrefs; direct static-initializer/destructor-wrapper disassembly at `0x00465960`/`0x00465A50`; pinned VC7.1 layout assertions |
| MAIN-016 | observed / corroborated | Three CC-delimited authored source bodies were missing from the original Ghidra candidate denominator and are now tracked: `0x00439350-0x0043938D` restores saved system settings, `0x00439660-0x004396F7` computes unlocked elapsed time, and `0x00439700-0x0043972C` creates the D3D9 interface and reports failure. None has a direct Ghidra or raw target call/literal xref. WinMain contains inlined copies of the restore and D3D-interface logic. Historical natural VC7.1 probe size/comparable-byte claims for the first two predate the strict COFF extent fix in `TOOLCHAIN-035`; strict two-build replays now establish their bounded normal-COFF exactness in `MAIN-026`. The first two have maintained source; the D3D helper remains source-absent and non-exact. Original identifiers and production physical ownership remain unknown. | Direct target boundaries and bounded raw xref scans; historical diagnostic probes; WinMain duplicate logic |
| MAIN-017 | observed / corroborated | `0x00439FF0-0x0043A1A9` is a reviewed authored startup/single-instance routine. WinMain is its unique direct caller and passes no explicit argument. The target creates the `Touhou 10 App` mutex, inspects startup/module/console paths, follows shortcut chains through `0x0043A290`, and writes reviewed window/timing storage `+0x28` when the resolved launch path differs from the module path. `GameWindow::CheckForRunningGameInstance` is an adjacent-supported proposed name only; the unused HINSTANCE seen in adjacent source is not transferred to the TH10 ABI. | Attested TH10 caller/decompile/disassembly; direct target boundary/data accesses; committed TH08/TH095 source only for naming/source-shape corroboration |
| MAIN-018 | observed / corroborated | `0x0043A1B0-0x0043A1B7` is a Ghidra-missed, CC-delimited authored body that consumes an HWND through live-in EAX and calls `SetForegroundWindow`. It has no direct/literal target entry xref, while the reviewed WindowProc contains the same foreground-window action inline. A normal-COFF HWND helper instead reads `[esp+4]`; pinned VC7.1 LTCG diagnostics show the private-register transformation class is possible without proving this body's physical owner. `GameWindow::ActivateWindow` is only a proposed adjacent-supported name. | Direct target bytes/imports and bounded xref scan; reviewed WindowProc inline behavior; pinned compiler diagnostics; committed adjacent source for name/order corroboration |
| MAIN-019 | observed / corroborated | `0x0043A1C0-0x0043A28E` is a reviewed authored executable-checksum routine with a unique WinMain caller. It obtains the module path, calls the target file loader at `0x0044B360` with filename in live-in EAX plus stack `size_out/mode`, sums all dwords except the final one, and stores the checksum/size at `0x00492380/0x00492384`. `GameWindow::CalcExecutableChecksum` is an adjacent-supported proposed name only. | Attested TH10 caller/decompile/disassembly; direct target global writes; committed TH08 source only for naming/source-shape corroboration |
| MAIN-020 | observed / corroborated | `0x0043A290-0x0043A39F` is a reviewed authored shortcut resolver. Its unique caller passes the shortcut path on the stack while supplying destination buffer in live-in EBX and capacity `0x104` in live-in EDI; the callee returns with `ret 4`. The body uses COM/ShellLink operations to load and resolve the shortcut. `GameWindow::ResolveShortcut` is an adjacent-supported proposed name only; source declaration and physical COFF/LTCG owner remain unresolved. | Attested TH10 caller/decompile/disassembly and target boundary; committed TH08/TH095 source only for naming/source-shape corroboration |
| MAIN-021 | observed | `0x0043A3A0-0x0043A488` is a Ghidra-missed authored dialog callback passed directly by WinMain to `DialogBoxParamA`. It handles `WM_INITDIALOG`/`WM_COMMAND`, calls the target `SendMessageA`, `GetDlgItem`, `EndDialog`, and `IsDlgButtonChecked` imports, and all callback exits use `ret 0x10`; `0x0043A489-0x0043A48F` is CC padding. The machine ABI is the standard four-argument DLGPROC family, but the original identifier/source owner remains unknown. | Direct WinMain callback pointer, target import table and raw disassembly; attested xref to `0x0043A3A0` |
| TOOLCHAIN-005 | observed | A fixed natural three-argument `OpenFileProbe(path, sizeOut, mode)` diagnostic compiled as normal VC7.1 COFF uses three stack arguments. The same source compiled with `/GL` and linked once with `/LTCG` promotes all three surviving inputs to private EDX/EDI/ESI registers and returns with plain `ret`; it does **not** reproduce the TH10 loader's EAX-plus-two-stack/`ret 8` machine boundary. This is a negative source-shape diagnostic, not evidence against LTCG in a different whole-program context, and grants no exactness credit. | `.analysis/gpt-web/20260912-file-loader-owner/compiler-shape-report.txt`; pinned VC7.1 SP1 build-6030 compiler/linker execution |
| TOOLCHAIN-006 | observed | Maintained `src/PbgArchive.cpp` compiles successfully with the pinned VC7.1 SP1 toolchain as both normal COFF (`/TP /MT /O2 /Gy /GF /Oi /DNDEBUG`) and a `/GL` LTCG object. Under normal COFF, `PbgArchiveEntry::PbgArchiveEntry` is 9 bytes and fully byte-equal to target `0x004352C0-0x004352C8`, `PbgArchive::PbgArchive` is 16 bytes and fully byte-equal to target `0x00434C10-0x00434C1F`, and the 26-byte entry destructor matches all 22 comparable non-relocation bytes. At that checkpoint these were target-bound diagnostic results with no canonical units; `TOOLCHAIN-032` records their later bounded exact promotion without resolving production physical ownership. | `.analysis/gpt-web/20260912-archive-owner/compiler-shape-report.txt`; pinned compiler execution; direct target bytes |
| FILESYSTEM-001 | observed / corroborated | `0x0044B360-0x0044B4C7` is a reviewed authored file loader. Its machine boundary consumes path through live-in EAX, consumes `size_out` and mode from two stack slots, and returns with `ret 8`; `0x0044B4C8-0x0044B4CF` is CC padding. With mode zero it strips path components, searches 0x10-byte archive records rooted at `0x00497990`, obtains decompressed size from record `+0x08`, allocates output, and calls the archive reader. Otherwise it performs `CreateFileA`/`GetFileSize`/allocation/`ReadFile`/`CloseHandle`. `FileSystem::OpenFile` is an adjacent-supported proposed name only; source-level convention, TU/profile, source mapping, and exactness remain unknown. | Attested TH10 function/caller/callee/disassembly/xref queries; direct target bytes/imports; committed TH08/TH09/TH095 source used only after TH10 behavior was established |
| FILESYSTEM-002 | observed / corroborated | Target storage rooted at `0x00497990` is physically bounded to exactly `0x10` bytes: entries pointer `+0x00`, entry count `+0x04`, heap-owned archive filename `+0x08`, and polymorphic file backend `+0x0C`. Archive lifecycle code uses only those four dwords; a dedicated shutdown wrapper passes exactly `0x00497990` to the release body, while independent densely referenced storage begins at `0x004979A0`. `PbgArchive` is retained as a source-level proposed type name, not an original-symbol or TU-owner claim. | Attested TH10 lifecycle/decompile/disassembly/xrefs; direct static-destruction wrapper and next-storage access audit; committed adjacent layout only for later corroboration |
| FILESYSTEM-003 | observed / corroborated | `0x0044B4D0-0x0044B53E` is a reviewed authored file-existence helper using the same file-system critical section/count as `FILESYSTEM-001`. It attempts a read-only `CreateFileA`, closes a valid handle, returns 1/0, and uses one stack argument with `ret 4`; `0x0044B53F` is CC padding before `0x0044B540`. `FileSystem::CheckIfFileAlreadyExists` is an adjacent-supported proposed name only, and the callee-pop machine shape is not promoted to a source-level `__stdcall` claim. | Attested TH10 decompile/disassembly/xrefs and direct target boundary bytes; committed TH08/TH095 source used only for naming/source-shape corroboration |
| ARCHIVE-001 | observed / corroborated | The reviewed authored archive core consists of `0x00434C10` constructor, `0x00434C20` destructor, `0x00434C30` load, `0x00434D10` release, `0x00434DD0` read/decrypt/decompress, `0x00434EF0` decompressed-size query, `0x00434F30` entry lookup, `0x00434F70` header parser, `0x004350D0` entry allocator, `0x00435220` filename copier, `0x00435250` entry destructor, retained `0x00435270`/`0x00435280` cursor helpers, and retained `0x004352C0` entry constructor. Machine ABIs include several private EAX/EBX/ECX/EDI/ESI inputs and therefore are recorded independently from the natural C++ declarations. | Attested TH10 function/caller/callee/disassembly/xref queries; direct target boundaries/padding; pinned compiler diagnostics; adjacent source only for naming corroboration |
| ARCHIVE-002 | observed / corroborated | `0x00434F70-0x004350CD` parses the TH10 archive format from a 16-byte header. It decrypts the header with `0x1B/0x37` and 16-byte chunk/max values, requires magic `0x31414854` (`THA1`), decodes file-table decompressed size by subtracting `123456789`, compressed size by subtracting `987654321`, and entry count by subtracting `135792468`, then reads the table from the end of the archive, decrypts it with `0x3E/0x9B/0x80`, decompresses it, and builds entries. | Direct TH10 disassembly/constants/backend virtual calls; committed TH095 source used only after the TH10 format was independently recovered |
| ARCHIVE-003 | observed / corroborated | TH10 `0x004350D0` allocates `count + 1` records of exactly `0x10` bytes and copies each file-table record into filename `+0x00`, data offset `+0x04`, decompressed size `+0x08`, and a third metadata dword `+0x0C`. The extra sentinel record stores the archive data/table boundary at `+0x04` and zero at `+0x08`; `ReadDecompressEntry` computes compressed size as `entry[1].dataOffset - entry.dataOffset`. The `+0x0C` dword is copied but has no observed retail reader in the reviewed scope, so maintained source names it `unconsumedMetadata` without assigning semantics. | Direct TH10 `AllocEntries`/reader disassembly and array-construction calls; adjacent archive layouts only for corroboration |
| ARCHIVE-004 | observed / corroborated | Ghidra missed several complete archive source bodies. `0x00434C10-0x00434C1F` is directly supplied as the constructor to an MSVC vector-constructor iterator for twenty 0x10-byte objects and clears all four archive fields. `0x004352C0-0x004352C8` is directly supplied as the 0x10-byte entry constructor. `0x00435270-0x0043527B` and `0x00435280-0x004352B2` are retained cursor helpers whose operations are inlined inside `AllocEntries`. Conversely `0x00434D70-0x00434DCC` is a compiler-generated scalar/vector deleting-destructor dispatcher and is tracked as an exclusion, not authored code. | Direct TH10 static initializer/array iterator references and raw boundaries; pinned normal COFF automatically generated the corresponding deleting-destructor class; committed adjacent source for retained-helper naming |
| ARCHIVE-005 | observed | `0x00434CA0-0x00434D03` is a reviewed authored retained alternate archive-load body. It has no Ghidra caller, no target little-endian VA occurrence, and no direct/literal entry reference, but its CC-delimited physical extent and source-shaped behavior are independent: live-in ECX is the 0x10-byte archive object, live-in EAX is the resource name, and it performs `Release`, allocates a 0x10-byte backend with vtable `0x0046F328`, calls the shared archive parser, copies the resource name to archive `+0x08`, returns true on success, and calls `Release` on failure. Unlike ordinary `PbgArchive::Load`, it does not reopen the backend after parsing because the resource-backed implementation already owns the complete resource bytes. Maintained `PbgArchive::LoadFromResource` is a descriptive reconstruction name, not an original identifier/TU claim. | Direct TH10 disassembly/padding, attested function/decompile/callee/xref queries, whole-target direct/literal VA scan, and the independently reviewed resource-backed `IPbgFile` vtable |
| ARCHIVE-006 | observed | The exact retail target has no `RT_RCDATA` (resource type 10) entry at all: its PE resource directory contains only type 3 icon, type 5 dialog, and type 14 group-icon resources. `CWin32ResourcePbgFile::Open` searches `FindResourceA(NULL, name, RT_RCDATA)`, while the retained `0x00434CA0` archive resource-load body has no target entry reference. Therefore no concrete archive resource name can be recovered from this executable and this source path is retained-but-unused in the current product image; absence of current use does not establish original TU or compiler ownership. | Direct `objdump -x` parse of the attested target `.rsrc` directory plus no-reference audit for `0x00434CA0` |
| SOURCE-002 | observed / corroborated | TH10 has maintained candidate source in `src/PbgArchive.hpp/.cpp`, `src/PbgFile.hpp/.cpp`, `src/ResFile.hpp/.cpp`, `src/Decompress.hpp/.cpp`, `src/FileSystem.hpp/.cpp`, `src/Lzss.hpp/.cpp`, and `src/ReplayManager.hpp/.cpp`. Fifty-five reviewed authored functions are source-present after the replay loader packet. These mappings assert maintained reconstruction source only; they do not identify original TH10 function identifiers/translation units, normal-COFF/LTCG physical owners, the production link graph, or exactness. `config/build.toml` remains intentionally open with no original source owner assigned. | Maintained source plus successful pinned normal-COFF and `/GL` compiles; tracking validation; TH10 target-local control-flow/ABI/data-owner evidence |
| TOOLCHAIN-007 | observed | Maintained `src/PbgFile.cpp` compiles with the pinned VC7.1 SP1 toolchain as both fixed-profile normal COFF (`/TP /MT /O2 /Gy /GF /Oi /DNDEBUG`) and a `/GL` LTCG object. In normal-COFF probe mode, target-bound relocation-masked comparisons are structural-exact for `CPbgFile::~CPbgFile` (34/34 comparable bytes), `Close` (30/30), `Read` (53/53), `Tell` (21/21), `GetSize` (17/17), and `Seek` (33/33). `Write`, `Open`, `ReadWholeFile`, `GetFullFilePath`, and constructors retain code-shape/profile/private-register differences. At that checkpoint the results had `acceptance_authority=none` and no relocation-complete canonical units were created; `TOOLCHAIN-030` records their later bounded exact promotion without resolving production physical ownership. | `.analysis/gpt-web/20260912-pbgfile-backend/compiler-shape-report.txt`; pinned compiler execution; target-bound probe comparator |
| PBGFILE-001 | observed / corroborated | TH10 `CPbgFile` storage is exactly `0x0C` bytes in reviewed construction sites: vptr `+0x00`, Win32 handle `+0x04`, and desired access `+0x08`. Target derived vtable `0x0046F230` has nine slots in this exact order: `Open` `0x00435370`, `Close` `0x00435460`, `Read` `0x00435490`, `Write` `0x004354D0`, `Tell` `0x00435510`, `GetSize` `0x00435530`, `Seek` `0x00435550`, compiler deleting destructor `0x00435320`, and `ReadWholeFile` `0x00435580`. The base vtable at `0x0046F210` supplies seven pure-virtual slots plus its deleting destructor. | Direct TH10 rdata/vtable bytes, constructor/temp-object writes, archive backend allocation size, and attested xrefs; committed TH08/TH095 declarations used only after target slot recovery |
| PBGFILE-002 | observed / corroborated | Reviewed authored `CPbgFile` bodies are constructor `0x00435300-0x00435314`, destructor `0x00435340-0x0043536D`, `Open` `0x00435370-0x0043545E`, `Close` `0x00435460-0x00435481`, `Read` `0x00435490-0x004354C8`, `Write` `0x004354D0-0x0043550E`, `Tell` `0x00435510-0x00435528`, `GetSize` `0x00435530-0x00435544`, `Seek` `0x00435550-0x00435574`, `ReadWholeFile` `0x00435580-0x00435608`, and `GetFullFilePath` `0x00435610-0x00435688`. The target path helper consumes filename through live-in EAX and output buffer through ECX; constructor consumes live-in EAX, while ordinary virtual methods use the expected ECX `this` plus callee-popped stack arguments where applicable. Source-level declarations are natural C++; private machine boundaries are not promoted to source calling-convention claims. | Direct TH10 disassembly/boundaries/imports/vtable; attested decompile/xrefs; maintained source compiler feedback |
| PBGFILE-003 | observed | Denominator review found five Ghidra-missed virtual methods at `0x00435490`, `0x004354D0`, `0x00435510`, `0x00435530`, and `0x00435550`, plus the Ghidra-missed `CPbgFile` constructor at `0x00435300`. `0x00435320-0x0043533D` and `0x004356A0-0x004356BE` are MSVC deleting-destructor helpers and are classified compiler-generated exclusions. Three additional CC-delimited raw bodies remain deliberately unresolved: destructor-shaped `0x00435690-0x00435696`, constructor-shaped `0x004356C0-0x004356C6`, and slash-pointer helper `0x004356D0-0x004356E3`; plausible shape is not enough to force source origin/ownership. | Direct target vtable/raw bytes; pinned normal-COFF generated helper shapes; no-origin policy for unreferenced retained bodies |
| PBGFILE-004 | observed | The connected higher-level pack/file seam is independent from the `CPbgFile` vtable. `0x004356F0-0x004357C3` derives a `.dat` name from the first path component and searches active 0x10-byte `PbgArchive` objects at `0x004923B0` using count `0x00477850`. Ghidra-missed `0x004357D0-0x004357F3` selects the next archive object, calls reviewed `PbgArchive::Load`, and increments that count on success. `0x00435800-0x004358D5` reads from a matching archive or a temporary disk `CPbgFile`; `0x004358E0-0x0043599D` returns the corresponding archive or disk size. The `0x00436400` ResFile owner seam consumes the latter two helpers. | Direct TH10 raw disassembly/global references and bounded call scans; attested decompile/xrefs; ResFile caller context |
| PBGFILE-005 | observed / exact-normal-coff | `CPbgFile::Write @ 0x004354D0` is a 63-byte authored normal-COFF leaf. Writing the final byte-count equality as an explicit `if (dataLen == numBytesWritten) return true; return false;` causes pinned VC7.1 SP1 build6030 to emit the retail post-`WriteFile` sequence `cmp esi,[esp+4]; sete al` instead of materializing the stack value in ECX and zeroing EAX first. Canonical unit `pbgfile-write` reproduces all 63 bytes and its sole DIR32 relocation to `__imp__WriteFile@20` at `0x004660D4`; two independent cold replays pass, and the six pre-existing normal-COFF exact units sharing `PbgFile.obj` remain exact. This bounded codegen fact does not establish production TU or LTCG ownership. | Maintained natural source; pinned VC7.1 SP1 normal-COFF cold replay; complete target extent and relocation replay |
| SOURCE-003 | observed | The target string table contains `.\src\pack\File.cpp:233 void` at `0x0046F254` and `.\src\pack\ResFile.cpp:82 BYTE` at `0x0046F348`, proving those source paths participated in the original build. Attested `xrefs_to`, bounded text-immediate scans, and raw pointer scans across the complete ResFile string range find no code reference, so neither string assigns any reviewed function to an original translation unit. Maintained `src/PbgFile.cpp` and `src/ResFile.cpp` are therefore reconstruction source mappings, not claims of original `src/pack/File.cpp`/`ResFile.cpp` physical ownership. | Attested TH10 string/xref queries plus bounded direct target immediate/pointer scans and reviewed ResFile vtables |
| TOOLCHAIN-008 | observed / corrected | Maintained `src/ResFile.cpp` compiles with the pinned VC7.1 SP1 toolchain as both fixed-profile normal COFF (`/TP /MT /O2 /Gy /GF /Oi /DNDEBUG`) and a `/GL` LTCG object. Normal-COFF probes were structural-exact for the descriptive memory-file destructor, `Close`, `Read`, `Write`, `Tell`, and `GetSize`; `TOOLCHAIN-031` records their later strict canonical promotion. Current strict COMDAT/target extents for the remaining mismatches are constructor 20/18 bytes, memory-file `Open` 51/46, `Seek` 125/107, and Win32-resource `Open` 197/168. Production physical ownership remains unresolved. | Historical `.analysis/gpt-web/20260912-resfile-owner/`; current strict batch probe; pinned compiler execution |
| TOOLCHAIN-009 | observed | After adding the natural descriptive `PbgArchive::LoadFromResource` source and using the target-verified `IPbgFile *` backend type, pinned VC7.1 SP1 compiles `src/PbgArchive.cpp` as both fixed-profile normal COFF and `/GL`. The normal `LoadFromResource` body is also exactly 100 bytes but matches only 10/72 comparable bytes: standalone source loads the resource name from the ordinary stack ABI and calls the base memory-file constructor, while target `0x00434CA0` receives the name in EAX and inlines the 0x10-byte derived construction. The same source causes VC7.1 to synthesize an 18-byte `CWin32ResourcePbgFile` constructor and a 30-byte deleting destructor; the constructor has a different base-call/private-register shape, while the deleting destructor matches all 22 comparable non-relocation bytes at target `0x00436660`. These are compiler/source-shape diagnostics only, with no canonical exactness authority. | `.analysis/gpt-web/20260912-archive-resource-load/compiler-shape-report.txt`; pinned compiler execution; target-bound probe comparator |
| FILESYSTEM-004 | observed / corroborated | `0x0044B0D0-0x0044B212` is a reviewed authored in-place data transform. Every target caller loads the initial XOR seed in live-in AL, then pushes data pointer, size, XOR increment, chunk size, and max bytes; the callee returns with `ret 0x14`. It preserves a small untransformed tail derived from `size % chunkSize`, `chunkSize / 4`, and size parity, copies `min(size,maxBytes)` bytes to scratch, then processes each bounded chunk by consuming scratch forward while writing alternating bytes backward from the chunk end and incrementing the XOR key. It frees scratch and returns the original buffer. Maintained `FileSystem::Decrypt` is adjacent-supported naming after TH10-local ABI/behavior recovery, not an original-symbol/TU claim. | Six TH10 raw call sites and full target disassembly; attested caller/decompile evidence; committed TH09/TH095 source only for later source-shape corroboration |
| FILESYSTEM-005 | observed / corroborated | `0x0044B220-0x0044B352` is the reviewed authored inverse transform paired with `FILESYSTEM-004`. Its machine boundary consumes the initial XOR seed in live-in AL plus five stack arguments (data, size, XOR increment, chunk size, max bytes) and returns with `ret 0x14`; `0x0044B353-0x0044B35F` is CC padding before `0x0044B360`. It copies `min(size,maxBytes)` bytes to scratch, preserves the same small tail rule as Decrypt, then reads each scratch chunk in reverse odd/even order while writing sequentially back to the original buffer and evolving the XOR key. Replay save calls it twice with `(seed, increment, chunk) = (0x3D,0x7A,0x80)` then `(0xAA,0xE1,0x400)`; score save uses `(0xAC,0x35,0x10)`, and all observed calls use `maxBytes == size`. Maintained `FileSystem::Encrypt` is adjacent-supported naming after TH10-local recovery, not an original-symbol/TU claim. | Attested TH10 function/decompile/caller/callee/xref/disassembly evidence; three raw TH10 call sites and direct boundary bytes; committed TH09/TH095 source only for later naming/source-shape corroboration |
| FILESYSTEM-006 | observed / exact-normal-coff | `FileSystem::CloseWriteFile @ 0x0044B7E0` has a complete 37-byte authored boundary. When the shared file handle is valid, it closes the handle, leaves the shared critical section, decrements the active-file byte, and returns zero. Natural maintained source emits the same 37 bytes with pinned VC7.1 SP1 normal COFF; two independent cold builds and a focused canonical replay pass all five complete DIR32 fields (three globals and two Win32 imports) with zero differences. This bounded exact claim does not establish original source/TU, production codegen profile, or ownership of the referenced data. | IDA target disassembly/callers and reviewed boundary; canonical `file-system-close-write-file` normal-COFF unit and cold comparison receipts under `.analysis/gpt-5.6-sol/20260918-close-write/` |
| COMPRESSION-001 | observed / corroborated | `0x00435DC0-0x00435F9A` is a reviewed authored LZSS-style decoder with four stack arguments and `ret 0x10`: input, compressed size, optional output buffer, and output size. It allocates output only when the caller supplies null, starts the ring head at 1, decodes a one-bit literal/backreference tag, uses a 13-bit match offset where zero terminates the stream, uses a 4-bit length field representing actual length minus three, mirrors every output byte into a 0x2000-byte ring with mask `0x1FFF`, and consumes trailing bits through the same fetch path. Maintained `DecompressData` is adjacent-supported naming after TH10 algorithm recovery, not an original identifier/TU claim. | Full TH10 disassembly plus four attested call sites; committed TH08/TH09/TH095 decoder source only after target-local algorithm recovery |
| COMPRESSION-002 | observed | The decoder/encoder dictionary is physically bounded to exactly `0x2000` bytes at `0x0048F868-0x00491867`. Decoder and encoder accesses mask indices with `0x1FFF`; the reviewed neighboring encoder clears all 0x2000 bytes before encoding; independent densely used storage begins exactly at `0x00491868`. The same ring is therefore shared encoder/decompressor state rather than decoder-private scratch. Maintained source defines one `g_DecompressionRing[0x2000]`; original source/data TU ownership remains unknown. | TH10 decoder/encoder target accesses and mask constants; target xrefs at the next storage boundary; compile-time maintained-source size assertion |
| COMPRESSION-003 | observed / corroborated | Two complete LZSS state helpers were missing from the Ghidra denominator. `0x00435FA0-0x00435FC3` consumes the root through live-in EAX, writes it into the sentinel tree node, sets `tree[root].parent = 0x2000`, and clears both child links; `0x00435FD0-0x00435FF9` clears the entire 0x2000-byte dictionary and all 0x2001 three-dword tree nodes. Both are CC-delimited, have no modeled entry xrefs, and their behavior is inlined in the neighboring encoder. `Lzss::InitTree` and `Lzss::InitEncoderState` are adjacent-supported proposed names only. A natural normal-COFF `InitEncoderState` probe is also 42 bytes and matches all 30 comparable non-relocation bytes; the 36-byte `InitTree` probe differs because standalone source reads the root from the stack while target consumes EAX. `TOOLCHAIN-033` records the later relocation-complete exact promotion of only `InitEncoderState`. | Direct TH10 raw boundaries/storage writes and encoder inline copy; Ghidra no-function/no-xref results; pinned VC7.1 helper probes; adjacent source only for naming |
| COMPRESSION-004 | observed / corroborated | `0x004359B0-0x00435DB4` is a reviewed 1,029-byte authored LZSS encoder with three stack arguments and `ret 0x0C`: input, input size, and output-size pointer. Its two retail callers use that exact boundary and consume the returned allocated buffer. The target allocates `inputSize*2`, resets the shared ring/tree state inline, preloads up to 18 bytes, emits literal or 13-bit-offset/4-bit-length match tokens, maintains the sliding dictionary/tree, writes a zero-offset terminator, stores compressed output size, and returns the buffer. The connected target tree helpers are reviewed as `Lzss::AddString` `0x00436000-0x00436203`, `DeleteString` `0x00436210-0x0043625F`, `ContractNode` `0x00436260-0x004362AE`, `ReplaceNode` `0x004362B0-0x00436324`, and `FindNextNode` `0x00436330-0x00436360`. Their machine boundaries use private EDX/ECX/ESI/EAX inputs; maintained source remains natural C++ and does not claim those as source calling conventions. | TH10 full disassembly, attested call graph/callers, raw caller sequences, and shared ring/tree accesses; committed adjacent LZSS source only after target-local recovery |
| COMPRESSION-005 | observed | The LZSS tree is physically bounded to exactly `0x00477858-0x0048F863`: 0x2001 nodes of 0x0C bytes with fields parent/left/right. `tree[0x2000]` is the sentinel/root node; its right field at `0x0048F860` is written by encoder initialization and read by AddString. `0x00477850` belongs to an independent archive counter, `0x00477854` has no observed reference, and `0x0048F864-0x0048F867` is a four-byte unreferenced gap before the separately reviewed 0x2000-byte dictionary at `0x0048F868`. Maintained `g_LzssTree[0x2001]` mirrors this target-proven extent, while original source/data-TU ownership remains unknown. | TH10 address arithmetic, all tree helper accesses, attested xrefs at both boundaries, and compile-time maintained-source layout assertions |
| COMPRESSION-006 | observed / unknown | TH10 retains source-path strings `.\src\pack\LzssUtil.cpp:201 BYTE` and `.\src\pack\LzssUtil.cpp:56 BYTE`, establishing that `src/pack/LzssUtil.cpp` participated in the original build. Neither string start has a modeled code xref, so these strings do not bind the reviewed encoder/decoder/tree functions or data objects to that original TU. Maintained names `CompressData` and `Lzss::*` are therefore source-shape/naming choices supported by TH10 behavior and adjacent committed source, not recovered original-symbol or physical-TU claims. | Attested TH10 string search and negative xref query; target-local LZSS behavior; committed TH08/TH09/TH095 naming corroboration |
| TOOLCHAIN-011 | observed / corrected | Natural `src/Lzss.cpp` compiles with pinned VC7.1 SP1 build 6030 as both fixed-profile normal COFF and the same source plus `/GL`. The old equal-extent claim came from the unsafe requested-window fallback corrected in `TOOLCHAIN-035`. Current strict normal COMDAT/target extents are CompressData 878/1029, InitTree 40/36, InitEncoderState 42/42, AddString 414/516, DeleteString 99/80, ContractNode 89/79, ReplaceNode 123/117, and FindNextNode 49/49 bytes. Only `InitEncoderState` is structural-exact and subsequently canonical exact under `TOOLCHAIN-033`; the other seven current strict probes are mismatches. Standalone normal source uses ordinary stack/helper calls while target inlines more tree operations and lowers surviving helper inputs to private registers. | Current strict full-backlog batch probe; historical `.analysis/gpt-web/20260912-lzss-encoder/`; pinned compiler and raw call-site ABI review |
| TOOLCHAIN-010 | observed / corrected | Maintained `src/Decompress.cpp`, `src/FileSystem.cpp`, and `src/PbgArchive.cpp` compile successfully with the pinned VC7.1 SP1 toolchain under both fixed-profile normal COFF and `/GL`. The old equal-extent claims came from the unsafe requested-window fallback corrected in `TOOLCHAIN-035`. Current strict normal COMDAT/target extents are 482/475 bytes for `DecompressData`, 278/323 for `FileSystem::Decrypt`, and 278/307 for `FileSystem::Encrypt`; all three are mismatches. A historical declaration-order Decompress probe remains only a source-shape hypothesis until rerun through the strict comparator. Successful `/GL` builds establish no per-function physical ownership or exactness. | Current strict full-backlog batch probe; historical `.analysis/gpt-web/20260912-archive-transform/` and `.analysis/gpt-web/20260912-filesystem-encrypt/`; pinned compiler execution |
| RESFILE-001 | observed | The target has a 0x10-byte memory-backed `IPbgFile` implementation with vtable `0x0046F308`: `Open` `0x00436400`, `Close` `0x00436430`, `Read` `0x00436460`, read-only `Write` `0x00436370`, `Tell` `0x004364D0`, `GetSize` `0x004364E0`, `Seek` `0x004364F0`, and deleting destructor `0x004363A0`. Raw constructor `0x00436380-0x00436391` writes that vtable and zeroes fields. Storage is vptr `+0x00`, DWORD size `+0x04`, current byte pointer `+0x08`, and owned malloc buffer `+0x0C`. Maintained `CMemoryPbgFile` is a descriptive reconstruction name, not an original identifier claim. | Direct TH10 rdata/vtable bytes, CC-delimited raw disassembly, field accesses, and target-bound compiler probes |
| RESFILE-002 | observed | The memory-file implementation is read-only and owns its buffer. `Open` calls the reviewed archive-or-disk data and size helpers, stores buffer/size/current, and ignores its second stack argument. `Close` frees the buffer and clears all three fields. `Read` copies the requested bytes when available or the remaining tail and advances current; `Write` always returns false; `Tell` is current-minus-base and `GetSize` returns `+0x04`. `Seek` implements target-specific `FILE_BEGIN/CURRENT/END` bounds, including allowing negative current-relative movement without a lower-bound check and rejecting exact-end/exact-beginning boundary cases. Maintained source deliberately preserves these observed edge conditions rather than normalizing them. | Direct TH10 disassembly of `0x00436370-0x0043655A`; both vtable references; reviewed `0x00435800`/`0x004358E0` helper contracts |
| RESFILE-003 | observed | A second vtable at `0x0046F328` is a derived 0x10-byte Win32-resource-backed implementation. It shares `Close`/`Read`/`Write`/`Tell`/`GetSize`/`Seek` with `0x0046F308`, overrides `Open` with `0x004366C0-0x00436767`, and has deleting destructor `0x00436660`. A direct allocation site at `0x00434CA0` performs `operator new(0x10)`, clears the three fields, and writes `0x0046F328`. The override closes prior data, loads `RT_RCDATA` using `FindResourceA`/`LoadResource`/`LockResource`, mallocs and copies `SizeofResource` bytes, and target exits return true even on the reviewed failure paths after cleanup. Maintained `CWin32ResourcePbgFile` is descriptive only. | Direct TH10 allocation/vtable/disassembly/import evidence; attested decompile at `0x00434CA0` and `0x004366C0` |
| RESFILE-004 | observed / unknown | Denominator review added eight Ghidra-missed CC-delimited ResFile entries: `0x00436370`, `0x00436380`, `0x00436400`, `0x00436460`, `0x004364D0`, `0x004364E0`, `0x004364F0`, and `0x00436640`. The first seven are reviewed authored memory-file source bodies. `0x00436640-0x00436651` and existing `0x00436680-0x004366B0` remain deliberately `unknown/review`: each is consistent with a compiler-synthesized derived constructor/destructor with inlined base lifecycle, but TH10 cannot distinguish that from explicit source. `0x004363A0` and `0x00436660` are independently classified compiler-generated deleting destructors. `0x004362B0/0x00436330` were correctly excluded from the ResFile owner; later target-local compression review identifies them as LZSS tree helpers rather than unrelated ResFile code. `0x00436570` remains outside this owner as process-launch logic. | Direct CC boundaries/vtables; bounded target analysis; later LZSS tree owner evidence refines the earlier routing without changing the ResFile boundary |
| STARTUP-001 | observed / corroborated | `0x0041F990-0x0041FABA` is a Ghidra-missed, CC-delimited authored startup worker body. `0x0041FAC0` stores its address at an object field and passes it to `_beginthreadex` with the same object as thread argument; its unique caller `0x0041FD00` first allocates 0x3F0 bytes, invokes `0x0041F850`, and then calls the thread launcher. The worker loads `sig.anm` and `text.anm`, reports text-initialization failure, loads `../../bgm/thbgm.fmt`, checks `thbgm.dat`, and reports BGM-initialization failure. The target returns with plain `ret`, so source callback convention and object type remain unresolved; the 0x3F0 allocation is not used to infer a type. `Supervisor::StartupThread` is an adjacent-supported proposed name because corresponding TH08/TH09/TH095 startup functions own the text/BGM sequence, not an original-symbol claim. | Direct TH10 raw boundary/string inspection; attested caller/decompile chain through `0x0041FAC0`/`0x0041FD00`; committed TH08/TH09/TH095 startup source used only for naming/source-shape corroboration |
| SOUND-002 | observed / corroborated | Two additional Ghidra-missed, CC-delimited authored sound bodies are tracked. `0x0043D080-0x0043D0EA` is passed as a thread start address, loads all 37 target `se_*.wav` entries through `0x0044B360`, stores their buffers, aborts on stop state 2, and waits with `Sleep(1)` while stop state is zero. `0x0043D0F0-0x0043D111` launches it with `CreateThread`, stores the handle at `0x004977AC`, and returns it; `0x00420100` contains the same launcher sequence inline. The target KERNEL32 IAT binds `0x004660AC` to `Sleep` and `0x00466118` to `CreateThread`. `SoundDataLoaderThread` and `StartSoundLoadThread` are adjacent-supported proposed names only. | Direct TH10 raw disassembly, filename-table decoding, and PE import-table mapping; attested xrefs; committed TH095 source only for naming/source-shape corroboration |
| SOURCE-001 | observed | The target string table contains `.\src\core\main.cpp:92 SprtCtrlInf` and `.\src\core\main.cpp:84 FuncCtrlInf`, proving that a `src/core/main.cpp` source path participated in the original build. Ghidra and bounded raw scans find no code reference to either path string, so this evidence does not assign any particular function or final-image extent to that translation unit and is not a source mapping by itself. | Attested TH10 string search plus bounded raw literal scan |
| CRT-001 | observed / corroborated | The target entry `0x004537DC` is VC runtime startup code rather than authored game logic: it initializes runtime state, argv/environment/heap/thread/IO support, constructs the WinMain argument set, calls `0x00438AD0`, and exits through CRT routines. | Attested Ghidra disassembly/decompile/callees plus direct target inspection |
| CRT-002 | observed | Target `0x00452706-0x00452717` is the 18-byte CRT `malloc` wrapper: it pushes the requested size and runtime new-mode global to `__nh_malloc`, is reached by 94 direct REL32 calls, and ends before CC padding. Target `0x0046054E-0x004605B6` is the 105-byte CRT `_stricmp` wrapper: its default-locale path calls the adjacent ASCII comparison implementation, its locale path performs per-byte mapping, 11 direct REL32 calls use the entry, and the next function begins immediately at `0x004605B7`. Both are classified library/CRT and excluded from the authored denominator without claiming an exact original CRT library member. | Hash-attested target bytes; complete `.text` direct-call scans; entry/tail and callee disassembly |
| CRT-003 | observed / boundary-partial | Target entry `0x00452930` is a complete 45-byte CRT `strrchr` implementation: it finds the terminator, scans backward for the requested byte, ends at `0x0045295C`, and is followed by CC padding; ten direct REL32 calls use this entry. It is classified library/CRT and excluded without claiming an exact original library member. Target entry `0x00452730` implements optimized `strchr` behavior and is reached by 18 direct references, including the `CPbgFile::GetFullFilePath` call at `0x00435619`, but match exits branch backward to the shared return tail at `0x00452720-0x00452724`, outside Ghidra's `0x00452730-0x004527ED` candidate. Its semantic entry is reviewed for the caller's linked field while its complete physical boundary/origin disposition deliberately remains unknown/review. The same PbgFile helper calls the KERNEL32 `GetModuleFileNameA` import through IAT slot `0x004660DC`, whose stored hint/name RVA resolves to that import. | Hash-attested target disassembly; complete `.text` direct-reference scans; PE import/IAT decoding; PbgFile caller control flow |

Do not turn cross-game similarity, a decompiler type, or an attractive reused
name into a TH10 fact without target-local evidence.
| REPLAY-001 | observed / corroborated | `0x00429B60-0x0042A1F0` is a reviewed 1,681-byte authored replay-save writer. `0x00429B54-0x00429B5F` and `0x0042A1F1-0x0042A1FF` are CC padding. Two direct TH10 callers at `0x00423F16` and `0x0043399D` load the replay owner from `0x00477838` into ECX, place the formatted replay path in EDX, push one replay-name pointer, and ignore the return value; the callee ends with `ret 4`. The target also retains `src/game/replay.cpp`/`.h` source strings and replay allocation/type labels. `ReplayManager::SaveReplay` is retained as a maintained descriptive name after TH10-local recovery and committed adjacent corroboration, not as an original symbol or source-level calling-convention claim. | Direct TH10 caller/body/tail disassembly; attested function/caller/xref evidence; target replay source/type strings; committed TH08/TH09/TH095 source only for later naming corroboration |
| REPLAY-002 | observed | The replay-save and replay-load paths establish a 0x64-byte replay-data header and 0x1C4-byte per-stage header with signed stage index at `+0x00`, six-byte record count at `+0x04`, and packed payload size at `+0x08`. Runtime frame storage has 3,600 six-byte records through `+0x545F`, record-end pointer at `+0x5460`, 3,600 one-byte FPS samples at `+0x5464`, FPS-end pointer at `+0x6274`, and an embedded 0x0C intrusive list node at `+0x6278`; `0x0042AA50` allocates exactly `0x6284` bytes and initializes the node value to the frame base. The writer serializes only the record/sample streams, while `0x0042A200` independently reconstructs record/fps cursors from the same `recordCount * 6` relationship. | Full bounded TH10 writer/load disassembly; raw `0x0042AA50` allocation/init body; target `ReplayRecDataInf`/`ReplayFrameDataInf` strings |
| REPLAY-003 | observed / corroborated | After packing, the replay-save body calls reviewed `CompressData`, applies `FileSystem::Encrypt` with `(seed, increment, chunk) = (0x3D,0x7A,0x80)` and then `(0xAA,0xE1,0x400)`, stores uncompressed/compressed sizes at replay file-header `+0x20/+0x1C`, and sets user-data offset `+0x0C` to `compressedSize + 0x24`. It opens `replay/%s`, writes the 0x24-byte file header and compressed payload, then emits two aligned `USER` blocks from a 0xFFFF-byte scratch buffer: type 0 contains version/name/date/character/rank/stage/score/slow-rate text and type 1 contains the retained comment text. Short-write paths close the handle and release the write critical section. Committed TH095 shows the same file-header/encryption/USER family, but TH10's eight-stage front half and all offsets here are target-local facts. | Direct TH10 raw disassembly/imports/format strings plus reviewed CompressData/Encrypt bodies; committed TH095 source used only after TH10 behavior recovery |
| REPLAY-004 | observed / corroborated | The reviewed replay lifecycle cohort is `0x004294A0-0x0042960E` (`ReplayManager::~ReplayManager`, 367 bytes), `0x00429610-0x004296C8` (`ReplayManager::Create`, 185), `0x004296F0-0x004297A4` (`ReplayManager::Load`, 181), `0x004297B0-0x004297C1` (`ReplayManager::Destroy`, 18), and `0x004297D0-0x00429A23` (`ReplayManager::ProcessFrame`, 596). The maintained names were selected only after TH10-local lifecycle/callsite behavior was recovered and are corroborated by committed adjacent ReplayManager source; they are not original-symbol, source-ABI, TU, or physical-owner claims. A previously untracked CC-delimited body at `0x004296D0-0x004296E8` has the MSVC scalar-deleting-destructor pattern and is classified compiler-generated/excluded, expanding the candidate denominator without expanding authored code. | Attested TH10 function/decompile/xref analysis; direct raw boundaries/calls; pinned VC7.1 deleting-helper synthesis; committed TH095/TH08 source used only for later naming corroboration |
| REPLAY-005 | observed | TH10 replay-object allocation/clear sites establish an exact maintained object size of `0x2D4`. Target-observed fields include mode `+0x10`, file header `+0x14`, replay data `+0x18`, eight stage-header pointers `+0x1C`, eight 0x0C intrusive frame-list roots beginning `+0x3C`, current frame link `+0x9C`, eight 0x24 stage states beginning `+0xA0`, decompressed payload `+0x1C0`, replay FPS byte `+0x1C4`, frame counter `+0x1C8`, a chain pointer `+0x1CC`, active stage `+0x1D0`, and 0x100-byte replay path `+0x1D4` through the object end. Each 0x24 stage state contains record/fps start+cursor pairs at `+0x00/+0x04/+0x08/+0x0C`, stage header `+0x10`, record index `+0x14`, and an embedded 0x0C intrusive node at `+0x18`. | Direct TH10 allocation/clear loops, raw `0x0042AC20/0x0042AC60` stage-state ctor/dtor bodies, `0x0042A200` load-state reconstruction, `0x0042AA50/0x0042AB20` frame-list allocation/free behavior |
| REPLAY-006 | observed | `ReplayManager::ProcessFrame` is target-locally a record/playback dispatcher. A wrapper at `0x0042A3D0` receives the replay owner in ECX, moves it to EDI, and calls `0x004297D0`. Record mode snapshots three u16 input fields into six-byte records, samples FPS every 30 frames using target constants `0.5f` and `256.0f`, and allocates a new 0x6284 frame block when the current block fills. Playback mode consumes the same six-byte records from the selected 0x24 stage state, reads the current FPS byte every frame while advancing its FPS cursor every 30 frames, clears inputs when the stream or active stage is unavailable, and advances record/frame indices. The private EDI machine ABI is not promoted to a source-level calling convention. | Attested decompile/xref plus direct wrapper/raw disassembly and target `.rdata` constants |
| REPLAY-007 | observed / corroborated | `0x00428F60-0x0042949F` is the reviewed 1,344-byte shared replay initializer mapped descriptively as `ReplayManager::Initialize`. `0x00428F5A-0x00428F5F` is CC padding after the preceding body, and the initializer ends at the `0x0042949F` RET immediately before the reviewed destructor at `0x004294A0`. Its sole caller loads the 0x2D4 replay owner in EBX, mode in EAX, and path in ECX; the body itself uses plain RET. Mode 0 publishes the global replay owner, initializes frame/header/stage storage and three chain elements; mode 1 loads replay data, restores runtime/current-stage state, installs the same chains, and leaves active stage `-1`; mode 2 performs load-only initialization without publishing the global owner or installing callbacks. The private EBX/EAX/ECX boundary is not promoted to a source calling convention. | Attested initializer function/decompile/caller/callee/disassembly results; direct raw pre/post boundaries; reviewed `ReplayManager::Create`, `LoadReplay`, frame allocation/clear context; committed TH095/TH08 used only after TH10 recovery for natural naming/source-shape corroboration |
| REPLAY-008 | observed / unknown-origin | Initializer code establishes a maintained 0x24-byte chain-element layout: dword priority `+0x00`, flag word/dword storage `+0x04`, callback/lifetime slots `+0x08/+0x0C/+0x10`, embedded 0x0C intrusive node beginning `+0x14`, and replay-owner argument `+0x20`. It installs calc callbacks with priorities `0x0B` and `0x1B` and a draw callback with priority `5`. Six previously untracked CC-delimited replay bodies were added to the candidate denominator but deliberately remain `unknown/review`: `0x00429A30-0x00429A69`, `0x00429A70-0x00429B25`, `0x00429B30-0x00429B53`, `0x0042A3D0-0x0042A3D9`, `0x0042A3E0-0x0042A423`, and `0x0042A430-0x0042A44B`. The three `0x0042A3xx` entries are active callback addresses written by the initializer; `0x0042A430` tail-jumps to `0x00429A70`. Source-written callback versus retained/LTCG adapter/clone origin is unresolved, so none receives authored or source-present credit. | Direct target raw boundaries; initializer callback-address stores; attested xrefs; callback/FPS-body disassembly; whole-target raw VA reference checks; committed TH08 chain source only for later abstraction corroboration |
| TOOLCHAIN-012 | observed | Final maintained replay lifecycle source compiles with pinned VC7.1 SP1 build 6030 under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same profile plus `/GL`. True normal `/Gy` text-section extents are 199/80/30/77/27/456 bytes for the destructor/Create/compiler scalar-delete/Load/Destroy/ProcessFrame bodies versus target 367/185/25/181/18/596, so the standalone normal profile is not a physical match. VC7.1 automatically emits `??_GReplayManager@@QAEPAXI@Z` from the natural non-virtual destructor/delete source, corroborating the 25-byte target helper as compiler-generated without exactness credit. Probe-mode `compare-coff-function.py` may use the caller-supplied expected size when an old VC7.1 function symbol lacks an auxiliary extent; such a fixed window is not evidence of the object-owned function size, so this packet uses `/Gy` section extents for the normal-size diagnostic. `/GL` compilation preserves an LTCG hypothesis but has no standalone exactness authority. | Pinned compiler output and COFF section/symbol tables under `.analysis/gpt-web/20260913-replay-lifecycle/`; inspected comparator behavior; target raw helper shape |
| TOOLCHAIN-013 | observed | Maintained `ReplayManager::Initialize` source compiles with pinned VC7.1 SP1 build 6030 under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same source plus `/GL`. The true normal `/Gy` COMDAT section for `?Initialize@ReplayManager@@QAEHHPBD@Z` is `0x53B` = 1,339 bytes, while the reviewed target extent is 1,344 bytes, so the fixed normal object is not a physical match. A separate 1,344-byte comparator window is also a mismatch at 55/1,060 comparable bytes with `acceptance_authority=none`; its reported `object_size=1344` is caller-window size and is not used as owned-extent evidence. `/GL` compilation preserves an interprocedural/LTCG hypothesis, but target-bound linked-image ownership/exactness remains unavailable. | Pinned normal and `/GL` compiler output; normal COFF section/symbol tables; `scripts/compare-coff-function.py` diagnostic under `.analysis/gpt-web/20260913-replay-initialize/` |
| REPLAY-009 | observed / corroborated | `0x0042A200-0x0042A3C1` is the reviewed 450-byte replay loader mapped descriptively as `ReplayManager::LoadReplay`. `0x0042A1F1-0x0042A1FF` and `0x0042A3C2-0x0042A3CF` are CC padding, and all three direct call sites pass the replay owner in live-in ESI, push one replay path, and receive a callee-popped `ret 4`. The disk path builds `replay/%s`, opens a stateful replay read handle, validates file magic `0x72303174` plus version 5, and reads the compressed payload; the archive-selected path calls reviewed `FileSystem::OpenFile`. Both paths apply `Decrypt` with `(0xAA,0xE1,0x400)` then `(0x3D,0x7A,0x80)`, call `DecompressData`, and reconstruct per-stage header/record/FPS starts from the 0x64-byte replay header and 0x1C4-byte stage records. The target deliberately replaces `stageCount >= 8` with loop bound 6. | Attested TH10 function/decompile/caller/callee/xref results; direct target entry/tail/call-site disassembly; reviewed replay layouts and transform helpers; committed TH095 source only after TH10 behavior recovery for naming/source-shape corroboration |
| REPLAY-010 | observed / corroborated | `0x0042A450-0x0042A695` is the reviewed 582-byte replay stage-transition owner mapped descriptively as `ReplayManager::BeginStage`. Both direct call sites inside `0x00418190` load the global 0x2D4 replay owner into live-in EBX; the callee has no stack arguments and uses plain RET. The body marks each non-null replay chain element through flag bit 1. Record mode rotates the current stage frame list, conditionally refreshes stage-header scalar fields, then snapshots runtime coordinates, a 0x108-byte block, four groups of four 8-byte runtime pairs, `+0x1B8`, and runtime `+0x4474` into the 0x1C4 stage header. Playback mode restores the same snapshot, invokes the connected runtime rebuild/reset helpers, applies the target special-case `shotType + character * 3 == 5`, clears the four slot `+0x58` reset fields, and resets the replay frame counter. Ghidra reports 579 reachable body bytes because `0x0042A60D-0x0042A60F` is a retained three-byte alignment LEA skipped by the branch at `0x0042A60B`; the physical extent remains 582 bytes and is followed by CC padding at `0x0042A696-0x0042A69F`. The maintained `BeginStage` name is TH09-correlated only after TH10-local recovery; original TH10 symbol/TU remain unknown. | Attested TH10 decompile/caller/callee/xref results; direct raw entry/tail/call-site and state-copy disassembly; reviewed ReplayManager/stage layouts; committed TH09 BeginRecordingStage/BeginPlaybackStage used only for later source-family naming corroboration |
| FILESYSTEM-006 | observed / corroborated | The replay disk-read seam contains three newly reviewed authored bodies. `0x0044B6B0-0x0044B738` opens a read-only shared handle while holding the file critical section and is maintained descriptively as `ReplayFile::Open`; `0x0044B790-0x0044B7D9` consumes a private live-in EDI byte count, allocates that many bytes, reads from the shared handle, and is maintained as `ReplayFile::Read`; `0x0044B7E0-0x0044B804` closes a valid shared handle, releases the critical section, decrements the active-file count, and is maintained as `FileSystem::CloseWriteFile`. Each extent is followed by CC padding. These names are adjacent-supported after TH10-local behavior recovery; none has a maintained source mapping or original-TU/exactness claim. | Direct TH10 raw disassembly/imports/shared-state accesses; attested function/decompile/caller evidence where provider transport succeeded; committed TH095 `ReplayFile`/`FileSystem` source used only for later naming corroboration |
| TOOLCHAIN-014 | observed | Maintained `ReplayManager::LoadReplay` compiles with pinned VC7.1 SP1 build 6030 under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same source plus `/GL`. The true normal `/Gy` COMDAT for `?LoadReplay@ReplayManager@@QAEHPBD@Z` is `0x199` = 409 bytes with 16 relocations, versus the reviewed 450-byte target extent, excluding the fixed normal object as a complete physical match. A separate 450-byte comparator window is a mismatch at 31/386 comparable bytes with `acceptance_authority=none`; its reported 450-byte object size is caller-window size, not the owned COMDAT extent. `/GL` compilation keeps LTCG/interprocedural ownership possible but unproven, so no canonical match unit or exact row is created. | Pinned normal and `/GL` compiler output; COFF symbol/section table; `scripts/compare-coff-function.py` diagnostic under `.analysis/gpt-web/20260913-replay-loader/` |
| PLAYER-001 | observed | `0x00426F70-0x0042792D` is a reviewed 2,494-byte player option-rebuild owner mapped descriptively as `RebuildPlayerOptions`. Nine direct call sites across five caller functions push the same player/runtime owner; the callee reads one stack argument and returns with `ret 4`. Ghidra reports 2,471 reachable body bytes because three retained unreachable alignment sleds at `0x0042701A-0x0042701F` (6 bytes), `0x00427197-0x0042719F` (9 bytes), and `0x004278D8-0x004278DF` (8 bytes) account exactly for the 23-byte difference from the physical extent. TH10 retains `.\\src\\game\\player.cpp:193 PlayerInf`, but the original function identifier, TU ownership, and source calling convention remain unproven. | Attested TH10 function/caller/xref/decompile results; raw entry/epilogue and all nine call-site disassemblies; target player source/type string |
| PLAYER-002 | observed / corroborated | The reviewed option-rebuild owner establishes four 0x98-byte option records beginning at player `+0x32A0`, not at replay snapshot offset `+0x32D4`. Target-observed option fields include state `+0x00`, four 8-byte position pairs at `+0x34/+0x3C/+0x44/+0x4C`, primary/secondary VM ids `+0x68/+0x6C`, option index `+0x88`, reset flag `+0x8C`, and callback `+0x90`; option count is player `+0x3500`. Player position is `+0x3CC/+0x3D0`, option resource data is reached through `+0x45C`, a 33-pair/0x108-byte position history occupies `+0x436C-0x4473`, and mode is `+0x4474`. The body rebuilds secondary VMs, derives count from signed power divided by 20 and clamps it to four, recreates active options according to character/shot data, retires inactive VM state, writes the count, and sets all reset flags. Committed TH08 Player source corroborates a four-option Player subsystem only after these TH10-local facts were recovered; its different option layout is not transferred. | TH10 raw/decompiled player rebuild body and replay-stage copy seam; committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` used only for source-family corroboration |
| PLAYER-003 | observed / unknown-origin | `0x00427AD0-0x00427ADB` is a newly tracked 12-byte CC-delimited retained callback wrapper missed by the Ghidra denominator. The player rebuild stores its address at `0x00427372`; the wrapper preserves EDI, moves live-in ECX to EDI, calls the separate `0x00427AE0` candidate, returns zero, and uses plain RET. It is preceded by CC padding after the `0x00427960-0x00427AC6` body and followed by `0x00427ADC-0x00427ADF` CC padding. Maintained source presence is now recorded for the callback entry (see `PLAYER-004`/`PLAYER-005`), while source-written adapter versus compiler/optimizer wrapper origin and exactness remain unresolved. | Direct TH10 raw callback-corridor disassembly; attested `xrefs_to` for `0x00427AD0` |
| TOOLCHAIN-015 | observed | Natural maintained `src/Player.cpp` and the Player-layout-refactored `src/ReplayManager.cpp` both compile with pinned VC7.1 SP1 build 6030 under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same profile plus `/GL`. The true normal `/Gy` `RebuildPlayerOptions` COMDAT is 1,943 bytes versus the 2,494-byte target body; a separate caller-supplied 2,494-byte comparator window mismatches at 186/2,246 comparable bytes with `acceptance_authority=none`. Therefore the fixed standalone normal profile is not the physical target owner; `/GL` feasibility preserves an interprocedural/LTCG hypothesis but provides no per-function ownership or exactness credit. | `.analysis/gpt-web/20260913-player-rebuild/`; pinned compiler; COFF symbol/section table; target-bound diagnostic comparator |
| PLAYER-004 | observed / unknown-origin | The option record has two distinct callback-like tail slots. In the player movement/update owner, `0x004255CC` forms ECX as the option record base, `0x00425600` loads the function pointer at option `+0x90`, and `0x00425607` calls it. Separately, `0x004263B3-0x004263D5` iterates four 0x98-byte records, loads a pointer at option `+0x94`, and also calls it with ECX equal to the record base. Therefore the rebuild writes of `0x00427950`/`0x00427AD0` at player `+0x3330` are correctly option `+0x90`; the `+0x94` slot is a different callback. Later TH10-local draw-chain evidence identifies its functional role as the per-option draw callback, while its producer/assignment remains unidentified. `0x00427950` and `0x00427AD0` are source-present callback entries, but their source-written-versus-compiler-adapter origin remains `unknown/review`; their adjacent bodies remain separately unmapped. | Direct TH10 disassembly at `0x004255B9-0x00425609`, `0x004263B3-0x004263D5`, `0x00427372`, and `0x004275D9`; attested xrefs/call graph |
| PLAYER-005 | observed | The `+0x90` callback cohort establishes option `+0x84` as a prior/current option-mode field. The trail callback entry `0x00427950` adapts ECX to an ESI-bound body at `0x00427960`; that body uses option `+0x88` to maintain the player's eight-sample-per-option history, derives/stores replay pair `+0x44` when mode is zero, reconstructs seven intermediate samples when mode is nonzero using the target `0.125f` constant at `0x00470B90`, then refreshes pair `+0x34` and stores the current mode at `+0x84`. The special callback entry `0x00427AD0` adapts ECX to an EDI-bound body at `0x00427AE0`; on mode transitions it applies managed-VM delete state 6 or 3 to primary VM id `+0x68`, copies pair `+0x3C` to `+0x4C` when returning to mode zero, or pair `+0x4C` to `+0x34` when entering nonzero mode, and updates `+0x84`. | Attested TH10 decompile/disassembly/callees for `0x00427950`, `0x00427960`, `0x00427AD0`, `0x00427AE0`, and `0x00449470`; direct target constant bytes |
| TOOLCHAIN-016 | observed | Maintained `PlayerOptionTrailCallback` and `PlayerOptionSpecialCallback` source compiles with pinned VC7.1 SP1 build 6030 under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same profile plus `/GL`. The target-shaped direct-index trail source produces a true 358-byte normal `/Gy` COMDAT versus the 359-byte adjacent target body; its relocation-aware 358-byte body-prefix diagnostic is still `mismatch` at 89/338 comparable bytes with `acceptance_authority=none`. The special callback normal COMDAT is 111 bytes, numerically equal to the target wrapper-plus-body code-byte total `12+99`, but those target chunks are separated by four `CC` bytes and a 99-byte body diagnostic is `mismatch` at 7/87 comparable bytes. Fixed standalone normal therefore does not establish exact ownership; successful `/GL` compilation preserves the optimizer/LTCG split hypothesis without exactness credit. | `.analysis/gpt-web/20260914-player-option-callbacks/`; pinned compiler; normal COFF section table; target-bound diagnostic comparator |
| PLAYER-006 | observed / unknown-origin | Player initialization registers two CC-delimited callback entries through the shared 0x24-byte callback-node manager. `0x00426500-0x00426506` is stored in the first list (`0x00449AE0`, node retained at Player `+0x08`) and adapts live-in ECX to the large `0x00425730` Player update owner by pushing the Player pointer. `0x00426510-0x00426516` is stored in the second list (`0x00449B70`, node retained at Player `+0x0C`), moves ECX to EAX, and tail-jumps to `0x00426360`. The main frame owner executes the paired first-list runner `0x00449C00` before rendering and the second-list runner `0x00449D40` during the render path, establishing maintained update/draw roles without proving original identifiers or source-written adapter ownership. Both 7-byte entries were absent from the Ghidra denominator and are retained as `unknown/review`. | Direct TH10 initialization disassembly at `0x00424856-0x004248A8`; raw callback corridor `0x004264F9-0x0042651F`; attested callback-manager decompilation/xrefs and frame owner `0x00439390` |
| PLAYER-007 | observed / corroborated | `0x00426360-0x004264F8` is the 409-byte EAX-bound body behind the registered Player draw entry `0x00426510`. When Player `+0x458 != 2`, it writes the embedded draw VM at Player `+0x14` positions `+0x340/+0x344/+0x348` from Player float coordinates `+0x3C0/+0x3C4/+0x3C8` with target constants `224.0f` and `16.0f`, calls the draw-VM owner, then iterates four 0x98-byte options from Player `+0x3334` and invokes each non-null option `+0x94` hook with ECX equal to the option base. A complete TH10 `.text` displacement scan found no Player-local `+0x94` producer, so the producer remains unknown; the maintained field name `drawCallback` is justified by the TH10 draw-chain execution context, not by adjacent transfer. Committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` independently has separate option update/render callback slots and is used only as later family corroboration. | Attested TH10 body/registration/callback-manager/frame analysis; full target `.text` `+0x94` displacement scan; committed clean TH08 source only after TH10-local role recovery |
| TOOLCHAIN-017 | observed | Maintained `PlayerDrawCallback` compiles with pinned VC7.1 SP1 build 6030 under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same profile plus `/GL`; the changed Player layout also leaves `ReplayManager.cpp` compiling under both profiles. The normal `/Gy` `PlayerDrawCallback` COMDAT is exactly `0x1A0` = 416 bytes, numerically equal to the target 409-byte body plus 7-byte registered adapter code-byte total. This is only a source-shape signal: the target chunks are non-contiguous, and a relocation-aware 409-byte body diagnostic is `mismatch` at 69/317 comparable bytes with `acceptance_authority=none`. The normal object preserves the same state gate, four-option `+0x94` loop, draw constants, auxiliary gate order, and floor-driven rectangle structure; target differences include the private ECX-to-EAX adapter/body split and register-bound draw callees. `/GL` remains a viable optimizer-context hypothesis but provides no standalone exactness credit. | `.analysis/gpt-web/20260914-player-postupdate/`; pinned compiler; normal COFF section/symbol/relocation table; target-bound diagnostic comparator |
| PLAYER-008 | observed / unknown-adapter-origin | The first Player callback lane registered during initialization resolves to a seven-byte CC-delimited entry at `0x00426500-0x00426506` and a separate 3,089-byte body at `0x00425730-0x00426340`. The entry receives Player in ECX, pushes that pointer, calls the body, and returns its result. The body independently reads exactly one stack Player pointer after its prologue and ends in `ret 4`; its bespoke Player power/state/option/effect/timer/shot logic establishes `authored_game` origin even though the source-written-versus-compiler/LTCG origin of the seven-byte adapter and the physical source ownership relation between the two chunks remain unknown. Five unreachable alignment sleds inside the body total exactly 29 bytes, explaining Ghidra's 3,060 reachable bytes versus the 3,089-byte physical extent. A separate three-byte alignment region and five-entry jump table follow the body before CC padding. | Direct TH10 callback registration and raw entry/tail disassembly; attested function/xref/decompile/callee analysis; full raw alignment-gap inventory |
| PLAYER-009 | observed / corroborated | The central Player update body is a five-state machine whose compiler jump table selects numeric states 0-4 while the physical case-block order is `0, 1, 4, 2, 3`; the state-4 block can transition and fall through into state 2. Target-local behavior includes respawn/history handling, power-triggered `RebuildPlayerOptions`, the connected movement/options owner, 32 repeated 0x6C effect rows at Player `+0x350C`, 0x14 timer-shaped records at `+0x460/+0x474/+0x488/+0x430C`, six derived Float3 rows at `+0x4324`, and the final connected input/shot updates. Maintained partial views expose only these observed offsets and operations; original type/member names remain unknown. Committed TH08 Player source was consulted only after this TH10 structure was recovered and independently corroborates that VC7 Player state-machine lexical block order and timer/source shape can be target-visible; no TH08 address, layout, state value, or exactness fact is transferred. | TH10 target state table, raw body instructions, attested helper decompilation, direct constants; committed clean TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` used only as later source-shape corroboration |
| TOOLCHAIN-018 | observed | Maintained `PlayerUpdateCallback` and the expanded partial Player layout compile with pinned VC7.1 SP1 build 6030 under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same profile plus `/GL`; `ReplayManager.cpp` also remains compilable under both profiles. The final normal `/Gy` `PlayerUpdateCallback` COMDAT is `0xAF0` = 2,800 bytes and naturally emits a five-entry relocated switch table with the target-observed lexical block ordering. The target has a separate seven-byte registered adapter, a 3,089-byte stack-bound body, and its jump table outside that body. A caller-window 3,089-byte relocation-aware diagnostic is still `mismatch` at 191/2,693 comparable bytes with 99 reported object relocations and `acceptance_authority=none`; the comparator's requested 3,089-byte window is not the true object-owned extent. Fixed normal is therefore not the physical owner. Successful `/GL` compilation preserves an optimizer/LTCG hypothesis without codegen or ownership credit. | `.analysis/gpt-web/20260914-player-update/`; pinned compiler; normal COFF section/symbol/relocation tables; target-bound diagnostic comparator |
| PLAYER-010 | observed / unknown-optimizer-ABI | `0x004250B0-0x004256E6` is the 1,591-byte central Player movement/options body. Its sole TH10 caller at `0x00425A8C` first loads EDI with the Player owner; the callee then uses live-in EDI for Player storage and returns zero with plain RET. Ghidra's provisional fastcall parameter is therefore not adopted. The physical body contains one retained unreachable three-byte LEA sled at `0x0042558D-0x0042558F`, explaining 1,588 reachable bytes. After the body are a one-byte NOP, two separate 32-byte/eight-entry switch tables at `0x004256E8-0x00425707` and `0x00425708-0x00425727`, then CC padding through `0x0042572F`. Both tables map direction values 1-8 onto target physical case-block order `4, 3, 1, 2, 5, 7, 6, 8`. The body is `authored_game`; original source declaration/TU and compiler/LTCG private-ABI/table ownership remain unknown. | TH10 attested function/decompile/disassembly/xrefs; direct raw caller/tail/table bytes |
| PLAYER-011 | observed / corroborated | The movement owner establishes Player `+0x3D4/+0x3D8` as mode-0/mode-1 axis speeds and `+0x3DC/+0x3E0` as the corresponding diagonal speeds, not independent X/Y speed fields. Input bits select directions 0-8; fixed-point deltas at `+0x3F0/+0x3F4` are speed multiplied by target timer scale, fixed position `+0x3CC/+0x3D0` is clamped to `[-18400,18400] x [3200,43200]`, and float draw position `+0x3C0/+0x3C4` is fixed position times target `0.01f`. `+0x329C` is a managed-VM handle created only in nonzero option mode with target layer 9/script `0x160`/runtime flag, retired with delete state 1 in mode zero, and repositioned from the Player draw position. The four-option loop selects offset pair `+0x44` in mode zero or `+0x4C` in nonzero mode, forms target pair `+0x34`, invokes option `+0x90` with ECX=option base, interpolates pair `+0x3C` when transition field `+0x4308 >= 30`, clears reset flag `+0x8C` on snap, and updates primary/secondary VM ids `+0x68/+0x6C`. Committed TH08 source independently corroborates the direction/speed-switch source family only after these TH10 facts were recovered; no TH08 layout or state value is transferred. | TH10 raw/decompiled movement owner and managed-VM callees; target constants; committed clean TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` only as later source-shape corroboration |
| TOOLCHAIN-019 | observed | Maintained `PlayerUpdateMovementAndOptions` plus the refined Player partial layout compile with pinned VC7.1 SP1 build 6030 under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same profile plus `/GL`; `ReplayManager.cpp` also compiles under both profiles. The fixed-normal `/Gy` movement COMDAT is `0x604` = 1,540 bytes, with local table labels at `0x5C4/0x5E4`, i.e. 1,476 code bytes plus two 32-byte relocated tables. Its table relocations reproduce the TH10 direction mapping and physical lexical block order, but target ownership is a 1,591-byte EDI-bound body followed by a NOP and two 32-byte tables. A 1,591-byte target-bound diagnostic is `mismatch` at 101/1,415 comparable bytes with 44 object relocations and `acceptance_authority=none`. Fixed normal is therefore not exact physical ownership; successful `/GL` preserves an optimizer/LTCG hypothesis without exactness credit. | `.analysis/gpt-web/20260914-player-movement/`; pinned compiler; normal COFF symbols/sections/relocations; target-bound diagnostic comparator |
| PLAYER-012 | observed / unknown-optimizer-ABI | `0x004247F0-0x00424D8A` is the reviewed 1,435-byte Player initialization owner. Its sole target caller allocates `0x4478` bytes, runs the adjacent zeroing constructor, moves the returned Player to EBX, then calls this body; the initializer itself never establishes EBX and consistently uses it as the Player base. The body chooses `pl00.anm`/`pl01.anm`, acquires character/shot `.sht` data, registers the reviewed update/draw callbacks in the two callback lists with priorities `0x10/0x16`, initializes the embedded draw VM, fixed/float starting positions, four mode speeds, 33-entry replay history, three timer-shaped records, three extent vectors and six derived vectors, then calls `RebuildPlayerOptions`. The only unreachable bytes inside the physical extent are the seven-byte `lea esp,[esp]` sled at `0x00424939-0x0042493F`; `0x00424D8B-0x00424D8F` is CC padding before independent `0x00424D90`. The body is `authored_game`; original source identifier/declaration/TU and the optimizer-owned EBX ABI remain unknown. | Attested TH10 function/caller/decompile/disassembly/callee evidence; direct raw gap/tail/data inspection |
| PLAYER-013 | observed | TH10-local allocation/reset evidence closes two important Player layout extents. The Player factory requests exactly `0x4478` bytes and `0x004246C0` clears exactly `0x111E` dwords from that object before publishing it as the global Player, establishing `sizeof(Player)==0x4478` for the maintained i386 layout. Separately, the initializer passes Player `+0x14` as the embedded draw-VM base and its reset owner `0x00401DE0` clears exactly `0xEB` dwords while preserving selected VM fields, establishing `0x3AC` bytes of embedded VM storage through Player `+0x3BF`; draw position begins immediately at `+0x3C0`. Callback-node handles at Player `+0x08/+0x0C` point to independently allocated `0x24`-byte nodes whose callback slot is `+0x08` and owner slot is `+0x20`; the paired Player teardown owner later removes those same nodes. | TH10 factory/constructor/VM-reset raw disassembly; initializer callback registration; teardown decompilation |
| TOOLCHAIN-020 | observed | Maintained `PlayerInitialize` and the closed `0x4478` Player partial layout compile with pinned VC7.1 SP1 build 6030 under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same profile plus `/GL`; the shared-header `ReplayManager.cpp` also compiles under both profiles. The true normal `/Gy` `PlayerInitialize` COMDAT is `0x474` = 1,140 bytes versus the reviewed 1,435-byte target EBX-bound body. A caller-window 1,435-byte relocation-aware diagnostic is `mismatch` at 133/1,251 comparable bytes with 46 object relocations and `acceptance_authority=none`; the comparator's requested window is not the true object-owned extent. Fixed normal is therefore not exact physical ownership. Successful `/GL` compilation preserves an optimizer/LTCG hypothesis without per-function ownership or exactness credit. | `.analysis/gpt-web/20260914-player-init/`; pinned compiler; normal COFF symbol/section/relocation tables; target-bound diagnostic comparator |
| PLAYER-014 | observed / unknown-optimizer-ABI | The reviewed Player lifecycle cohort is `0x004246C0-0x004247B3` (`Player::Player`, 244 bytes), `0x00424D90-0x00424EA0` (`PlayerResetRuntimeState`, 273), `0x00424ED0-0x00425014` (`Player::~Player`, 325), and `0x00425020-0x00425064` (`PlayerCreate`, 69). The constructor receives Player through live-in ESI from the factory, performs constructor-visible subobject initialization, clears exactly `0x111E` dwords = `0x4478` bytes, publishes `g_Player`, returns the same object and RETs. The reset owner receives Player through live-in ESI from all three observed callers, sets runtime state 1, resets three timer-shaped records, retires the mode VM id and tail-jumps into VM-group state maintenance. The destructor receives one stack Player pointer and RET 4 after MSVC SEH-framed callback unlink, resource/cache/option ownership cleanup and draw-VM owned-data release. The no-argument factory performs `new(0x4478)` -> constructor -> `PlayerInitialize`, and destroys/frees the object on initialization failure. All four bodies are bespoke Player lifecycle code and are `authored_game`; their natural maintained C++ declarations do not claim the target private optimizer ABIs or original TU. | Attested TH10 function/caller/callee/decompile/disassembly/xref evidence; direct raw caller/tail/SEH/lifetime corridors; pinned VC7 compiler feedback |
| PLAYER-015 | observed / denominator-expanded | TH10 constructor order exposes additional Player layout/source-shape facts. Player `+0x49C..+0x329B` is exactly 128 rows of `0x5C`; each row has a constructor-visible timer flag at row `+0x10`. The four `0x98` option rows have constructor flag `+0x80`. The constructor iterates 33 `0x6C` effect rows from `+0x350C`, clearing the nested timer flag at row `+0x54`; central update still processes only the first 32 rows, so the 33rd remains structurally present without invented gameplay semantics. Draw-VM relative `+0x358` (Player `+0x36C`) is an owned pointer freed by the Player destructor. Before the draw-VM parent constructor clears its 0x3AC bytes, nine neutral nested subobject flags at VM-relative `+0x6C/+0xB0/+0xFC/+0x128/+0x174/+0x1B0/+0x1FC/+0x228/+0x378` clear bit zero, matching normal C++ member-constructor ordering. Denominator review added Ghidra-missed bodies `0x004247C0-0x004247C6`, `0x004247D0-0x004247D9`, `0x004247E0-0x004247E6`, `0x00424EB0-0x00424EB2`, `0x00424EC0-0x00424EC2`, `0x00425070-0x00425088`, and `0x00425090-0x004250A1`. Only `0x00425070` is classified: it is the compiler-generated Player scalar deleting-destructor helper, by opcode-template identity with the reviewed Replay helper plus pinned VC7 synthesis. The other six remain `unknown/review`, including the three constructor-shaped bodies despite exact normal source-shape diagnostics. | Direct TH10 raw boundaries/layout/counts; target destructor lifetime evidence; pinned VC7 constructor/deleting-helper synthesis; no-origin policy for unreferenced retained bodies |
| TOOLCHAIN-021 | observed | Final maintained Player lifecycle/layout source compiles with pinned VC7.1 SP1 build 6030 under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same profile plus `/GL`; shared-header `ReplayManager.cpp` also compiles under both profiles. True normal `/Gy` text extents are 186 bytes for `Player::Player`, 274 for `PlayerResetRuntimeState`, 266 for `Player::~Player`, 30 for compiler-emitted `??_GPlayer@@QAEPAXI@Z`, and 72 for `PlayerCreate`, versus target 244/273/325/25/69. Current target-bound diagnostics are mismatches at 27/236 comparable bytes with 2 relocations for the constructor, 28/253 with 5 for reset, 17/213 with 28 for destructor, and 12/49 with 5 for factory; all have `acceptance_authority=none`. Separately, normal maintained constructors are byte-identical to the retained target bodies at `0x004247C0` (7/7), `0x004247D0` (10/10), and `0x004247E0` (7/7), with zero relocations, but source/physical compiler ownership is unresolved so they receive no canonical exactness credit. Fixed normal keeps the draw-VM constructor as a call while target optimizer context inlines it; successful `/GL` preserves the optimizer/LTCG hypothesis. | `.analysis/gpt-web/20260914-player-lifecycle/`; pinned compiler; normal COFF section/symbol tables; target-bound diagnostic comparator |
| PLAYER-016 | observed / corroborated | `0x00428280-0x004285EB` is the reviewed 876-byte Player shot-update owner mapped descriptively as `PlayerUpdateShots`. Its sole caller is the central Player update body, and its machine boundary is one stack Player pointer with `RET 4`; the only bytes inside the physical extent omitted from Ghidra's reachable-body count are the six-byte unreachable alignment region at `0x004282AA-0x004282AF`. The body iterates exactly 128 `0x5C` rows at Player `+0x49C`. Each row has the established `0x14` timer, a `0x2C` motion block at `+0x14`, state `+0x40`, primary/secondary managed-VM ids `+0x44/+0x48`, collision handoff fields `+0x50/+0x54`, and a descriptor pointer `+0x58`. The pointed `.sht` descriptor is `0x34` bytes; reviewed fields include signed schedule bytes `+0x00/+0x01`, signed damage short `+0x02`, spawn offsets `+0x04/+0x08`, hitbox extents `+0x0C/+0x10`, angle/speed `+0x14/+0x18`, signed source index `+0x1C`, type `+0x1D`, animation/sound shorts through `+0x22`, and callbacks at `+0x24/+0x28/+0x30`. The update callback at `+0x28` receives Player in ECX and the shot row in EDX; its return value is not consumed. Type-3 shots use source slots 1-4, retire their VMs when their source option disappears, and use the four occupancy ints at Player `+0x42F8` through natural `sourceIndex - 1` indexing; this produces the target address expression `+0x42F4 + sourceIndex*4` while preserving the independently proven 33-row effect constructor at `+0x350C`. Motion uses target polar helpers, managed-VM position is offset by `224.0f/16.0f`, ordinary shots are culled after timer frame 10, and the familiar timer-scale update closes each surviving row. Committed clean TH08 Player source was consulted only after these TH10-local facts were recovered and independently corroborates the broad fixed-slot/update-callback source family; its `0x484` shot layout, addresses and exactness are not transferred. | Attested TH10 function/caller/callee/decompile/disassembly/xrefs; direct spawn/collision owner context; target raw boundary and constants; committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` only for later source-family corroboration |
| PLAYER-017 | observed / denominator-expanded | Raw review immediately after `PlayerUpdateShots` found a complete Ghidra-missed body at `0x004285F0-0x00428626`: four `CC` bytes separate it from the shot owner, it performs floating-point min/max-style pair construction through register-bound pointers using target constant `0x00470B0C`, ends in plain RET, and is followed by nine `CC` bytes before independent `0x00428630`. Native Ghidra reports no containing function and no xrefs to `0x004285F0`. It is therefore added to the denominator as `unknown/review`; physical source owner, exact ABI, role and origin are not guessed. | Direct TH10 raw boundary/disassembly; attested Ghidra function/xrefs_to negative results |
| TOOLCHAIN-022 | observed | Maintained `PlayerUpdateShots` and the refined Player shot/descriptor layout compile with pinned VC7.1 SP1 build 6030 under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same profile plus `/GL`; the shared-header `ReplayManager.cpp` also compiled under both profiles during the packet. The final normal `/Gy` `PlayerUpdateShots` COMDAT is `0x2B3` = 691 bytes versus the 876-byte target owner. A caller-window 876-byte relocation-aware diagnostic is `mismatch` at 47/784 comparable bytes with 23 object relocations and `acceptance_authority=none`. The normal object calls ordinary maintained helper spellings while the target mixes private register helpers, inline primary-VM lookup and a secondary lookup call; fixed normal is therefore not exact physical ownership. Successful `/GL` compilation preserves the optimizer/LTCG hypothesis without ownership or exactness credit. Restoring the target-proven 33 `0x6C` effect rows after a temporary semantic split also restores the prior 186-byte normal Player constructor, while four shot-source occupancy ints naturally begin at `+0x42F8`. | `.analysis/gpt-web/20260914-player-shot-update/`; pinned compiler; normal COFF section/symbol/relocation tables; target-bound diagnostic comparator; direct target constructor loop |

| PLAYER-018 | observed / corroborated | `0x00428630-0x00428AC1` is the reviewed 1,170-byte Player damage/collision owner mapped descriptively as `Player::CalculateDamageToTarget`. Its sole direct caller at `0x0040E13C` pushes the global Player pointer, target position and target size, clears ECX, calls this body, and subtracts EAX from a hostile health-like field. The body ends in `RET 12`. Target-private optimization rotates the four logical member-function values: the optional output pointer is live in ECX, while Player, position and size are the three stack values; maintained source keeps the natural Player-member spelling and does not claim that private ABI as the original source convention. The function processes all 128 `0x5C` shot rows, uses half-extents for AABB collision, invokes descriptor `+0x30` as an ECX/EDX Player/shot callback with target position on the stack, marks hit state/VM transitions, accumulates descriptor `+0x02` as signed damage, rate-limits type-3 damage on four-frame timer boundaries, and spawns a collision/effect row for type 2. It then consumes the first 32 `0x6C` Player rows as circle or axis-aligned/rotated rectangular damage regions. TH10 spawn/update/collision paths jointly establish row radius/radius delta `+0x00/+0x04`, angle/angle delta `+0x08/+0x0C`, extents `+0x10/+0x14`, Float3 position `+0x18`, Float3 velocity `+0x24`, timer at `+0x44`, damage/hit accumulator/hit cap/collision interval `+0x58/+0x5C/+0x60/+0x64`, and active/mode flags `+0x68`. The 33rd constructor-visible row remains structurally present but is not assigned these first-32 runtime semantics. Committed clean TH08 `Player::CalcDamageToEnemy` was consulted only after the TH10 layout/control flow was recovered and corroborates the broad source family; no TH08 address, extent, layout or exactness fact is transferred. | Attested TH10 function/caller/callee/decompile/disassembly/xrefs; direct raw caller and target constants; TH10 row-spawn helper; committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` only for later naming/source-family corroboration |
| PLAYER-019 | observed / denominator-expanded | Physical review after the damage owner establishes `0x00428AC2-0x00428ACF` as 14 bytes of `CC`, followed by a separate `0x00428AD0-0x00428B04` 53-byte `RET 4` body, 11 bytes of `CC`, a separate `0x00428B10-0x00428C19` 266-byte plain-RET body, and 6 bytes of `CC` before the existing `0x00428C20` candidate. Ghidra reports no containing function and no xrefs for either raw entry. Their Player-shot-compatible offsets make them relevant denominator candidates, but indirect callback/source ownership, exact logical ABI, authored-versus-retained origin and exactness are not inferred; both remain `unknown/review`. | Direct verified TH10 PE corridor extraction and i386 disassembly; attested negative Ghidra function/xrefs_to results |
| TOOLCHAIN-023 | observed | Maintained `Player::CalculateDamageToTarget`, the corrected Player collision/effect-row layout, and the shot collision-callback type compile with pinned VC7.1 SP1 build 6030 under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same profile plus `/GL`; shared-header `ReplayManager.cpp` also compiles under both profiles. The true normal function section for `?CalculateDamageToTarget@Player@@QAEHPBUPlayerFloat3@@0PAH@Z` is `0x46C` = 1,132 bytes versus the reviewed 1,170-byte target owner. A 1,170-byte target-bound comparator window is `mismatch` at 72/1,078 comparable bytes with 23 relocations and `acceptance_authority=none`. The target private ABI moves the optional output pointer into ECX and the Player owner onto the stack, while normal standalone C++ uses the ordinary member ABI; fixed normal therefore does not establish physical ownership despite the close size. Successful `/GL` compilation preserves the interprocedural/LTCG hypothesis, but no linked-image target-bound Oracle exists and exactness remains unknown. | `.analysis/gpt-web/20260914-player-shot-collision/`; pinned compiler; normal COFF symbol/section/relocation table; target-bound diagnostic comparator |
| ENEMY-001 | observed / corroborated | `0x0040DC80-0x0040E5EB` is the reviewed 2,412-byte hostile runtime update owner mapped descriptively as `EnemyRuntimeUpdate`. Both direct TH10 callers pass full enemy object `+0x103C`; the body itself loads exactly one stack pointer and every normal/early exit uses `RET 4`. The owner copies the prior 0x2C motion record, updates two position and four scalar interpolation records, composes three 0x2C motion records, applies optional movement bounds/offscreen retirement, switches animation state, runs ECL, calls the reviewed Player damage owner, applies Player/game-state damage scaling, subtracts life at runtime `+0x13C0`, resolves callbacks/death/score reward, checks Player collision, updates eight managed VM ids, selects a Player tracked-enemy pointer, applies four-frame damage flash state and advances timers. Player `+0x3504/+0x3508`, previously neutral scratch fields, are target-observed here as a full-enemy tracked pointer plus per-frame validity byte. Committed TH08 `EnemyManager::OnUpdate` was consulted only after the TH10 object/update structure was independently recovered and corroborates the broad enemy-update/damage source family; no TH08 address, layout, ABI or exactness fact is transferred. | Attested TH10 function/caller/callee/decompile/disassembly evidence; direct raw entry/tail/call sites; committed clean TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` only for later naming/source-family corroboration |
| ENEMY-002 | observed | TH10 allocation/constructor/teardown evidence closes the maintained hostile object extents independently of decompiler types. `EnemySpawn` requests exactly `0x2518` bytes. Constructor `0x0040D830` clears exactly `0x537` dwords = `0x14DC` bytes from full object `+0x103C` to the allocation end and stores the full-object back-pointer at runtime `+0x14D8` (full `+0x2514`). Within that tail, four consecutive 0x2C motion records occupy `+0x000/+0x02C/+0x058/+0x084`; two 2D collision-size pairs begin `+0x0B0/+0x0B8`; teardown proves ten managed VM ids at `+0x0C0` while the runtime update consumes the first eight; spawn parameters occupy eight dwords at `+0x0FC`; a 0x14 timer is at `+0x11C`; the exact 12-byte manager list node is `+0x130`; two 0x4C position interpolators begin `+0x13C`; four 0x3C scalar interpolators begin `+0x1D4`; reward/life are `+0x13BC/+0x13C0`; item-drop type/count storage begins `+0x13CC`; constructor writes `32.0f` at `+0x1400/+0x1404`; death sound/effect controls begin `+0x1408`; damage-flash frames are `+0x1414`; timers begin `+0x141C/+0x1430`; flags are `+0x1444`; manager slot is `+0x1450`; eight 0x10 callback-threshold records begin `+0x1458`; and owner is `+0x14D8`. Maintained VC7 assertions keep `sizeof(EnemyRuntimeView)==0x14DC` and `sizeof(EnemyFullObjectView)==0x2518` without claiming original type/member identifiers. | TH10 spawn allocation; constructor clear/nonzero writes; teardown ten-id scan; runtime raw offsets; pinned VC7 layout assertions |
| TOOLCHAIN-024 | observed | Maintained `src/Enemy.cpp/.hpp` and the refined Player tracked-enemy fields compile with pinned VC7.1 SP1 build 6030 under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same profile plus `/GL`; `Player.cpp` also compiles under both profiles after the shared declaration change. The final normal `/Gy` `EnemyRuntimeUpdate` section is `0x935` = 2,357 bytes versus the reviewed 2,412-byte target owner. A 2,412-byte relocation-aware target diagnostic is `mismatch` at 134/2,160 comparable bytes with 63 object relocations and `acceptance_authority=none`. The target uses multiple register-private motion/VM/ECL helpers and its production physical owner/profile remain unresolved; fixed normal is therefore non-exact. Successful `/GL` compilation preserves the target Rich-record LTCG/interprocedural hypothesis but provides no standalone codegen or exactness credit. | `.analysis/gpt-web/20260914-hostile-runtime/`; pinned compiler; normal COFF symbol/section/relocation table; target-bound diagnostic comparator |
| ENEMY-003 | observed / denominator-expanded | The reviewed Enemy lifecycle seam consists of `EnemySpawn` at `0x0040CFB0`, private-EDI `EnemyManagerUpdate` at `0x0040D750`, the directly registered update/draw callbacks at `0x0040D810/0x0040D820`, ESI-bound constructor `0x0040D830`, stack-bound teardown `0x0040DAE0`, and death finalizer `0x0040E5F0`. Spawn corrects the initial Ghidra boundary: executable code ends at `0x0040D1A4`, a three-byte alignment sled occupies `0x0040D1A5-0x0040D1A7`, its directly indexed five-entry jump table is `0x0040D1A8-0x0040D1BB`, and its 53-byte selector table is `0x0040D1BC-0x0040D1F0`; `0x0040D1F1-0x0040D1FF` is CC padding. Therefore the source-owned physical span is 577 bytes, not the imported 501-byte candidate. Manager initialization directly stores `0x0040D810` as update callback priority `0x12` and `0x0040D820` as draw callback priority `0x14`, so those Ghidra-missed 10-byte and 6-byte bodies are added to the denominator. The primary enemy vtable slot `+0x14` points to scalar-deleting wrapper `0x0040CC50`, which calls `EnemyTeardown` before optional deallocation; maintained source keeps wrapper/delete mechanics separate from the teardown body. | TH10 raw entry/tail/table bytes; direct table xrefs; manager callback registration; vtable contents and indirect deletion call; reviewed caller/callee seams |
| ENEMY-004 | observed | The lifecycle seam closes several cross-object references. `EnemyManagerView` is exactly `0x68` bytes; its timer begins `+0x40`, script database `+0x54`, list head/tail `+0x58/+0x5C`, active count `+0x60`, and spawn counter `+0x64`. The embedded enemy list node at full `+0x116C` (runtime `+0x130`) stores the full object, next and previous links. Teardown scans ten managed VM ids and marks resolved VM/child states for deletion, then clears Player `+0x3504/+0x3508` if it tracks this enemy and scans 128 Player shot rows at Player `+0x4E8` with stride `0x5C`; because `shots[]` begins at `+0x49C`, this proves each `PlayerShotRuntimeView +0x4C` is an `EnemyFullObjectView *trackedEnemy`, which teardown nulls before object destruction. Death finalization uses direct drop type at runtime `+0x13CC` plus eleven count slots at `+0x13D0`, position at runtime `+0x02C`, and target-observed sound/effect/item helpers. Raw aligned bodies at `0x0040D200`, `0x0040D730`, `0x0040D800`, and `0x0040DC70` remain inventory gaps with no modeled xref or absolute-pointer occurrence in the reviewed PE scan; they are not promoted to authored merely from body shape. Subsequent manager-lifecycle review independently promoted `0x0040D260` from repeated constructor semantics and classified `0x0040D710` as compiler-generated; see `ENEMY-005`/`ENEMY-006`. | TH10 teardown/death raw control flow; Player shot offset reconciliation; manager factory/list users; bounded PE absolute-pointer scan plus attested Ghidra xrefs |
| TOOLCHAIN-025 | observed / exact-one-unit | Final lifecycle source compiles with pinned VC7.1 SP1 build6030 as fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and with the same profile plus `/GL`; the refined shared Player header also compiles in both modes. True normal `/Gy` sections are 628/163/10/6/586/418/157 bytes for `EnemySpawn`, `EnemyManagerUpdate`, update callback, draw callback, `EnemyConstruct`, `EnemyTeardown`, and `EnemyFinalizeDeath`, versus target physical extents 577/166/10/6/673/386/162. Probe diagnostics are respectively mismatches at 76/536, 11/142, 1/6, structural-exact 6/6, 63/657 on a fresh clean-HEAD rebuild, 41/362, and 9/138 comparable bytes; probe mode has `acceptance_authority=none`. Only the six-byte draw callback has a canonical target-bound normal-COFF unit: `artifact_kind="coff"`, complete owned extent, zero relocations, and deletion/rebuild/recompare repeatedly returns `result="exact"` with 6/6 bytes. It is therefore the sole canonical exact promotion in this packet. The other bodies remain source-present/non-exact; successful `/GL` preserves an interprocedural/LTCG hypothesis without standalone exactness authority. | `.analysis/gpt-web/20260914-enemy-lifecycle/`; pinned VC7 compiler; normal COFF section/symbol tables; probe comparator; replayable `enemy-manager-draw-callback` canonical unit |
| ENEMY-005 | observed / denominator-expanded | The reviewed Enemy manager lifetime is a coherent source family around the exact `0x68` manager: `0x0040D260-0x0040D27C` is a Ghidra-missed 29-byte manager constructor body, `0x0040D280-0x0040D3CD` is the EBX-bound initializer, `0x0040D530-0x0040D65B` is the EAX-bound destructor body, and `0x0040D6B0-0x0040D70E` is the one-argument factory/rollback owner. The retained constructor clears timer flag bit zero at `+0x50`, zeroes all 26 dwords, sets manager flags bit 1, publishes `g_EnemyManager`, and returns the object; the same construction sequence appears inline inside the factory. Initialization installs the primary resource and registers 0x24-byte update/draw callback nodes at priorities `0x12/0x14`; each target node constructor clears only flags bit zero, stores `self` at `+0x14`, clears `+0x0C/+0x10/+0x18/+0x1C`, and the initializer later stores the manager owner at `+0x20`. Teardown clears live enemy state, removes both callback nodes under the callback critical section, releases ECL and animation resources, and clears the global owner. Pinned VC7.1 natural constructor/destructor/member source reproduces the same object family and also synthesizes the deleting-destructor helper class, but fixed normal target comparisons remain mismatches and `/GL` feasibility has no exactness authority. | TH10 raw constructor/factory duplicate sequence; callback allocation/registration/removal; reviewed callers and manager offsets; pinned VC7.1 normal `/Gy` plus `/GL` diagnostics in `.analysis/gpt-web/20260914-enemy-manager-lifecycle/` |
| ENEMY-006 | observed / denominator-expanded | The manager-owned ECL resource is exactly `0x1098` bytes and is naturally modeled as a polymorphic base/derived partial type. Target derived vtable `0x0046D0B4` contains inherited slot `0x00450220`, package loader `0x0040D400`, and file loader `0x0040CD20`; base vtable `0x0046D0F0` contains `0x00450220` and `0x0040C810`. `0x0040D3D0-0x0040D3F1` pre-clears neutral nested dwords at `+0x1090/+0x1094`, then clears the complete `0x1098` bytes and installs the derived vptr. Natural VC7.1 source with those two nested dword constructors and a containing full-object clear emits the same core ordering, differing by the conventional ECX-to-EDX receiver setup. `0x0040D400-0x0040D4EE` parses `ANIM`/`ECLI` package lists and uses a virtual file-load dispatch; `0x0040CD20-0x0040CD7E` loads one ECL file and passes the target file loader all three logical values (filename plus zero size-out/mode); `0x0040D680-0x0040D6A3` transitions to the base vptr and releases the owned lookup table at `+0x8C`. Teardown independently proves 32 owned script-data pointers at `+0x0C..+0x88`. The scalar deleting wrappers at `0x0040CC50`, `0x0040D660`, and `0x0040D710` are compiler-generated/excluded; VC7.1 automatically emits the corresponding helper family from maintained natural destructor/delete source. No body in this packet is exact under the fixed normal diagnostic. | TH10 vtable contents, raw constructor/file-loader/package/destructor bodies and manager teardown; existing target-local file-loader ABI evidence; pinned VC7.1 class/vtable/deleting-helper output and relocation-aware diagnostics |

| TOOLCHAIN-026 | observed | Final manager/resource source compiles with pinned VC7.1 SP1 build6030 under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same profile plus `/GL`. True normal `/Gy` sections versus reviewed target extents are: ECL `LoadFile` 97/95, manager constructor 31/29, manager initializer 314/334, ECL-resource constructor 36/34, package loader 218/239, manager destructor 298/300, ECL-resource destructor 40/36, and manager factory 97/95 bytes. Complete target diagnostics are all `mismatch` with `acceptance_authority=none`: 50/79, 1/25, 8/286, 0/30, 32/223, 12/184, 4/28, and 43/75 comparable bytes respectively. The compiler also emits 60-byte and 30-byte scalar deleting helpers corresponding by semantics to target `0x0040D660`/`0x0040D710`, but those target bodies remain non-exact. `/GL` success preserves an optimizer/LTCG hypothesis and grants no standalone exactness. | `.analysis/gpt-web/20260914-enemy-manager-lifecycle/Enemy.final.*`; pinned compiler; COFF sections/symbols; complete target-bound diagnostic JSON |
| ENEMY-007 | observed / denominator-expanded | TH10 establishes a six-slot derived Enemy ECL-evaluation vtable at `0x0046D0C0-0x0046D0D4` and a separate six-slot base-state vtable at `0x0046D0D8-0x0046D0EC`; they are not one twelve-slot table. `EnemyConstruct` writes the derived vptr at full-object `+0x00`, teardown restores that same derived vptr before the base destructor path, and the base constructor at `0x0040C710` writes `0x0046D0D8`. Derived slot zero is the reviewed 11-byte `0x0040E760-0x0040E76A` forwarding method: it adds the independently proven runtime-tail offset `0x103C` to ECX and tail-jumps to `0x0040E770`. The runtime target is an authored Enemy-specific ECL instruction dispatcher with machine boundary `ECX EnemyRuntimeView* / RET`; it reads the full owner from runtime `+0x14D8` and switches over opcodes `0x100-0x1B4`. Its source-owned physical span is `0x0040E770-0x00411FBF` = 14,416 bytes, not Ghidra's 13,759 reachable-code bytes: after the final RET at `0x00411D2E`, the owner includes one-byte alignment, a 108-entry jump table, a 181-byte selector table, three-byte alignment, and two five-entry nested jump tables before the next method at `0x00411FC0`. Derived slots one through four are separately reviewed authored operand interfaces at `0x00411FC0-0x004122F3` (`ReadIntOperand`, 820 bytes), `0x00412300-0x0041234F` (`ResolveIntOperand`, 80), `0x00412350-0x004126C3` (`ReadFloatOperand`, 884), and `0x004126D0-0x0041271F` (`ResolveFloatOperand`, 80); their maintained names are descriptive. At this checkpoint their source bodies remained unknown; `ENEMY-008` records the later source reconstruction. Committed TH08/TH095 ECL source was consulted only for broad execute/operand-family context after the TH10 vtable and switch structure were recovered; it does not supply TH10 names, addresses, layouts or exactness. | TH10 raw vtable bytes and constructor/destructor vptr writes; attested vtable/data xrefs and ECL-dispatch decompilation; direct switch-table references and target-owned table extents; committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` and committed TH095 HEAD `3c54464e6d567a37219b31c4ee8bc49e033b61c1` used only as later source-family hypotheses |
| TOOLCHAIN-027 | observed / exact-one-unit | A minimal natural VC7.1 source-shape probe and the maintained full `src/Enemy.cpp` both lower `EnemyFullObjectView::DispatchEclInstruction() { return runtime.DispatchEclInstruction(); }` to exactly `ADD ECX,0x103C` followed by a REL32 tail JMP, producing an 11-byte normal `/Gy` COMDAT with the sole relocation at offset `+7` against `?DispatchEclInstruction@EnemyRuntimeView@@QAEHXZ`. Fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same source plus `/GL` both compile. Probe comparison is structural-exact on all seven non-relocation bytes with `acceptance_authority=none`; the canonical `artifact_kind="coff"` unit `enemy-ecl-dispatch-forwarder` additionally replays that REL32 to reviewed target `0x0040E770` and, after deleting/rebuilding the object twice, returns repeatable zero-difference `result="exact"` across all 11 bytes. This exact promotion applies only to the forwarding method. At that checkpoint the 14,416-byte runtime dispatcher and four adjacent operand resolvers were still source-absent/non-exact; the operand-method status is subsequently superseded by `ENEMY-008`/`TOOLCHAIN-028`. The pre-existing six-byte `EnemyManagerDrawCallback` canonical unit also rebuilds exact from the same modified `Enemy.cpp`. | `.analysis/gpt-web/20260914-enemy-dispatcher/virtual-forward-probe.*`, `Enemy.normal.obj`, `Enemy.ltcg.obj`, `forwarder-probe-vs-target.json`, `forwarder-canonical*.json`, and `existing-draw-callback-regression.json`; pinned VC7.1 SP1 build6030; canonical relocation replay |
| ENEMY-008 | observed / corroborated | The four reviewed Enemy ECL operand methods now have maintained source: integer reader `0x00411FC0-0x004122F3`, integer lvalue resolver `0x00412300-0x0041234F`, float reader `0x00412350-0x004126C3`, and float lvalue resolver `0x004126D0-0x0041271F`. The two 51-entry readers normalize negative selectors by adding decimal 10000 (`0x2710`) and preserve target-specific type-dependent behavior. Direct jump-table entries prove integer selectors `-9996` and `-9976` both route directly to target `0x0041219A` while `ECX` still names this Enemy, so they read this Enemy world Y before float-to-int conversion; only selector `-9962` enters through `0x00412192`, replaces `ECX` with the manager primary special-enemy pointer, and then falls through to the same Y load, while `-9963` exposes the corresponding special-enemy X directly. The last four selectors are direct difficulty equality tests: the integer reader compares against `0/1/2/3`, and the float reader compares the same selector sequence against `0.0/1.0/2.0/3.0`. The two lvalue methods independently prove the full-object `+0x1138..+0x1157` / runtime `+0x0FC..+0x11B` block is four writable integer values followed by four writable float values, refining the earlier eight-dword spawn-parameter observation. Target spawn copies exactly eight dwords from request `+0x20` into this block; maintained source uses the same 0x20-byte view on both sides, while the original request-side C++ declaration remains unknown. Reader cases are grounded in TH10-local fields/helpers: RNG state, Player position and angle-to-point helper, current/special Enemy motion, update timer, runtime flag bit `0x10000`, managed-VM id lookup and VM word `+0x38A`, life, rank-like global at `0x00474C98`, and difficulty at `0x00474C74`. TH10 initialization uses `0x00474C74` as a `0..4` difficulty index and the operand protocol exposes direct value plus equality-to-0/1/2/3 tests; `0x00474C98` is target-observed as the bounded `[-0x400,+0x400]` rank scalar: gameplay paths increment/decrement and clamp it, while the ECL operand readers expose its integer/float value. Committed TH08 and TH095 operand sources were consulted only after these TH10 tables/offsets were decoded and corroborate the broad split int/float/lvalue operand family; no adjacent selector id, address, layout or exactness fact is transferred. | Direct TH10 raw switch tables and case bodies; attested helper/global xrefs and decompilation; spawn-copy offsets plus exact lvalue extents; committed TH08 HEAD `a45e99fb1942714e6edded20847e32a654d56f97` and committed TH095 HEAD `f18d7c5e25087d8134146220917e2e33bfb2ab28` used only for later source-family corroboration |
| TOOLCHAIN-028 | observed / exact-two-units | Final operand source compiles with pinned VC7.1 SP1 build6030 under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same profile plus `/GL`. True normal `/Gy` sections are 800/80/800/80 bytes for `ReadIntOperand`, `ResolveIntOperand`, `ReadFloatOperand`, and `ResolveFloatOperand`, versus reviewed target extents 820/80/884/80. Target-window diagnostics for the two readers remain non-exact with `acceptance_authority=none`: integer 115/452 comparable bytes with 92 object relocations and float 95/540 with 86 object relocations. The two lvalue resolvers each have an explicit `artifact_kind="coff"` canonical unit covering the complete 80-byte owned extent and all five DIR32 jump-table relocations; after deleting/rebuilding the objects twice, both return repeatable `result="exact"` at 80/80 bytes. `/GL` success for the whole Enemy source preserves the production optimizer/LTCG hypothesis but grants no exactness to the two readers. | `.analysis/gpt-web/20260914-enemy-operands/Enemy.final.*`, reader diagnostic JSON, and two-pass canonical lvalue replay JSON; pinned VC7.1 SP1 build6030; complete target-bound relocation replay |
| ENEMY-009 | observed / origin-unknown-helper-seam | Hard-frontier review of `EnemyRuntimeView::DispatchEclInstruction` keeps the complete physical owner `0x0040E770-0x00411FBF` source-absent/non-exact but resolves a central ECL execution-context seam. Full Enemy `+0x008..+0x101F` is a `0x1018`-byte execution context; full `+0x004` points at the active context. Context `+0x004` is the current variable-length ECL instruction pointer, whose target-observed header has opcode at `+0x004`, operand-variable mask at `+0x008`, and dword operands from `+0x010`. Generic operand helpers prove context `+0x1008` is operand-stack state, `+0x100C` participates in local-variable address formation, and context `+0x1014` is the operand-resolver owner. Constructor target code stores the full Enemy itself at full `+0x101C` = context `+0x1014`, binding that resolver to the Enemy vtable typed operand methods. Adjacent machine wrappers `0x00412A00-0x00412A0F` and `0x00412A10-0x00412A5A` use private EAX=`EnemyRuntimeView*`, ECX=argument-index ABIs for integer argument read/lvalue resolution; all 30 and 9 direct calls respectively are inside the reviewed dispatcher, but source-written versus compiler/LTCG-outlined origin remains unknown. The top-level selector maps opcodes `0x15D..0x164` to `0x004118BB,0x004118E2,0x0041198B,0x004119FC,0x00411A3E,0x00411B23,0x0041009B,0x004100EA`; this family performs rank/difficulty-selected typed writes and interpolation, including float rank interpolation using target constants `1024.0f` and `1/2048.0f` and integer interpolation at `0x00411B23` with signed truncation-toward-zero correction. The two five-entry nested tables at `0x00411F98` and `0x00411FAC` are reached by the difficulty cases. Three additional CC-delimited bodies `0x00412A60-0x00412A6D`, `0x00412A70-0x00412A7D`, and `0x00412A80-0x00412A90` were missed by the initial inventory; complete target direct-REL32 and absolute-pointer scans find no references, so they are added only as origin-unknown review candidates, not authored/source-present functions. Committed adjacent-game source was consulted only as source-family hypothesis material after TH10-local recovery: clean TH08 `a45e99fb1942714e6edded20847e32a654d56f97`, clean TH09 `f1d4c1e332828a0cb8b9308b61b79ce77f86e4d0`, and only committed HEAD content from dirty TH095 `dee282c33dbb75ca0545d3115cd421a6e4f6a7bd`; no adjacent address, opcode, layout, owner or exactness fact is transferred. | Direct TH10 raw dispatcher/table/helper bytes and complete target reference scans; attested `th10-ghidra` helper/decompile/xref evidence with passed `factory-native-command` target attestation; target constructor initialization; committed adjacent repositories only after TH10-local recovery |
| TOOLCHAIN-029 | observed / structural-no-regression | The maintained Enemy prefix is refactored into explicit `EnemyEclInstructionView` and exact-size `EnemyEclContextView` without changing any proved full-object offsets: `sizeof(EnemyEclContextView)==0x1018`, active context at full `+0x004`, embedded context at `+0x008`, mirror pointer at `+0x1030`, flags at `+0x1028`, owned-allocation pointer at `+0x1034`, and runtime tail at `+0x103C`. Pinned VC7.1 SP1 build6030 compiles the revised `src/Enemy.cpp` under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same profile plus `/GL`. A separately rebuilt clean-HEAD baseline and the revised normal object both emit the same 586-byte `EnemyConstruct` COMDAT with byte-identical section contents and four relocation sites; only the unresolved `EnemyLookupEclSubroutine` symbol's declared return type changes from integer to the target-observed instruction pointer type. Both fresh target diagnostics are the same non-exact `63/657` comparable bytes with `acceptance_authority=none`. The added header declarations shift VC7.1 compiler-internal resolver labels by exactly +37 while preserving each switch table/case label section offset: integer `$L3750/$L3129..$L3132` -> `$L3787/$L3166..$L3169`, and float `$L3792/$L3207..$L3210` -> `$L3829/$L3244..$L3247`. The canonical manifests refresh only those generated object-symbol bindings; their relocation offsets/types and target VAs remain unchanged, and exactness is retained only after a fresh zero-difference target replay. This structural refinement gives no dispatcher or constructor exactness credit and does not establish the helpers' source/compiler origin. | `.analysis/gpt-web/20260914-enemy-dispatcher-hard/`; pinned VC7.1 SP1 compiler; clean-HEAD/revised COFF section-byte comparison; target-bound constructor diagnostics |
| TOOLCHAIN-030 | observed / exact-six-units | The earlier PbgFile diagnostic in `TOOLCHAIN-007` identified six structural-exact normal-COFF functions but stopped before canonical relocation replay. Current `src/PbgFile.cpp`, compiled from absent output paths with pinned VC7.1 SP1 build6030 under `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc`, now has six explicit target-bound units: destructor 46 bytes with three DIR32 relocations, `Close` 34/one, `Read` 57/one, `Tell` 25/one, `GetSize` 21/one, and `Seek` 37/one. Two independent cold-output builds replay all 220 bytes and all nine relocations with zero differences. This supersedes only `TOOLCHAIN-007`'s old “no canonical exact row” disposition: the bounded claims prove exact reproduction through the declared normal-COFF profile, while original production TU partition, normal-COFF versus LTCG physical ownership, and whole-product closure remain unknown. Probe mode in `compare-coff-function.py` now reports the object addend, target-encoded value, and target-derived relocation candidate for review; its result still has `acceptance_authority=none` until a declared canonical unit passes. | Fresh target bytes and import/vtable destinations; passed `th10-ghidra` attestation and reviewed PbgFile boundaries/vtable ownership; `config/match-units.toml`; two cold canonical replay passes; updated probe diagnostics |
| TOOLCHAIN-031 | observed / exact-six-units | The six structural-exact ResFile probes from `TOOLCHAIN-008` now have canonical target-bound units. Current `src/ResFile.cpp`, compiled twice from absent shared output with pinned VC7.1 SP1 build6030 under `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc`, replays `CMemoryPbgFile::Write` 5/5 bytes, destructor 49/49 with two DIR32 vtable relocations and one REL32 `free` relocation, `Close` 37/37 with one REL32 `free` relocation, `Read` 108/108, `Tell` 7/7, and `GetSize` 4/4. Both cold passes reproduce all 210 bytes and four relocations exactly. Canonical units now share one object path per source/profile, and `replay-exact-units.py` builds that object once before strictly checking all selected functions. Manifest and tracking validation reject invalid extents/relocations, inconsistent shared outputs, source/name/address/size/origin drift, and any difference between the canonical-unit and exact-ledger sets. These bounded claims do not identify the original production TU or its normal-COFF versus LTCG physical owner. | Fresh target bytes; passed Ghidra target binding and reviewed ResFile vtable/boundaries; two cold canonical replay passes; strengthened match graph and ledger validation |
| TOOLCHAIN-032 | observed / exact-three-units | The three structural-exact archive lifecycle probes from `TOOLCHAIN-006` now have canonical target-bound units. Current `src/PbgArchive.cpp`, compiled twice from an absent shared output with pinned VC7.1 SP1 build6030 under `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc`, replays `PbgArchive::PbgArchive` across its complete relocation-free 16 bytes, `PbgArchiveEntry::~PbgArchiveEntry` across 26 bytes with its sole REL32 `free` relocation, and `PbgArchiveEntry::PbgArchiveEntry` across its complete relocation-free nine bytes. Both cold passes reproduce all 51 bytes and the relocation exactly. Fresh hash-attested Ghidra review still models only the entry destructor; raw target boundaries plus vector constructor/destructor references establish the two inventory-missed constructors. These bounded exact claims do not identify the original production TU or its normal-COFF versus LTCG physical owner. | `.analysis/gpt-5.6-sol/20260914-pbgarchive-exact/`; fresh target bytes and Ghidra query; two cold source-object canonical replay passes |
| TOOLCHAIN-033 | observed / exact-one-unit | Current `src/Lzss.cpp` under pinned VC7.1 SP1 build6030 fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` emits a 42-byte `Lzss::InitEncoderState` COMDAT whose 30 ordinary bytes match the complete target body. Its three DIR32 relocations resolve exactly: dictionary base `0x0048F868` at offset `+9`, and `g_LzssTree` base `0x00477858` at offsets `+16` and `+35` with object addends `+4` and `+0x18010`. Target encodings `0x0047785C` and `0x0048F868` therefore cover the first node field and one-past-tree loop bound without inventing separate globals. Two cold source-object passes reproduce all 42 bytes and three relocations exactly. Ghidra still misses this CC-delimited retained helper; raw target code and the neighboring encoder's inline copy establish its boundary and behavior. The exact claim does not identify the original production TU or its normal-COFF versus LTCG physical owner. | `.analysis/gpt-5.6-sol/20260914-lzss-init-exact/`; direct target disassembly/data bounds; two cold canonical replay passes |
| TOOLCHAIN-034 | observed / workflow | `report-exact-backlog.py` joins the function, origin, source-mapping and exact ledgers into an address-sorted triage queue, validates the control plane before reporting, and supports exact source/module/state filters plus structured JSON. At the 20-unit checkpoint, the 90 source mappings split into 20 canonical exact, 66 authored non-exact functions / 26,540 bytes, and four origin-review Player callbacks / 38 bytes. Progress and live status now show the authored-source and origin-pending split instead of labeling all source mappings authored. Queue membership grants no exactness or normal-COFF ownership credit. | Repository ledgers; target-independent report execution and public CI |
| TOOLCHAIN-035 | observed / workflow-corrected | Normal-COFF symbol enumeration now reports exact decorated names, COFF-derived extents, extent provenance, sections and relocation counts in text or JSON. This exposed and corrected an unsafe probe fallback: when a function symbol lacked an auxiliary size, the old comparator used the requested target length and could read through the end of the function's COMDAT into later object-file contents. The comparator now accepts an extent only from a nonzero function-definition auxiliary record or a single-function offset-zero COMDAT code section, rejects cross-section reads, and records every absent candidate byte as a diagnostic difference. A fresh PbgArchive probe now reports the true 25-byte `GetEntryDecompressedSize` object against its 51-byte target instead of claiming a 51-byte object window. All 20 canonical units / 700 bytes cold-replay exact under the corrected extent rule, so no accepted exact claim depended on the unsafe fallback. | Current VC7.1 `PbgArchive.obj` symbol/section tables and corrected diagnostic; full canonical cold replay |
| TOOLCHAIN-036 | observed / workflow | `probe-exact-backlog.py` compiles each selected authored source once under the fixed normal-COFF profile, joins conservative source-name hints to uniquely matching external MSVC symbols, derives every extent from strict COFF metadata, and runs target-bound probe comparison. It reports unresolved mappings, size mismatch, byte/relocation diagnostics, and structural-exact results separately, always with `acceptance_authority=none`. A cold scan of the complete current authored source backlog resolved all 66 functions across nine sources and classified all 66 as real mismatches; there is no further unregistered structural-exact function in that fixed-profile queue. The scan corrects historical window-derived size observations but does not disprove an alternate normal profile or LTCG ownership. | Reproducible `python3 scripts/probe-exact-backlog.py --json`; current ledgers/source; pinned VC7.1 compiler and hash-attested target |
| TOOLCHAIN-037 | observed / workflow | The linked LTCG diagnostic now obtains complete candidate function extents from VC7.1 linker PDB DBI section contributions rather than a requested target window or next-public guess. The reader binds PDB to PE through the RSDS GUID/age and binds the map through PE timestamp, image base, public start, and section layout. `probe-ltcg-backlog.py` cold-compiles each source with `/GL`, iteratively resolves missing link symbols to unique non-runnable data anchors, enumerates PE HIGHLOW and decoded external control-flow fields, and compares every full PDB extent to the hash-attested target with `acceptance_authority=none`. The complete 66-function authored backlog scan across nine sources produced 13 structural-exact diagnostics / 715 bytes and 53 mismatches. The candidates include three Enemy lifecycle bodies, four Lzss tree helpers, five PbgArchive helpers, and `CPbgFile::GetFullFilePath`; only relocation-free `PbgArchive::SeekPastInt` and `SeekPastString` have raw complete equality (63 bytes total). These results establish a useful linked-codegen frontier but not production link context, physical ownership, or canonical LTCG exactness. | Cold per-source linked probes; PE/map/PDB identity checks; two repeatable PbgArchive diagnostic passes; pinned VC7.1 build-6030 compiler/linker and hash-attested target |
| TOOLCHAIN-038 | observed / exact-two-units | Canonical exact replay now supports declared `artifact_kind="linked-pe"` units without treating the incomplete linked harness as a product. `build-match-unit.py` cold-compiles `/GL` and links with the fixed `/OPT:NOREF` context and unique non-runnable data anchors. `compare-linked-function.py` rebinds PE/map/PDB identity, requires one complete PDB contribution owned by `source.ltcg.obj`, verifies the hash-pinned Capstone 5.0.6 decoder consumes the full extent, rejects undeclared PE HIGHLOW or external control-flow fields, validates declared semantic symbols through map publics/anchor aliases, replays their target addresses, and compares every byte. Two independent cold builds reproduce `PbgArchive::SeekPastInt` at `0x00435270` across 12/12 bytes and `PbgArchive::SeekPastString` at `0x00435280` across 51/51 bytes. Both complete extents contain zero linked fields and are raw-equal, for two newly accepted functions / 63 bytes. A focused `/OPT:REF` link changed `SeekPastInt` to the ordinary 16-byte stack ABI, while the declared `/OPT:NOREF` source context preserves the target's 12-byte private-register form; this directly records the LTCG context dependency. The bounded exact claims do not establish production translation-unit partition, physical ownership, link order, runnable product, or whole-build closure. | Two cold canonical linked-PE builds; PE/map/PDB identity and PDB contribution extents; pinned Capstone component hashes; full 22-unit mixed-artifact replay |
| TOOLCHAIN-039 | observed / exact-three-units | TH10-local review closes the sole linked field in each remaining structural-exact PbgArchive helper. `PbgArchive::GetEntryDecompressedSize` has a complete 51-byte PDB contribution and one REL32 at `+0x15`; `PbgArchive::FindEntry` has a complete 53-byte contribution and one REL32 at `+0x18`; both semantic symbols are `__stricmp` and replay to the reviewed target CRT `_stricmp` entry `0x0046054E`. `PbgArchive::CopyFileName` has a complete 47-byte contribution and one REL32 at `+0x11` for `_malloc`, replayed to the reviewed target CRT wrapper `0x00452706`. Two independent cold linked-image builds decode the entire 151-byte combined extent, find exactly those three declared fields, and compare every replayed byte with zero differences. Together with the two zero-field helpers, the shared PbgArchive `/GL` context now provides five bounded linked-PE exact units / 214 bytes. The anchor image remains non-runnable and production ownership/link closure remain unknown. | Hash-attested target CRT boundaries/behavior/direct-call scans; two cold canonical linked-PE replays; PE/map/PDB binding; complete field manifests |
| TOOLCHAIN-040 | observed / exact-four-units | The four structural-exact Lzss candidates now have complete canonical linked-field manifests. Every one of their 24 DIR32 fields resolves through the single linked public `?g_LzssTree@@3PAULzssTreeNode@@A` and replays to the TH10-local tree base `0x00477858` with only layout-proved addends: `+0` for `parent`, `+4` for `left`, `+8` for `right`, and `+0x18008` for `tree[0x2000].right`. The latter is inside the independently bounded `0x2001 * 0x0C = 0x1800C` tree owner, not a separate global. Two cold `/GL` links reproduce complete PDB contributions and zero differences for `Lzss::InitTree` 36 bytes/four fields, `ContractNode` 79/eight, `ReplaceNode` 117/nine, and `FindNextNode` 49/three: four newly accepted functions / 281 bytes. Together with the normal-COFF exact `InitEncoderState`, the Lzss seam now has five bounded exact helpers / 323 bytes across two declared artifact contexts. Production TU partition, data TU ownership, physical compiler owner, runnable product, and whole-build closure remain unknown. | Target-proven tree layout/bounds in `COMPRESSION-005`; two cold canonical linked-PE replays; exhaustive PE HIGHLOW manifests; PE/map/PDB identity |
| TOOLCHAIN-041 | observed / exact-four-units | The last four structural-exact LTCG diagnostics now have canonical field manifests. A shared Enemy `/GL` link reproduces `EnemyManagerView::EnemyManagerView` at `0x0040D260` across 29 bytes/one DIR32 to `g_EnemyManager` `0x00477704`, `EnemyEclResourceView::EnemyEclResourceView` at `0x0040D3D0` across 34 bytes/one DIR32 to derived vtable `0x0046D0B4`, and its destructor at `0x0040D680` across 36 bytes with a DIR32 to base vtable `0x0046D0F0` plus REL32 to target `free` `0x00452422`. A separate PbgFile `/GL` link reproduces `CPbgFile::GetFullFilePath` at `0x00435610` across 121 bytes with REL32 fields to `strchr` `0x00452730` and `strrchr` `0x00452930`, plus a DIR32 field to the `GetModuleFileNameA` IAT slot `0x004660DC`. Two independent cold links reproduce all four complete PDB contributions and all seven fields with zero differences across 220 bytes; negative tests prove either an omitted Enemy DIR32 or PbgFile REL32 is rejected. A fresh post-promotion Enemy/PbgFile backlog scan reports 18 real mismatches and zero structural-exact candidates. Thus all 13 candidates / 715 bytes from the original linked diagnostic have complete canonical manifests and exact replay; none is left awaiting ledger-only promotion. Production link context and physical ownership remain unknown. | TH10-local manager global/vtables/CRT entries/import slot; two cold canonical linked-PE replays; exhaustive linked-field negative tests; fresh post-promotion backlog scan |
| TOOLCHAIN-042 | observed / workflow | `rank-exact-backlog.py` obtains one current authored backlog, cold-runs both the strict normal-COFF and PDB-owned LTCG diagnostics, requires each lane to contain exactly the same address/name/source/target-size keys, and rejects input changes during the run through a before/after SHA-256 over the target, tracking ledgers, tool locks, participating scripts, and complete maintained `src/` tree. It ranks diagnostics by result class, then the explicit distance `abs(candidate_size-target_size)/target_size + mismatched_comparable_bytes/comparable_bytes`, followed by linked-field count, target size, and address. The report keeps each lane's size, byte, field, and result metrics plus signed `ltcg_gain`; it explicitly has `acceptance_authority=none`. A fresh full run covers all 53 current authored backlog functions with 53 normal mismatches and 53 LTCG mismatches, no unresolved keys. Its leading experiments are `Lzss::AddString` in LTCG (512/516 bytes, 51/408 comparable mismatches, 27 fields, distance 0.133), `CPbgFile::Write` in normal COFF (67/63, 10/59, one field, 0.233), and `EnemyManagerCreate` in LTCG (94/95, 22/75, five fields, 0.304). Ranking does not establish profile/physical ownership or exactness. | Fresh full dual-codegen run against the hash-attested target; input identity receipt; exact current backlog and strict probe outputs |
| ENEMY-010 | observed / source-present | TH10 has a reusable ECL-host lifecycle separate from the 14,416-byte Enemy opcode dispatcher. At both callback-subroutine transitions, `EnemyRuntimeUpdate` obtains a callback identifier, calls `0x0040C6E0` to free every payload/node pair in the full object's list at `+0x1034`, then calls `0x0040C730` to restore the embedded `0x1018` context at full `+0x008` as both active pointer `+0x004` and mirror `+0x1030`. Reset clears the current instruction and state, installs the full object at context `+0x1014` as the operand resolver, writes `-1` at context `+0x1010`, and clears allocation ownership without erasing the `0x1000`-byte operand payload. The maintained runtime source now expresses both target calls instead of collapsing them into one unresolved reset helper. The release body is exact; reset remains a 69-byte LTCG candidate versus the target's 71-byte ESI-preserving form. | TH10 direct disassembly and both call sites at `0x0040E1D3/DE` and `0x0040E28C/97`; target object offsets; current VC7.1 normal and linked diagnostics |
| TOOLCHAIN-043 | observed / exact-six-units | Six ECL lifecycle/resource functions have canonical target-bound `/GL` units. Two independent cold Enemy links reproduce `EnemyEclHostBaseView::ReleaseEclAllocations` at `0x0040C6E0` across 45 bytes with both REL32 fields replayed to the target `free` thunk `0x004524A1`; `EnemyEclContextView::EnemyEclContextView` across 15 raw-equal bytes; base-resource `LoadPackage` and `GetScriptData` across five raw-equal bytes each; and `EnemyEclResourceBaseView` construction/destruction across 34/36 bytes with exhaustive base-vtable `0x0046D0F0` and direct-`free` `0x00452422` fields. The complete PDB-owned extents total 140 bytes and decode without gaps. Negative manifests omitting the allocation-release REL32, base-constructor DIR32, or base-destructor REL32 are rejected. After the subsequent host-hierarchy definitions, current integer resolver label bindings are `$L3934/$L3257..$L3260` and float bindings are `$L3974/$L3335..$L3338`; their five relocation offsets, target case addresses, complete 80-byte extents, and ordinary bytes remain unchanged and exact. These base-resource results independently complete the class layer beneath the already exact derived resource constructor/destructor; they do not establish production link ownership. | Two cold pinned VC7.1 SP1 build6030 links; PE/map/PDB identity; exhaustive linked-field replay and omitted-field negative tests; hash-attested target; fresh strict COFF resolver replay |
| ENEMY-011 | observed / source-present | The primary Enemy object is a real C++ derivation from a reusable `0x103C`-byte ECL host. The base vtable at `0x0046D0D8` contains five typed execution/operand slots followed by scalar deleting destructor `0x0040C7B0`; the derived vtable at `0x0046D0C0` replaces the five slots and points its deleting slot at `0x0040CC50`. Base construction at `0x0040C710` installs the base vtable and clears the embedded context cursors; derived construction at `0x0040D830` performs the same base/context initialization before installing the derived vtable and appending the runtime tail. Derived teardown at `0x0040DAE0` restores the derived vtable and ends with the inline base-destructor sequence that restores `0x0046D0D8` and releases the `+0x1034` allocation list. Enemy-manager deletion reaches virtual slot `+0x14` with deleting flag one. Maintained source therefore uses `EnemyEclHostBaseView` as an actual polymorphic base, `EnemyFullObjectView` as its derived class, real constructors/destructors, `new`, and virtual `delete`, while preserving the exact `0x103C`/`0x2518` layouts. | Direct TH10 vtable contents/xrefs, constructor/destructor sequences and manager virtual-delete call; pinned VC7.1 layout and code-generation probes; no adjacent-game fact transferred |
| TOOLCHAIN-044 | observed / exact-seven-units | The recovered host hierarchy adds seven canonical target-bound units / 95 bytes. Normal COFF exactly reproduces the base neutral virtuals at `0x0040C5E0`, `0x0040C690`, `0x0040C6A0`, `0x0040C6B0`, and `0x0040C6C0` across 3/5/5/9/5 bytes; only the float reader has a link field, one DIR32 to zero-float constant `0x00470B04`. The `/GL` linked-image Oracle exactly reproduces the complete 21-byte PDB-owned base constructor with its vtable DIR32 and the complete 47-byte base destructor with its vtable DIR32 plus two REL32 calls to `free` thunk `0x004524A1`. Two independent cold builds pass all seven units. The hierarchy renumbered compiler-local symbols in the two existing 80-byte derived lvalue resolvers, but strict replay proves their code bytes, relocation offsets/types and target addresses remain exact. All 20 Enemy exact units now replay together across 511 bytes. Base reset and the large derived constructor/destructor remain source-present/non-exact; production TU partition and product link closure remain unknown. | Two cold normal-COFF and linked-PE replays; complete COFF/PDB extents; exhaustive relocation/linkage manifests; full current Enemy exact-source replay |
| ANM-001 | observed / source-present | TH10's ANM VM lifecycle establishes a complete `0x3AC`-byte object view. The constructor at `0x00402050` first clears bit zero in nine nested timer flags at VM `+0x6C/+0xB0/+0xFC/+0x128/+0x174/+0x1B0/+0x1FC/+0x228/+0x378`, clears exactly `0xEB` dwords, and sets the signed active-sprite index at `+0x384` to `-1`. The destructor at `0x00401FF0` frees and clears the optional generated-vertex pointer at `+0x358`. Reusable reset `0x00401DE0` preserves owner `+0x20` and three fields `+0x340/+0x344/+0x348`, clears the full object, restores self-linked sentinels at `+0x04/+0x10`, scale `+0x3C/+0x40`, identity matrix `+0x23C`, flags `+0x35C = 7`, primary timer `+0x5C`, seven observed state fields, and sentinel `+0x2FC = -1`. Two adjacent CC-delimited retained helpers implement matrix identity at `0x00401F00` and timer initialization at `0x00401F40`; both operations also appear inline in reset. TH08/TH095 were consulted only for the broad ANM-VM lifecycle hypothesis; every size, offset, boundary and behavior here comes from TH10-local evidence. | Hash-attested TH10 constructor/destructor/reset disassembly and xrefs; target store ranges and constants; pinned VC7.1 layout/code-generation probes; adjacent games used only as hypotheses |
| TOOLCHAIN-045 | observed / exact-four-units | Four ANM VM lifecycle functions have canonical target-bound units across 192 bytes. Fixed normal COFF reproduces the complete 34-byte `AnmVmView` destructor, including its sole REL32 field to target `free` at `0x00452422`, and the complete relocation-free 88-byte constructor. A shared `/GL` linked image reproduces the 55-byte `AnmMatrixView::SetIdentity` and 15-byte `AnmVmTimerView::Initialize` as raw-equal PDB-owned contributions with no linked fields. Two independent cold builds pass all four units. The maintained VM reset has the target semantics and matches the 273-byte target extent under normal COFF but differs in receiver/register ordering; the later explicit manager-factory linked context emits 271 bytes and is also non-exact. The target consumes its receiver through private EDX. The naturally emitted seven-byte timer constructor byte pattern appears at target `0x00405E10` and `0x004247C0`, not in the reviewed ANM helper neighborhood; existing ownership evidence does not bind either copy to this ANM source, so no mapping or exactness credit is assigned. | `.analysis/gpt-5.6-sol/20260914-anm-core/`; two cold normal-COFF/linked-PE canonical replays; complete COFF and PDB extents; direct target byte-pattern and ANM-boundary review |
| ASCII-001 | observed / source-present | The `0x89AC` owner published at `0x004776E0` is TH10's ASCII text manager, while the distinct ANM renderer is consumed through `0x00491C10`. TH10-local proof is the pair of `0x68`-byte text queues at `+0x076C` (256 records) and `+0x6F6C` (64 records), counts at `+0x896C/+0x8970`, append/format functions at `0x00401530-0x0040175D`, and renderers at `0x00401760/0x00401A50`; adjacent TH095 supplied only the initial class-role hypothesis. The owner embeds ANM VMs at `+0x14/+0x3C0`, loads `ascii.anm`, `text.anm`, and `capture.anm`, and registers update plus two draw callbacks. Constructor `+0x8974 = -1` is default color `0xFFFFFFFF`, `+0x8978/+0x897C` are scales, and `+0x898C = 9` is character-space width. The earlier lifecycle/layout observations remain valid under the corrected `AsciiManagerView` owner. | Hash-attested TH10 disassembly and raw PE bytes; append/draw field agreement; call sites; vtable/data xrefs; adjacent source used only to propose the role/name |
| TOOLCHAIN-046 | observed / exact-four-units | Four ASCII-manager functions have canonical exact units across 290 bytes. With `AsciiManagerCreate` as the real `/GL` entry, VC7.1 emits the complete 251-byte constructor with the target's private ESI receiver; its only fields are DIR32 references to vtable `0x0046CB14` and global `0x004776E0`. Fixed normal COFF emits the six-byte virtual `GetSize`, 26-byte update callback, and seven-byte regular-text draw callback exactly, with the callback's sole REL32 replayed to `0x00401760`. Renaming the semantically misclassified owner changes decorated probe symbols but preserves all 290 target bytes and field manifests. | `.analysis/gpt-5.6-sol/20260914-anm-manager-core/`; current canonical replays; complete COFF/PDB extents; exhaustive linkage manifests |
| TOOLCHAIN-047 | observed / workflow | The LTCG backlog and dual-lane ranking tools accept repeatable `--entry SOURCE=SOURCE_NAME` overrides when target call evidence requires a real maintained caller to expose interprocedural codegen. The probe resolves that source name through the normal-COFF symbol inventory, requires exactly one external decorated symbol, rejects an override whose source is absent from the selected backlog, and records the choice in JSON. In the corrected ASCII-owner experiment, making `AsciiManagerCreate` the linked entry emits the structurally exact 251-byte `AsciiManagerView` constructor with its target ESI ABI. Entry selection remains diagnostic and grants no exactness or physical-owner credit without a canonical unit. | Current `probe-ltcg-backlog.py`/`rank-exact-backlog.py`; explicit-entry linked reports; invalid-source negative run |
| ASCII-002 | observed / source-present | TH10's maintained ASCII pipeline covers the retained reset, regular/GUI appenders, three variadic formatters, and both queue renderers. `AsciiManagerStringView` is exactly `0x68` bytes: 64-byte text, `Float3` position, color, two scales, one untouched dword, viewport index, font-bank selector, and shadow flag. `AddString` bounds at 256 and copies the shadow field; `AddGuiString` bounds at 64 and leaves that final field untouched. The regular renderer selects glyph `(smallFont * 0x62 + character - 0x20)` from a `0x44`-byte table, applies 7/9 or scaled-space/14 advances, and can draw a two-pixel half-alpha shadow. The GUI renderer uses the first glyph bank and selects rounded versus non-rounded drawing through an exact `scaleX == 1.0f` comparison. Both switch viewports only on state changes and restore the default after flushing. | Target disassembly for `0x004014D0-0x00401C89`; raw boundary/padding review; target constants; formatter caller ABIs; TH095 used only as a source-shape hypothesis |
| TOOLCHAIN-048 | observed / exact-three-units | In the real `AsciiManagerCreate` `/GL` context, pinned VC7.1 build6030 reproduces three additional complete PDB contributions with no linked fields: `AsciiManagerView::ResetStrings` 26 bytes, `AddString` 132 bytes, and `AddGuiString` 109 bytes. Each candidate is raw-equal across its full CC-delimited target extent; canonical replay covers 267 bytes. The three variadic formatters and both renderers compile as maintained source but remain mismatches. | `.analysis/gpt-5.6-sol/20260914-anm-draw-core/`; target boundary review; linked PDB contributions; canonical cold replay |
| ANM-002 | observed / source-present | The real renderer reached through pointer `0x00491C10` owns a `0x20000`-entry packed vertex buffer. Each vertex is `0x1C` bytes, so the observed base at renderer `+0x3ADACC` spans exactly `0x380000` bytes to end cursor `+0x72DACC`; start cursor follows at `+0x72DAD0`. Sprite count is at `+0x3ADAC8` and per-frame flush count at `+0x58`. `ClearVertexBuffer @ 0x00442F30` clears the sprite count and resets both cursors. `AddSpriteToDrawBuffer @ 0x00442FE0` copies quad corners `0/1/2/1/2/3`, advances the end cursor by six vertices (`0xA8` bytes), increments the sprite count, and returns zero. `FlushVertexBuffer @ 0x00442F50` submits two D3D9 triangle-list primitives per sprite from the current start cursor, advances start to end, clears the count, and increments the flush count. | Hash-attested TH10 disassembly and 22 flush callers; buffer-offset arithmetic; target D3D9 vtable slots; TH08/TH095 used only for the initial source-shape hypothesis |
| TOOLCHAIN-049 | observed / exact-three-units | A regular-text draw `/GL` context reproduces the renderer buffer trio with their target private ABIs: `ClearVertexBuffer` is a raw-equal 29-byte PDB contribution with EAX receiver, `FlushVertexBuffer` is 133 bytes with ESI receiver and four exhaustive DIR32 fields to device global `0x00491C30`, and `AddSpriteToDrawBuffer` is a raw-equal 157-byte contribution with EAX/EDX inputs. Two independent cold canonical builds replay all 319 bytes. Because the pinned VC7.1 Platform SDK predates D3D9, maintained `D3d9View.hpp` declares only the target-used COM slots at vtable offsets `+0x10C/+0x14C/+0x164`, guarded by compile-time offset checks. | `.analysis/gpt-5.6-sol/20260914-anm-draw-core/renderer-*`; complete linked PDB contributions; exhaustive linked-field manifests; two cold 14-unit source replays |
| ANM-003 | observed / source-present | TH10's shared four-vertex draw owner is `DrawInner @ 0x00442670`. It adds renderer screen shake `+0x5C/+0x60`, optionally applies four x87 nearest-even rounds followed by the D3D9 half-pixel subtraction, copies UVs from sprite `+0x20..+0x2C` plus VM scroll `+0x54/+0x58`, culls four computed bounds against the unsigned viewport reached through global `0x00491FAC`, flushes on texture/shader changes, selects VM primary/secondary color at `+0x2FC/+0x300`, optionally mixes all BGRA channels with renderer color `+0x732458`, updates blend/filter state, and queues the quad. The axis-aligned owners at `0x00443080/0x00443290` implement the three anchor modes in VM flag bits 18-21. The no-round owner has a target-local difference: its common Z reads VM `spriteOffset.y @ +0x350`, while the rounded owner reads `spriteOffset.z @ +0x354`. | Hash-attested TH10 disassembly/decompilation and caller seams; target field accesses; compile-time layout checks; TH08/TH095 consulted only after TH10-local behavior was established |
| TOOLCHAIN-050 | observed / exact-five-units | The regular-text `/GL` context canonically reproduces the complete shared draw seam: `MixAnmColor` 25 bytes/no fields, `SetRenderStateForVm` 201 bytes/six fields, `DrawInner` 1,111 bytes/91 fields, `DrawNoRotation` 515 bytes/29 fields, and `DrawNoRotationNoRound` 486 bytes/29 fields. Two independent cold builds replay all 2,338 bytes and all 155 linked fields with zero differences. The `DrawInner` manifest resolves every shared-quad interior address through one `g_AnmQuadVertices` base plus reviewed addends, all four half-pixel references through `g_AnmHalfPixel`, all four unsigned-conversion constants, the viewport/device globals, and every helper call. The no-round source uses sequential floating accumulation to preserve VC7.1 evaluation order and explicitly retains the observed `+0x350` Z input. The full current `AnmManager.cpp` replay passes 19 exact units / 3,406 bytes across four artifacts. | `.analysis/gpt-5.6-sol/20260914-anm-draw-core/`; two cold canonical linked-PE builds; complete PDB contribution extents; exhaustive linked-field manifests; full-source exact replay |
| ANM-004 | observed / source-present | `AnmRenderManagerView::ProjectCameraFacingQuad @ 0x00443B60` builds an identity world matrix translated by VM `position + preservedPosition + spriteOffset`, projects zero and camera-right through the active owner at global `0x00491FAC`, rejects the first projected Z outside `[0,1]`, derives screen scale from the projected-vector distance, applies VM Z rotation and anchor bits 18-21, and writes the projected Z to all four shared vertices. The target-proved viewport-owner layout is camera-right `+0x30`, view matrix `+0x4C`, projection matrix `+0x8C`, and viewport `+0xCC`. The 33-byte wrapper at `0x00443F80` propagates projection failure or enters `DrawInner(vm, 0)`. Direct CC-boundary review also adds the Ghidra-missed `TranslateRotation @ 0x00443680` to the denominator. TH095 was consulted after TH10-local recovery and differs in its final Z source. | Hash-attested TH10 disassembly, import thunk, callers and global accesses; compile-time layout checks; adjacent source used only as a later source-shape hypothesis |
| TOOLCHAIN-051 | observed / exact-two-units | The regular-text `/GL` context canonically reproduces the complete 1,047-byte `ProjectCameraFacingQuad` contribution with all 25 DIR32/REL32 fields and the complete 33-byte `DrawCameraFacingQuad` contribution with both internal REL32 calls, for 1,080 newly exact bytes. VC7.1 build6030 LTCG local-symbol ordering is codegen-significant in the projection body: a semantic-only rename of the two 12-byte projection temporaries preserves the 1,047-byte extent but changes exactly eight stack-displacement bytes; the retained source form replays every byte. The 50 effective instruction bytes of `TranslateRotation` are raw-equal, but its PDB contribution owns two additional trailing `CC` bytes, so it remains non-exact under the complete-contribution Oracle. | `.analysis/gpt-5.6-sol/20260914-anm-draw-core/camera-facing-probe-*.json`; canonical linked-PE replay; complete PDB extents and exhaustive linkage manifests |
| ANM-005 | observed / source-present | The former combined `0x004436C0-0x00443B5E` range is two real 591-byte rotated-draw entries separated by `CC @ 0x0044390F`. `Draw @ 0x004451C0` extracts VM render mode from flags bits 22-25 and tail-jumps from mode 1 to `0x004436C0` and mode 3 to `0x00443910`. The bodies are instruction-identical except for their REL32 displacements: zero Z rotation enters `DrawNoRotationNoRound`; otherwise they use one x87 `FSINCOS`, accumulate `spriteOffset + preservedPosition + position` for X/Y/Z, scale sprite dimensions, apply anchor modes from bits 18-21, rotate all four shared XY pairs, and call `DrawInner(vm, 0)`. Maintained names are `Draw2D` and TH08-corroborated `Draw2DRotatedOrAxisAligned`; the second original identifier remains a cross-game hypothesis. | Hash-attested TH10 raw boundaries and complete bodies; Ghidra xrefs from the target Draw jump table; target-local field/control-flow evidence; TH08 consulted only for the mode-3 name hypothesis |
| TOOLCHAIN-052 | observed / near-two-units | Pinned VC7.1 SP1 build6030 `/GL` emits complete 591-byte PDB contributions for both maintained rotated-draw methods. Each has 507 comparable bytes after 21 decoded link fields and matches 497; the only ten non-linkage byte differences are an equal-length scheduling permutation at `+0x54..+0x5E`, where the target completes horizontal-anchor masking before beginning the Y-offset load. The stack extent, x87 `FSINCOS`, anchor branches, fully inlined four-corner rotation, common Z stores, and both helper calls otherwise align. Definition-order, explicit-mask, calculation-order, method-name and aggregate-layout probes do not close the scheduling gap; the aggregate and reordered-calculation variants are materially worse. Both units remain non-exact, and no inert dependency, padding or target bytes are introduced. | `.analysis/gpt-5.6-sol/20260915-anm-draw2d/draw2d-probe-*.json`; complete target disassembly; PE/map/PDB contribution extents; natural-source variants |
| ANM-006 | observed / source-present | `Project3DQuad @ 0x00444240` maintains two VM matrices at `+0x23C/+0x27C`. Unless flag bit 14 suppresses recomputation, dirty scale/rotation bits copy the first matrix to the second, multiply `_11/_22` by VM scales, apply nonzero X/Y/Z rotations through the D3DX helpers, and clear the dirty bits. A local 256-by-256 quad uses anchor bits 18-21; X/Y add `spriteOffset + preservedPosition + position` to the cached matrix translation, while TH10 replaces Z with that three-vector sum. Four `D3DXVec3Project` calls use the active viewport owner, and the completed matrix is cached at renderer `+0x3AD0F0`. `DrawProjected3DQuad @ 0x00444580` then calls `DrawInner(vm, 0)` and restores all shared vertex RHW fields to `1.0`. TH095 supplied the initial source-shape hypothesis, but the Z behavior, offsets, calls, flags and boundaries are TH10-local. | Hash-attested TH10 disassembly and call targets; target field/global accesses; compile-time VM/renderer layout checks; adjacent source used only as a hypothesis |
| TOOLCHAIN-053 | observed / exact-one-unit | Pinned VC7.1 SP1 build6030 `/GL` reproduces the complete 818-byte PDB-owned `Project3DQuad` contribution. Canonical linked replay covers three double-zero constants, six D3DX matrix-helper calls, four viewport-owner references, four shared-quad references, and four `D3DXVec3Project` calls: all 21 fields and all remaining bytes compare exactly. The first natural candidate was 822 bytes and otherwise aligned until Z translation; changing Z from matrix-relative addition to target-observed replacement removed exactly four bytes and closed the unit. The 60-byte draw wrapper is source-present but needs the real dispatcher as LTCG entry to reproduce its private ESI/EDI ABI; as a standalone entry it retains a 72-byte conventional thiscall shell. | `.analysis/gpt-5.6-sol/20260915-anm-projected/project3d-probe-*.json`; canonical linked-PE replay; complete target disassembly and PDB extent |
| ANM-007 | observed / source-present | `AnmRenderManagerView::DrawMode6 @ 0x00443FB0` first projects a camera-facing quad, computes the distance from accumulated VM position to the background camera at `0x00491D7C`, and optionally applies the renderer color multiplier. Beyond the near distance at `0x00491E78`, it interpolates BGRA toward float B/G/R channels at `0x00491E80/84/88` and zero alpha across the near/far interval, rejects a completed fade, copies the result to all four shared vertices, and enters `DrawInner(vm, 2)`. `Draw @ 0x004451C0` independently rejects invisible, draw-disabled, and zero-primary-alpha VMs, then dispatches flag bits 22-25 across modes 0-9. Mode 9 passes the VM vertex pointer and twice its count to the generated-vertex owner. The dispatcher's physical PDB contribution is 216 bytes through `0x00445297`: 173 code bytes, a three-byte alignment instruction, and the ten-entry absolute jump table at `0x00445270`. | Hash-attested TH10 disassembly, target global/field accesses and dispatch table; real caller at `0x00409230`; compiler-linked PDB contribution extent |
| TOOLCHAIN-054 | observed / exact-two-units | Supplying the real two-VM callback `AnmPairOwnerDrawCallback @ 0x00409230` as the `/GL` entry reproduces that complete 50-byte callback and the complete 60-byte `DrawProjected3DQuad` private ESI/EDI form. Canonical replay covers four callback fields and six wrapper fields with zero differences, adding 110 exact bytes. In the same context, natural `DrawMode6` is 645 bytes versus the 643-byte target because VC7.1 assigns a `0x34` candidate stack frame where the target uses `0x20`; its private EDI/ESI ABI and behavior otherwise align. The source-present `Draw` dispatcher remains non-exact at 208 bytes versus its 216-byte physical code/alignment/table contribution while mode 7, direct 3D, and generated-vertex callees remain only declarations. Full `AnmManager.cpp` replay passes 24 exact functions / 5,414 bytes across five cold artifacts. | `.analysis/gpt-5.6-sol/20260915-anm-projected/draw-real-caller-probe.json`; canonical linked-PE replay of `anm-pair-owner-draw-callback` and `anm-render-draw-projected-3d-quad`; complete target/PDB extents |
| TOOLCHAIN-055 | observed | A linked-image PDB contribution may contain executable code followed by inline data, as proven by `Draw` and its absolute jump table. Capstone decodes the data tail as if it were instructions and can therefore report a false relative control-flow immediate overlapping a real PE `HIGHLOW` base relocation. `linked_code_fields()` now records confirmed base-relocation byte extents first and suppresses only Capstone-derived relative fields that overlap them; the authoritative absolute field remains, and the existing general overlap rejection still fails closed for every other collision. This removes the false `overlapping linked-image fields` diagnostic without weakening ordinary code-field checks. | Mixed code/data `Draw` linked-image probe before and after the fix; PE base relocation table; `python3 -m py_compile scripts/linked_image.py`; canonical linked replay |
| ANM-008 | observed / exact | `AnmRenderManagerView::DrawMode7 @ 0x004445C0` is TH10's projected-3D photo-blend path. It calls `Project3DQuad`, then transforms each of the four 0x14-byte renderer-local source vertices at `+0x3ADA78` through cached world matrix `+0x3AD0F0` using `D3DXVec4Transform`. Each transformed XYZ is measured independently from camera global `0x00491D7C`. Vertices inside near distance `0x00491E78` retain the selected VM color; intermediate vertices interpolate B/G/R toward float channels `0x00491E80/84/88` while preserving VM alpha; vertices at or beyond the far end receive dword color `0x00491E90` with VM alpha restored. Unlike mode 6, mode 7 does not apply renderer mix color and does not reject a fully blended quad. It enters `DrawInner(vm, 2)` and restores all shared RHW fields to one. | Hash-attested TH10 disassembly and fields; canonical 402-byte linked-PE replay with all 24 fields; TH095 exact source consulted only as the initial source-shape hypothesis |
| TOOLCHAIN-056 | observed / exact-one-unit | In the real two-VM draw-callback `/GL` context, the direct three-pointer spelling of `DrawMode7` compiled to a 404-byte PDB contribution because LTCG rebased the color induction variable from alpha to the color dword. Returning to the natural indexed four-vertex loop lets VC7.1 derive the target alpha, source, and transformed-vertex cursors and reduces the body to 403 bytes. Preserving the selected red and alpha bytes as one live 16-bit color snapshot across the last `__ftol2` conversion produces the target word reload and closes the complete 402-byte contribution and all 24 linked fields. Renaming the old pointer cursor through eight independently calibrated identifier buckets produced the identical 404-byte negative Oracle, so identifier hashing does not control this register induction choice. | `.analysis/gpt-5.6-sol/20260916-anm-mode7-buckets/`; two focused canonical cold replays; complete target/candidate disassembly and PDB contribution extents |
| ANM-009 | observed / source-present | `SetRenderStateForVm3D @ 0x004423E0` is the direct-3D sibling of the batched 2D state owner. It flushes before incompatible blend, texture-factor, or sampler changes; maps blend mode zero to inverse-source-alpha and modes one and two to one; selects the VM primary/secondary color; optionally applies the renderer's per-channel mix; publishes the result through D3D9 texture factor; and maintains the point/linear filter cache. `Draw3D @ 0x00444760` flushes pending 2D sprites, updates the VM scale/Euler-rotation matrix, applies anchor translation from all three VM position vectors, sets world and optional texture transforms, installs the renderer's 0x14-stride four-vertex stream, and draws a two-primitive triangle strip. TH10 binds the sprite texture before refreshing the sprite/UV transform cache, tests `uvScrollX` twice in the texture-transform condition, and briefly forms world Z without `spriteOffset.z` before overwriting it with the full three-vector sum after the state helper; maintained source preserves each observed operation. | Hash-attested TH10 disassembly, target D3D9 vtable calls, renderer/VM field accesses and dispatcher ABI; compile-time layout checks; TH095 source used only as the initial source-shape hypothesis |
| TOOLCHAIN-057 | observed / near-two-units | In the real ANM `/GL` context, maintained `SetRenderStateForVm3D` and `Draw3D` reproduce the complete target PDB contribution sizes exactly: 440 and 929 bytes. The state owner matches 396 of 408 comparable non-linkage bytes and the draw owner matches 816 of 829; all remaining differences are single contiguous, equal-length instruction-scheduling regions around a D3D9 COM call. In each target region the pushed value and cache store precede loading the global device/vtable in an order VC7.1 does not reproduce from the repository's minimal explicit vtable view; assignment-expression, explicit-local, and inline member-wrapper probes leave the same output. This is compiler/source-frontend inference only, so neither function is promoted to exact and no artificial dependency or assembly constraint is introduced. | `.analysis/gpt-5.6-sol/20260915-anm-draw3d/draw3d-probe-*.json`; complete target/candidate disassembly; PDB contribution extents and linked-field reports |
| ANM-010 | observed / source-present | The generated-geometry corridor contains eight authored source bodies. `InitializeHorizontalTextureStrip @ 0x00444B10` and its coordinate-transposed vertical sibling at `0x00444BE0` fill alternating rows/columns with sprite UVs, VM primary color, and unit RHW. `SetGeneratedVertexColor @ 0x00444CB0` copies primary color across a 0x1C-stride range. `DrawGeneratedVertices @ 0x00444CE0` is dispatcher mode 9's visible/alpha-gated textured triangle-strip path; `QueueSpriteQuad @ 0x00444DC0` feeds a supplied quad into the shared batch. The 0x14-stride untextured strip/fan paths at `0x00444E60/0x00444FA0` temporarily select diffuse-only texture operations and disable Z writes, then restore renderer caches and normal texture operations. `DrawTexturedTriangleFan @ 0x004450E0` is the textured fan path called with 33 generated vertices at `0x00445880`. The initial Ghidra inventory missed complete CC-delimited entries at `0x00444CB0`, `0x00444DC0`, `0x00444E60`, and `0x00444FA0`; those four are now in the denominator. Names without target callers are descriptive or adjacent-supported, while boundaries, ABIs, state values, strides and behavior are TH10-local. | Hash-attested target disassembly and boundaries; Ghidra dispatcher/caller xrefs; bounded absolute-pointer and direct-call scans; TH08/TH095 source consulted after TH10 behavior was established |
| TOOLCHAIN-058 | observed / exact-three-units / near-five-units | Pinned VC7.1 SP1 build6030 `/GL` in the real ANM draw context reproduces the complete relocation-free PDB contributions for horizontal strip initialization (201 bytes), vertical strip initialization (201), and generated-vertex color copying (34), adding 436 canonical exact bytes after two independent cold replays. The remaining five natural source bodies are also unusually close: `DrawGeneratedVertices` and `DrawTexturedTriangleFan` reproduce their 224/211-byte extents and differ only in one shared 13-byte texture-bind scheduling region; `QueueSpriteQuad` and both 313-byte untextured primitive submitters reproduce every target instruction and linked field, but their candidate PDB contributions own two following `CC` bytes beyond the reviewed target code extents. Strict extent policy therefore keeps all five non-exact. | `.analysis/gpt-5.6-sol/20260915-anm-generated/generated-probe-initial.json`; `canonical-replay-1.json` and `canonical-replay-2.json`; target/candidate disassembly and PDB contribution extents |
| ANM-011 | observed / source-present | TH10's generated pulsing radial trail uses a 0x4B0-byte VM-owned payload: 33 textured 0x1C-byte vertices, 33 radii, 33 radial velocities, a two-float UV velocity and one trailing dword. `InitializePulsingRadialTrail @ 0x004452F0` replaces the allocation, stores update/draw callbacks at VM `+0x398/+0x39C`, initializes a center vertex plus 31 radial vertices, and derives radii/velocities from RNG object `0x004918B0`. `UpdatePulsingRadialTrail @ 0x00445620` scrolls both U and V with the payload X velocity, adds one to all 33 coordinates after a negative wrap, advances/closes the ring, copies VM primary color to the center, and clears ring alpha. `DrawPulsingRadialTrail @ 0x00445880` submits all 33 vertices through the textured triangle-fan path. `AnmFloat3View::FromAngleMagnitude @ 0x004458B0` uses one x87 `FSINCOS` and intentionally leaves Z unchanged. TH095 supplied the initial source-shape hypothesis only after TH10 fields, constants, callbacks, RNG state, boundaries and the V-uses-X quirk were established locally. | Hash-attested TH10 disassembly and direct caller at `0x0043FDFB`; callback stores and calls; target field/constant accesses; compiler source-shape comparison; committed TH095 source as later hypothesis material |
| RNG-001 | observed / exact-four-units | The eight-byte gameplay RNG view contains a 16-bit seed, saved seed and 32-bit generation counter. `GetRandomU16 @ 0x0044B9B0` applies `((seed ^ 0x9630) - 0x6553)`, rotates the top two bits into the low end through multiply-by-four arithmetic, increments the counter and returns the new seed. `GetRandomU32 @ 0x0044B9E0` concatenates two successive results. `GetRandomF32 @ 0x0044BB20` divides by `UINT_MAX`; `GetRandomF32Signed @ 0x0044BB90` divides by `INT_MAX` and subtracts one. Natural `RandomMath.cpp` under pinned normal VC7.1 reproduces all 341 bytes and all five constant relocations exactly. Boundary review additionally found an unreferenced instruction-identical 78-byte body at `0x0044BA30`; it remains origin-unknown and source-absent. | Complete target bodies and direct caller scan; normal COFF symbol extents/relocations; canonical cold replay of four units; TH095 consulted only for naming/source-shape corroboration |
| TOOLCHAIN-059 | observed / multi-source-LTCG | The linked-image driver, backlog probe, match-unit builder and exact replay now support an ordered list of repository-local `/GL` support sources. `probe-ltcg-backlog.py --support SOURCE=SUPPORT_SOURCE` compiles each input separately with the same pinned profile, links them in one diagnostic image, and still attributes compared functions only to the primary `source.ltcg.obj` PDB contribution. Manifest `support_sources` participates in build grouping and cold replay. With `RandomMath.cpp` supplied to the ANM initializer context, VC7.1 sees the real RNG definitions and expands the loop calls: `InitializePulsingRadialTrail` changes from a 572-byte single-source candidate to 804 bytes against the 803-byte target. It remains non-exact because the true caller is the source-absent 9,587-byte ANM script executor, whose target call fixes the VM in EDI. The same multi-source artifact canonically replays `DrawPulsingRadialTrail` 25 bytes/two fields and `FromAngleMagnitude` 30 bytes/no fields exactly. | `.analysis/gpt-5.6-sol/20260915-anm-trail/ltcg-init-context.json` and `ltcg-init-with-rng.json`; pinned compiler/linker; PE/map/PDB ownership; full 29-unit ANM cold replay using multi-source context |
| ANM-012 | observed / source-present | The four script-variable helpers immediately before `ExecuteScript @ 0x0043EE30` establish VM integer locals at `+0x30C..+0x318`, float locals at `+0x31C..+0x328`, and counter locals at `+0x32C/+0x330`. `GetFloatVar @ 0x0043EAC0` accepts IDs 10000-10021: the locals and counters, three RNG forms selected between owners `0x004918A8/0x004918B0` by VM flag bit 30, VM position, background-camera Float3 `0x00491D7C`, and a second conservatively named Float3 at `0x00491DA0`. `GetIntVar @ 0x0043EC70` resolves IDs 10000-10009, converting the four float locals through `__ftol2`. The two pointer helpers at `0x0043ED00/0x0043EDA0` first test the instruction argument mask, then redirect writable float operands to four float locals or position XYZ and writable integer operands to four integer locals or two counters. Their reviewed physical contributions include the trailing absolute jump tables: 424, 144, 156, and 132 bytes respectively. TH095 corroborates source shape and names only through position Z; TH10-local reads establish the extra six global IDs and every accepted offset. | Hash-attested target disassembly and physical boundaries; attested caller/decompilation of the adjacent script executor; target global/VM accesses; compiler-generated table layouts; adjacent source used only as a later hypothesis |
| TOOLCHAIN-060 | observed / exact-three-units / near-one-unit | A shared multi-source `/GL` artifact with `AnmManager.cpp` as primary, `RandomMath.cpp` as support, and `GetFloatVar` as the real maintained entry reproduces complete PDB contributions for `GetIntVar` (144 bytes), `GetFloatVarPtr` (156), and `GetIntVarPtr` (132). Canonical replay validates all ordinary calls plus every self-relative absolute jump-table entry and compares all 432 bytes exactly. Capstone can stop inside the last absolute table entry because the data suffix is not an instruction stream; linked-image normalization now accepts such a suffix only when every undecoded byte belongs to complete PE `HIGHLOW` fields, while ordinary undecoded bytes and undeclared fields still fail closed. `GetFloatVar` remains source-present at 452 candidate bytes versus the 424-byte target: the candidate public entry saves/restores ESI around a conventional this receiver, whereas the true target caller is the source-absent `ExecuteScript`, which already retains the VM in ESI. Full affected replay passes 36 exact functions / 6,678 bytes across eight cold artifact builds and two source files. | `.analysis/gpt-5.6-sol/20260915-anm-runtime/normal-inventory.json`, `ltcg-get-float-entry.json`, and `exact-*.json`; canonical linked-PE replay; complete target/PDB contributions and exhaustive field manifests |
| ANM-013 | observed / source-present | `AnmRenderManagerView::ExecuteScript @ 0x0043EE30-0x004413A2` is the 9,587-byte physical owner for TH10's complete variable-length ANM instruction loop; its separate absolute dispatch table begins at `0x004413A4`. The eight-byte instruction header stores signed opcode, unsigned size, signed time and a writable-argument mask before the argument payload. Maintained source covers all opcodes `-1..92`, interrupt save/return, local-variable arithmetic and branches, RNG/trigonometry, sprite/render flags, child-VM creation calls, render-mode setup, and the post-instruction update of angular velocity, scale growth, UV scrolling, position offset, seven interpolation slots, generated mode-9 geometry and the script timer. Target-local offset review closes the 0x3AC-byte VM's interpolation region `+0x070..+0x233`, matrices `+0x23C/+0x27C/+0x2BC`, BGRA colors `+0x2FC/+0x300`, script variables `+0x30C..+0x330`, three position vectors, flags, interrupt state, resource/script/sprite references and callbacks. The four child-creation methods and VM-id lookup are source-present non-exact callees. | Complete hash-attested target disassembly and physical extent; attested decompilation/callees/xrefs; typed instruction and VM offset cross-checks; TH08/TH095 consulted only after TH10-local recovery for source-family hypotheses |
| ANM-014 | observed / source-present | The executor's interpolation family consists of a 0x4C-byte Float3 slot, two 0x4C-byte integer-color slots, two 0x2C-byte scalar-alpha slots and a 0x3C-byte Float2 scale slot. Evaluators implement modes `0..17`, including endpoint completion, state-mutating add/accelerate behavior, component-wise integer truncation in Hermite color mode, and the shared curve function at `0x0044C350`. Setup functions at `0x0041AB70`, `0x00441F50`, `0x00442050`, `0x00442220` and `0x00442300` install endpoints/mode/duration and reset their embedded timers. `SetSprite @ 0x0043E5A0` binds the loaded sprite and rebuilds texture matrices; `AddNormalizeAngle @ 0x0044BC10` bounds a signed angle with the target loop cap; timer `Add @ 0x0044BF40` applies the speed pointer outside the near-one interval. | Target helper bodies/callers and VM offsets; target constants and private ABIs; maintained executor call context; adjacent interpolation code only as later hypothesis material |
| TOOLCHAIN-061 | observed / exact-eight-units / central-owner-open | Making the complete maintained `ExecuteScript` the pinned VC7.1 SP1 build6030 `/GL` entry supplies the real interprocedural context for eight canonical units. Two independent cold builds reproduce child-node insertion (23 bytes), scale setup (136), `SetSprite` (356), secondary-alpha setup (116), both color setups (208 each), `AddNormalizeAngle` (94) and timer `Add` (88): **1,229/1,229 bytes** plus all declared linkage fields are zero-difference. Source order is codegen-significant: duration-before-mode recovers the secondary-alpha ECX/DL seam, constructing final and initial integer color values before assignment recovers the color-helper register schedule, and the target `RET 8` requires the angular helper's `__stdcall` convention. Primary-alpha setup remains 128/130 bytes because the partial candidate registerizes mode in EDX while the target keeps it on the stack. The complete executor is source-present but non-exact at a current 8,548-byte PDB contribution versus 9,587 target bytes; this diagnostic has `acceptance_authority=none` and is not promoted. Full current-source replay separately passes all 40 `AnmManager.cpp` canonical units / 7,566 bytes across eight cold artifacts. | `.analysis/gpt-5.6-sol/20260915-anm-executor/ltcg-execute-final.json`, `replay-exact-final-{4,5}.json`, and `replay-anm-full-final.json`; canonical match units; PDB-owned extents; complete target disassembly |
| TOOLCHAIN-062 | observed / near-match / no-acceptance-authority | Compiling `AnmManager.cpp` with the maintained `ExecuteScript` as `/GL` entry and `RandomMath.cpp` as support supplies both the target-observed EDI caller context and real RNG bodies to `InitializePulsingRadialTrail`. Its candidate becomes exactly 803 bytes against the 803-byte target with 631 comparable bytes and only eight ordinary-byte differences. Moving the direction temporary into loop scope lets VC7.1 reuse the target's earlier 12-byte stack slot, and placing the vertex-Z store before the UV stores recovers the target schedule; these changes remove 18 of the prior 26 differences. The remaining bytes are one independent load moved across an x87 multiply and one commutative Y-add operand order. The result remains non-exact and is not entered in `matches.csv`; `UpdatePulsingRadialTrail` likewise remains 598 bytes with two ordinary displacement-byte differences. | `.analysis/gpt-5.6-sol/20260915-anm-executor/ltcg-execute-with-rng-v2.json`; target/candidate full disassembly; diagnostic linked-image Oracle with `acceptance_authority=none` |
| ANM-015 | observed / source-present | TH10's ANM manager owns an inline pool of 4096 complete 0x3AC-byte VMs at manager `+0x68`, 4096 occupancy bytes at `+0x3AC068`, the next pool index at `+0x3AD068`, primary and secondary list head/tail pairs at `+0x72DAD4..+0x72DAE0`, twenty draw-layer sentinel VMs at `+0x72DAE4`, and the next nonzero VM id at `+0x732454`. Each VM has a distinct manager-order node at `+0x04` and parent/child layer node at `+0x10`; its draw-layer link and layer value are `+0x1C/+0x20`. Maintained source now covers pool allocation, front/back insertion in both manager lists, two-list lookup, stale-id clearing, interrupt/immediate-interrupt propagation, tree deletion, screen/world position propagation, position lookup, loaded-resource deletion, and all twelve creator variants. The creator family consists of four render-layer forms plus screen and world position forms for each; the world forms add TH10's directly observed `(+224,+16)` playfield origin. Six creator bodies plus `GetVmPosition` omitted by the initial Ghidra inventory are now included in the candidate denominator from direct target boundary review. | Hash-attested TH10 manager/VM address arithmetic, creator corridor and call graph; direct i386 disassembly and CC-delimited boundaries; maintained VC7 layout assertions; adjacent source used only after TH10-local recovery for name/source-shape hypotheses |
| ANM-016 | observed / source-present | Three adjacent loaded-ANM methods implement distinct script transitions. `InitializeAndExecuteScriptIndex @ 0x0043E710` performs new-VM initialization before binding and frame-zero execution; `SetAndExecuteScriptIndex @ 0x0043E7E0` binds and executes without that initializer and clears the complete VM when the script is absent or the resource is loading; `SetAndExecuteScriptIdx @ 0x0043E8B0` leaves an invalid existing VM unchanged, otherwise rebuilds flags, primary color, timer and all seven interpolation durations before frame-zero execution. Its pending horizontal-flip path negates `scaleX`, not rotation. Each valid path stores resource/script identity, clears flip bits 9-10, hides the VM, executes the script through the target's callee-cleans `RET 4` entry, and increments manager counter `+0x4C`. | Complete target bodies and direct calls; target field writes and constants; maintained complete executor context; TH08/TH095 used only for later method-family/name corroboration |
| TOOLCHAIN-063 | observed / exact-three-units / near-lifecycle-family | Pinned VC7.1 SP1 build6030 `/GL` in the complete maintained ANM executor context reproduces `SetAndExecuteScriptIndex` (194 bytes), primary-tail insertion (118), and secondary-tail insertion (118) exactly. Two final independent cold replays rebuild all 43 canonical `AnmManager.cpp` units / 7,996 bytes across eight artifacts with zero differences. The 287-byte restart method has the exact target extent and 249/271 comparable bytes, with all 22 ordinary differences confined to an ESI/EDI register exchange. Other lifecycle bodies remain source-present/non-exact: initialize-and-bind 197/203, `InitializeVm` 201/217, allocation 153/161, lookup 65/73, stale-id lookup 19/21, creator shapes 74/73, 93/95 and 116/118, and the two front insertion variants 94/112. These diagnostics have no acceptance authority. Repository totals become 93 exact functions / 9,987 bytes from 1,305 candidates and 212 source mappings. | `.analysis/gpt-5.6-sol/20260915-anm-child-vm/ltcg-execute-v13.json`, `ltcg-execute-v17.json`, `replay-anm-final-{4,5}.json`; canonical match units and linked-field manifests; complete target/PDB extents |
| TOOLCHAIN-064 | observed / tooling-fixed | Progress generation formerly joined `functions.csv`, `function-origins.csv`, and `reccmp-functions.csv` using literal address strings even though tracking validation correctly treats address spelling numerically. A valid lowercase `0x00448d00` mapping and uppercase `0x00448D00` origin therefore raised `KeyError` and prevented report generation. `scripts/progress.py` now parses every cross-ledger address with base-zero integer conversion before lookup; regenerated Markdown/SVG progress and tracking validation both succeed with mixed hex case. | Reproduced `KeyError: '0x00448d00'`; patched progress generator; regenerated `docs/PROGRESS.md` and `resources/progress.svg`; tracking/CI validation |
| TOOLCHAIN-065 | observed / provider-rebound | The Factory-native `th10-ghidra` refusal was an intentional fail-closed implementation-binding check, not a target or Ghidra-project failure. Commit `f5a0c4f` added the pinned Capstone 5.0.6 identity to registered `config/tools.lock.toml`, changing that file's SHA-256 from `c10dde50...` to `24d77ab2...` and the provider aggregate from `cc1e1bd9...` to `406ab2a2...`; the ignored private operator configuration still held the former value. After reviewing that sole registered-file change, the private binding was refreshed. Factory validator, live MCP discovery, and live MCP `check {}` all passed for `target:th10-main` with `provider_transport=factory-native-command`, the canonical executable SHA-256 and six mapped-byte samples. | Git history and per-file/aggregate SHA-256 recomputation; Factory registration renderer and validator; live Factory MCP discovery/check attestation |
| ANM-017 | observed / source-present | `UpdatePrimaryVms @ 0x00448860` rebuilds transient draw layers 0–18 from the primary manager-order list; `UpdateSecondaryVms @ 0x00448900` clears the shared processed count and rebuilds reserved layer 19 from the secondary list. Both save each manager-node successor before callbacks, remove VMs marked by flag `0x04000000` or completed by `ExecuteScript`, run the position callback before the executor, append survivors through VM `nextInDrawLayer +0x1C`, and increment manager `+0x64` for every visited VM. `DrawLayer @ 0x00448980` skips newly marked VMs, invokes an optional draw callback at `+0x39C`, then reaches the common draw dispatcher. `RemoveVm @ 0x00448BB0` updates either manager head/tail pair, unlinks both VM nodes, clears the corresponding occupancy byte for VMs inside the 4096-entry pool, frees generated storage, and resets the VM in place; heap fallbacks are destroyed and deleted. | Fresh Factory-attested TH10 decompile/disassembly/calls/xrefs; direct target disassembly; maintained VC7 layout assertions and source; adjacent source only as an initial control-flow hypothesis |
| TOOLCHAIN-066 | observed / exact-four-units / destructor-open | Pinned VC7.1 SP1 build6030 `/GL` in the complete maintained ANM executor context canonically reproduces `UpdatePrimaryVms` (149 bytes), `UpdateSecondaryVms` (123), `DrawLayer` (67), and `RemoveVm` (268). Two independent cold replays rebuild all **607/607 bytes** and eleven declared `RemoveVm`, `ExecuteScript`, `Draw`, `free`, `Initialize`, and `operator delete` REL32 fields with zero differences. The manager destructor at `0x00446220` is separately source-present: it drains both lists through `RemoveVm`, then compiler/member order destroys twenty sentinel VMs, destroys the embedded primary VM whose generated-vertex pointer is at manager `+0x3AD488`, and destroys 4096 pool VMs. Its current 197-byte candidate versus 206-byte target remains non-exact because the target retains a different SEH/vector-destructor context. Repository totals become 97 exact functions / 10,594 bytes from 1,305 candidates and 217 source mappings. | `.analysis/gpt-5.6-sol/20260915-anm-manager-update/final-cold-replay-{1,2}.json`; canonical linked-image match units; target/PDB extents; focused mismatch report for the destructor |
| ANM-018 | observed / source-present | `AnmRenderManagerView::AnmRenderManagerView @ 0x00445900` constructs the 4096 pool VMs, one complete embedded primary VM at manager `+0x3AD130`, and twenty draw-layer sentinel VMs before its body clears the complete `0x732460`-byte manager. It initializes the two global four-vertex UV templates, restores the manager state caches, initializes every pool VM, registers update callbacks at priorities `0x1A/0x08`, registers draw callbacks for layers 0–16 and 19 at their eighteen target priorities, and clears the active D3D9 vertex shader. The embedded VM's relative `generatedVertices +0x358` is manager `+0x3AD488`; this corrects the former interpretation of that field as a standalone allocation owner. `SetupVertexBuffer @ 0x004462F0` constructs the manager's fixed `-128..+128` XYZ/UV quad, mirrors XYZ/UV into the independent `0x00497930` background vertices, creates and fills an 80-byte managed D3D9 vertex buffer, and binds stream zero with stride `0x14`. | Hash-attested TH10 disassembly and member-construction sequence; raw caller at `0x0042025B`; callback addresses/priorities; D3D9 vtable slots; compile-time manager/VM layout checks; TH095 consulted only after TH10 established the embedded-primary-VM hypothesis |
| ANM-019 | observed / source-present | `ApplyTextureAlphaBleed @ 0x004465B0` locks texture surface level zero and fills RGB for zero-alpha pixels from nonzero-alpha orthogonal neighbors. It supports serialized/D3D formats zero and 21 as 32-bit ARGB, 25 as A1R5G5B5, and 26 as A4R4G4B4; one neighbor is copied, two or more are averaged, and no neighbors produce zero RGB while alpha remains zero. Its physical contribution is 1,463 bytes through `0x00446B66`: code returns at `0x00446B38`, followed by three alignment bytes, a four-entry absolute jump table and a 27-byte selector table. Four target callers at `0x00446C38`, `0x00446D48`, `0x00446E92`, and `0x00446F1F` establish the texture-entry argument. | Hash-attested TH10 code/table bytes, direct call scan, surface ABI and target format tables; Factory-attested decompilation; TH095 source used only after target-local control flow and formats were established |
| TOOLCHAIN-067 | observed / setup-core-near | The first complete `/GL` diagnostic for the new setup bodies compiles and links in the maintained `ExecuteScript` context with `RandomMath.cpp` support. `SetupVertexBuffer` is 475 candidate bytes versus the 472-byte target, while `ApplyTextureAlphaBleed` is 1,343 candidate bytes versus the 1,463-byte code-plus-table target contribution. The constructor is 1,284 versus 2,315 because the maintained semantic body does not reproduce the target's compiler-inlined member construction and callback-node allocation context. The alpha-bleed normalization is intentionally incomplete at the raw selector-table suffix; no exactness is inferred and no new match unit is created. All previous 47 canonical `AnmManager.cpp` units remain independently replayable. | `.analysis/gpt-5.6-sol/20260915-anm-manager-setup/ltcg-execute-context.json`; PDB contribution extents; reviewed target physical extents; canonical exact replay |
| ANM-020 | observed / source-present | The manager's texture/resource pipeline at `0x00446C70-0x004479CF` loads complete or rectangular textures, creates textures from files and serialized ANM payloads, parses synchronous/preloaded ANM resources, relocates entry-owned script/sprite offsets, uploads embedded/external texture data, services queued loads, releases resource ownership, and materializes sprites. The manager owns 33 `0x130`-byte `AnmLoadedView` records at `+0x3AD06C`; each view retains its file buffer, first entry, 32 script pointers and 32 sprite pointers. Raw entry, sprite and texture-header layouts are bound by compile-time offsets derived from direct target reads. `ServicePreloadedAnms` preserves the target-observed apparent null write after a successful postload instead of silently correcting it. | Hash-attested TH10 disassembly, direct manager/entry offsets and resource call graph; D3DX/D3D9 imported ABIs; maintained layout assertions; adjacent source consulted only after TH10-local structure recovery |
| ANM-021 | observed / source-present | The ANM text family at `0x004479D0-0x00447C71` formats into guarded 512-byte stack buffers, selects a texture and sprite through the VM resource identity, rasterizes within the sprite rectangle, and publishes VM visibility. VM bytes `+0x3A0/+0x3A1` are glyph width/height; flag bit one at `+0x360` selects the alternate rasterizer. Right alignment subtracts half of `strlen(text) * (glyphWidth - 1)` from sprite width, while centered alignment starts at half sprite width minus one quarter of that span. The surface/capture family at `0x00447C80-0x004485CA` loads and preloads offscreen surfaces, releases paired surface/data ownership, copies either primary or backup surfaces to the backbuffer, captures backbuffer rectangles into textures or paired surfaces, and copies texture subrectangles. Manager arrays at `+0x3AD4E0/+0x3AD560/+0x3AD5E0/+0x3AD660` hold 32 primary surfaces, backup surfaces, data buffers and sizes; 32 image-info records of stride `0x1C` begin at `+0x3AD6E0`. | Direct TH10 instruction/branch and field-offset review; target D3D9 vtable calls and D3DX imports; maintained exact-sized view types; TH08/TH095 used only for later source-shape corroboration |
| TOOLCHAIN-068 | observed / profile-fixed | `scripts/probe-ltcg-backlog.py` now accepts repeatable `--profile-flag` values and appends them consistently to both normal-inventory and linked `/GL` compiler profiles while recording them in JSON output. This exposed the corridor's missing `/GS` dimension: without `/GS`, the guarded text formatters had irreconcilable stack frames; with `/GS`, eight complete contributions became structurally exact. This is a diagnostic-profile correction and does not loosen canonical acceptance. | Pinned VC7.1 normal/LTCG compiler diagnostics with and without `/GS`; `.analysis/gpt-5.6-sol/20260915-anm-resource/ltcg-corridor-gs-text-texture.json`; script help and Python compilation |
| TOOLCHAIN-069 | observed / exact-thirteen-units | Five resource units reproduce `CreateTextureFromFile` (132 bytes), `CreateEmptyTexture` (41), `LoadAnm` (50), `PostloadAnmEntry` (174), and `LoadSprite` (143), for 540 exact bytes. A separate `/GS` artifact reproduces the three ASCII formatters at `0x00401630/0x00401690/0x00401700`, four ANM text functions at `0x004479D0-0x00447C71`, and `ReleaseSurface` (87), for 1,032 exact bytes. Each group passes two independent cold canonical replays with complete PDB extents and exhaustive declared link fields. A full current-source cold replay passes all 60 `AnmManager.cpp` units / 10,175 bytes across ten artifacts. Repository totals become 110 exact functions / 12,166 bytes from 1,309 candidates and 247 source mappings. | `.analysis/gpt-5.6-sol/20260915-anm-resource/exact-resource-pass{1,2}.json`, `exact-text-surface-pass{1,2}.json`, and `exact-anm-full-pass{1,2}.json`; canonical linked-PE match units and exhaustive linkage manifests |
| ANM-022 | observed / coverage-accounted | The tracked ANM setup corridor from `0x00445900` through the physical end of `CaptureToSurface @ 0x004485CA` contains 31 candidate functions / 10,628 bytes, all source-present. Across the complete `AnmManager` module, 123 of 125 tracked candidates are source-present (98.4% by function count and 41,695/41,712 bytes); the only unmapped candidates are two 10-byte and 7-byte leaf bodies at `0x004452E0` and `0x004458A0`. This metric measures semantic source presence, independently from exact code generation. The small `0x004485D0-0x00448850` wrapper/id-propagation family remains outside this core batch and is reserved for the Web leaf queue. | Numeric ledger audit of `config/functions.csv`; reviewed corridor boundaries and maintained source mappings |
| ENEMY-012 | observed / source-present | `EnemyRuntimeView::DispatchEclInstruction @ 0x0040E770` now has a complete maintained switch covering all 124 target-active opcode values across `0x100-0x1B4`. The families implement enemy creation and ANM binding, offset/base motion and interpolation, hitboxes and lifecycle flags, item drops, callbacks, dialog/spells, rank/difficulty selection, bullet-pattern state and four laser request forms. The target's physical owner remains `0x0040E770-0x00411FBF` = 14,416 bytes: code ends at `0x00411D2E`, followed by alignment, a 108-entry destination table, the 181-byte selector table, alignment and two five-entry nested difficulty tables. The 108 table entries are destination slots rather than the number of semantic opcode cases. Typed 0x40-byte spawn storage and 0x1F8 union-backed laser storage replace Ghidra stack-overlap/array-overrun expressions while preserving target-observed field positions and packed shorts. Opcode/helper identifiers are descriptive where original symbols remain unknown, and the six private argument helper bodies remain source-absent leaf seams. | Hash-attested target selector/destination tables and instruction bodies; Factory-attested Ghidra decompilation/callees/xrefs; direct target disassembly; maintained typed runtime/request layouts; adjacent ECL maps used only after target recovery to suggest descriptive family names |
| TOOLCHAIN-070 | observed / complete-source-nonexact | `scripts/report-ecl-dispatch-table.py --check` verifies the canonical target, decodes all 181 selectors through the 108-entry destination table, and joins the resulting active opcode values to the maintained enum and switch. The target has 124 active and 57 default-routed opcodes; source has 124 unique enum entries and 124 unique case labels with no missing or extra values. Pinned VC7.1 SP1 build6030 compiles the complete dispatcher under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc` and the same profile plus `/GL`. Adding the six runtime helper declarations shifted the two existing resolver units' compiler-local `$L...` labels by exactly +14; the first cold replay failed closed on those names while preserving all ten relocation offsets/types, and two subsequent cold builds replayed all 20 `Enemy.cpp` canonical units / 511 bytes exactly after the manifest-name refresh. These checks establish complete semantic source coverage and compiler viability only. No normal-COFF or linked-image extent is accepted for the original LTCG-sized owner, no `matches.csv` row is added, and its exactness remains unknown. Repository totals become 248 source mappings and remain 110 exact functions / 12,166 bytes. | Target-bound ECL table report; maintained `src/EnemyEclDispatcher.cpp`; pinned normal and `/GL` compile probes; `.analysis/gpt-5.6-sol/20260915-ecl-dispatcher/enemy-exact-replay-{1,2,3}.json`; unchanged canonical exact ledger |
| ENEMY-013 | observed / source-present | `EnemyResolveRuntimeCallback @ 0x004127A0` is the ECX-bound 593-byte core called at both Enemy runtime callback checkpoints. It scans eight `0x10`-byte records at full Enemy `+0x2494`; each record holds a life threshold, timer threshold, life callback name and timer callback name. The life path stores `life-threshold` at full `+0x2404`, clamps life at threshold, disables the record, resets the update timer, clears runtime flag `0x10000` and returns the life callback name. The timer path publishes a seconds countdown capped at 99 to visual owner `+0x9EC0`; at expiry it clamps life, disables the record, resets the timer, sets flag `0x10000`, reduces global `0x00474C4C` by 3000 with a floor of 5000, and conditionally clears bit one in Player words `+0x378C/+0x0AD4/+0x0E80/+0x15D8/+0x1984/+0x1D30/+0x20DC/+0x2488` before returning the timer callback name. The timer reset writes `current=0`, `previous=-999999`, `subframe=0`, scale `0x00476F78` and flag bit zero during initialization, then always ends with `current=0`, `subframe=0`, `previous=-1`. | Hash-attested target disassembly; Factory-attested decompilation, two callers and full target extent; maintained exact-offset views; TH08 consulted only after TH10-local recovery for callback-family corroboration |
| TOOLCHAIN-071 | observed / size-close-normal-nonexact | The callback core is isolated in `src/EnemyCallback.cpp` because appending its control-flow-heavy source to `Enemy.cpp` renumbered VC7.1 compiler-local switch labels used by otherwise unchanged exact units. The isolated source compiles under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc`, `/GL`, and `/W4`; its fastcall normal COMDAT is 598 bytes versus the 593-byte target and is a non-exact 48/565 comparable-byte diagnostic with seven relocations and `acceptance_authority=none`. A cold canonical replay after isolation still reproduces all 20 existing `Enemy.cpp` units / 511 bytes exactly with their original label bindings. No match row is added. Repository totals become 249 source mappings and remain 110 exact functions / 12,166 bytes. | `.analysis/gpt-5.6-sol/20260915-enemy-callback-core/`; pinned compiler normal/LTCG/warning probes; target-bound diagnostic comparator; cold canonical replay |
| PLAYER-020 | observed / source-boundary-corrected | `PlayerUpdateCallbackBody @ 0x00425730-0x00426340` and the registered update entry at `0x00426500-0x00426506` are distinct target boundaries. The entry receives Player in ECX and is exactly `push ecx; call body; ret`; the body loads the sole Player pointer from its stack frame and ends in `ret 4`. The maintained five-state update implementation now belongs to `__stdcall PlayerUpdateCallbackBody`, while a separate `__fastcall PlayerUpdateCallback` models the registered entry. This adds source presence to the 3,089-byte authored core rather than crediting its semantics only to a seven-byte adapter. Ghidra's provisional two-parameter `__thiscall` signature is contradicted by the raw boundary, sole incoming call and stack cleanup. | Fresh hash-attested target entry/tail/table/adapter disassembly; Factory-attested decompile, calls and xrefs; maintained full Player update source and layout |
| TOOLCHAIN-072 | observed / adapter-structural-exact | Pinned VC7.1 SP1 build6030 compiles the split Player body and adapter under fixed normal, `/GL`, and `/W4` profiles. The normal `PlayerUpdateCallbackBody` COMDAT remains 2,800 bytes including its compiler-owned five-entry table versus the target's 3,089-byte physical body and is non-exact at 139/2,693 comparable bytes with 99 relocations. The isolated adapter is a seven-byte normal COMDAT whose three ordinary bytes and sole REL32 field reproduce target `0x00426500`; the relocation resolves to `PlayerUpdateCallbackBody @ 0x00425730`. Because target evidence does not distinguish a source-written wrapper from an LTCG-generated public entry, its origin remains `unknown/review`, the structural-exact probe has `acceptance_authority=none`, and no canonical unit or match row is added. Repository totals become 250 source mappings and remain 110 exact functions / 12,166 bytes. | `.analysis/gpt-5.6-sol/20260915-player-update-core/`; pinned compiler probes; complete target body/adapter disassembly; target-bound COFF diagnostics |
| ECLVM-001 | observed / corroborated | `EclVmContext::Run @ 0x0044E1A0` is the generic TH10 typed-stack ECL interpreter beneath the Enemy-specific `0x0040E770` high-opcode dispatcher. The target checks `float(instruction.time) <= currentTime`, gates on the context/instruction difficulty masks, dispatches 59 active opcodes in `0x00..0x57`, and forwards the remaining 29 selector values plus every opcode outside that range through host vtable slot zero. Its `0x1024`-byte context has time/instruction at `+0/+4`, a `0x1000`-byte stack at `+8`, stack top/frame base at `+0x1008/+0x100C`, thread id/host at `+0x1010/+0x1014`, thread control/difficulty at `+0x1018/+0x101C`, and flags at `+0x1020`. The maintained core covers call/return/jump, threads, frames, typed operand/local/host resolution, arithmetic/comparisons, logical/bitwise operations and trigonometric/vector instructions. Raw opcode `0x55` executes integer `NEG` on the float storage dword before pushing type `f`; source preserves that observed behavior. The reviewed physical owner is `0x0044E1A0-0x0044FD0B` = 7,020 bytes, including two alignment bytes, the 60-entry destination table and 88-byte selector table after code ends at `0x0044FBC1`. | Canonical target disassembly and bytes; Factory-native Ghidra decompile/calls/xrefs; `scripts/report-ecl-vm-table.py`; `.analysis/gpt-5.6-sol/20260915-ecl-vm-core/` |
| TOOLCHAIN-073 | observed / normal-and-ltcg-nonexact | Pinned VC7.1 SP1 build6030 compiles `src/EclVm.cpp` under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc`, the same profile plus `/GL`, and `/W4`. The natural `EclVmContext::Run` member is a 2,760-byte normal COMDAT; its `/GL` diagnostic PDB contribution is also 2,760 bytes, versus the target's 7,020-byte source-owned extent. Both target-bound diagnostics are mismatches and carry `acceptance_authority=none`. The target entry consumes context in private EAX and only the float delta on stack with `RET 4`, while the maintained member declaration expresses the logical source ABI. No match row or exactness promotion is made. Repository totals become 251 source mappings and remain 110 exact functions / 12,166 bytes. | Pinned compiler normal, `/GL` and `/W4` probes; normal COFF and LTCG linked diagnostics below `build/ecl-vm-core/`; raw target entry/callers/tail |
| ECLVM-002 | observed / nested-stack-owner | `EclVmContext + 0x08` is a distinct `0x1008`-byte `EclVmStackView`, containing `data[0x1000]`, `stackTop` at stack `+0x1000` and `frameBase` at stack `+0x1004`; the enclosing context remains exactly `0x1024` bytes with those fields at context `+0x1008/+0x100C`. `Push @ 0x004505B0` optionally emits a four-byte typed-slot tag and copies an arbitrary-sized value. `EnterFrame @ 0x00450690` reserves locals, saves the prior frame base and publishes the prior top. `LeaveFrame @ 0x004506D0` restores the saved base and resets the top to the frame captured on entry. Their reviewed target ABIs use the nested stack receiver in private EAX. | Hash-attested target code and callers; Factory-native Ghidra helper decompilation; maintained exact-size layout assertions and natural source |
| TOOLCHAIN-077 | observed / exact-two-units | `EclVmStackView::Push` and `EclVmStackView::EnterFrame` reproduce their complete relocation-free linked `/GL` PDB contributions at 96 and 64 bytes. Two independent focused cold replays plus the full `src/EclVm.cpp` gate close 2 units / 1 artifact / 160 bytes. Natural `LeaveFrame` is the same 41-byte size and matches 39 bytes; its sole difference is the semantically equivalent `SUB ECX,4` versus target `ADD ECX,-4`, so it remains a near match. After the nested-owner refactor, the selected `Run` contribution is still about 2,756 bytes versus the target's 7,020-byte physical owner, confirming that the central gap remains source/helper architecture rather than only field layout. Repository totals become 285 source mappings and 128 exact functions / 17,890 bytes. | `.analysis/gpt-5.6-sol/20260916-ecl-vm-stack/`; canonical linked-PE units; pinned VC7.1 SP1 build6030; unchanged target owner evidence |
| ECLVM-003 | observed / recovered-missed-owner | Direct target review recovers the Ghidra-missed `EclVmStackView::Pop @ 0x00450610-0x00450689` as a 122-byte owner ending in `RET 4`, followed by six `CC` bytes before `EnterFrame`. Its private ABI uses EAX for the nested stack receiver, ECX for byte count, BL for requested scalar type and one stack output pointer. It copies the value, optionally consumes a four-byte type slot and converts stored `i`/`f` values when the requested type differs. | Complete hash-attested target disassembly and neighboring boundaries; exact linked contribution; maintained natural source |
| TOOLCHAIN-078 | observed / exact-typed-pop | Natural `EclVmStackView::Pop` source reproduces its complete 122-byte linked `/GL` PDB contribution and sole `__ftol2` REL32 field in two independent cold replays. The full `src/EclVm.cpp` gate closes Push, Pop and EnterFrame as 3 units / 1 artifact / 282 bytes. Replacing the provisional local pop implementation raises the diagnostic `Run` contribution only from about 2,756 to 2,776 bytes against the 7,020-byte target, so the central mismatch remains architectural. Repository totals become 1,313 candidates, 286 mappings and 129 exact functions / 18,012 bytes. | `.analysis/gpt-5.6-sol/20260916-ecl-vm-pop/`; pinned VC7.1 SP1 build6030; target-bound linked diagnostic with `acceptance_authority=none` |
| ECLVM-004 | observed / source-present-and-exact-one | The reviewed operand corridor `0x0044FDB0-0x004500CC` contains six distinct `EclVmContext` helpers for immediate operands, frame locals, typed-stack pops, and host-owned integer/float variables. `ResolveFloat @ 0x00450070` is canonical exact across its complete 93-byte linked `/GL` PDB contribution and all three fields. `ReadFloat`, `ReadIntValue`, and `ReadFloatValue` reach their exact target sizes but each retains a two-byte `SUB register,4` versus target-equivalent `ADD register,-4` encoding difference. `ReadInt` and `ResolveInt` remain sensitive to target-private receiver registers supplied by the production whole-program call graph. | Complete target corridor disassembly and boundaries; hash-attested local Ghidra decompilation; maintained member source; focused and full-source replay reports below `.analysis/gpt-5.6-sol/20260916-ecl-vm-read-resolve/` |
| TOOLCHAIN-086 | observed / exact-one-and-near-three | Two independent cold replays reproduce `ResolveFloat` at 93/93 bytes with its zero-constant DIR32 and two `__ftol2` REL32 fields; the full `src/EclVm.cpp` gate closes 4 units / 1 artifact / 375 bytes. The final selected-entry `/GL` diagnostic retains `ReadFloat` at 183/183 with 165/167 comparable bytes, `ReadIntValue` at 124/124 with 118/120, `ReadFloatValue` at 171/171 with 153/155, `ReadInt` at 120/144, `ResolveInt` at 59/58, `LeaveFrame` at 41/41 with 39/41, and `Run` at 2,772/7,020. Every noncanonical result has `acceptance_authority=none`. Repository totals become 1,313 candidates, 292 mappings and 130 exact functions / 18,105 bytes. | `.analysis/gpt-5.6-sol/20260916-ecl-vm-read-resolve/canonical-replay-{1,2}.json`; `full-source-replay-final.json`; `final-ltcg-diagnostic.json`; pinned VC7.1 SP1 build6030 |
| ECLVM-005 | observed / host-thread-and-database-owners | The generic ECL host has a target-proven `0x103C` prefix: active context `+0x4`, embedded `0x1024` context `+0x8`, script database `+0x102C`, and an embedded three-pointer thread-list sentinel at `+0x1030`. `FindThread` searches that sentinel and its secondary nodes and returns the containing node; target `Run` callers then dereference the context. `StopAllThreads` begins at the sentinel's next pointer and clears only secondary context instructions. `SpawnThread` allocates a new context and node, inherits the active difficulty mask, links it at the head and starts the requested subroutine. `StartSubroutine` copies and converts call arguments, records return time/instruction state, changes the active context and resolves the named instruction through the script database. | Canonical target disassembly and callers; hash-attested local Ghidra decompilation; complete offset assertions; maintained source in `src/EclVm.cpp` |
| TOOLCHAIN-087 | observed / exact-three-control-units | Natural linked `/GL` source reproduces `EclVmHost::FindThread` at 36/36 bytes, `EclVmHost::StopAllThreads` at 31/31 and `EclVmScriptDatabase::FindSubroutine` at 136/136, all relocation-free. Two independent cold replays close 3 units / 1 artifact / 203 bytes, and the full `src/EclVm.cpp` gate closes 7 units / 1 artifact / 578 bytes. `SpawnThread` remains a 142/142-byte near match with 120/130 comparable bytes equal; `StartSubroutine` is 487/550; `Run` is 2,756/7,020. Noncanonical diagnostics retain `acceptance_authority=none`. Repository totals become 1,313 candidates, 297 mappings and 133 exact functions / 18,308 bytes. | `.analysis/gpt-5.6-sol/20260916-ecl-vm-control/control-exact-replay-{1,2}.json`; `full-source-replay-final.json`; `final-ltcg-diagnostic.json`; pinned VC7.1 SP1 build6030 |
| ECLVM-006 | observed / source-present-script-database | `EclVmScriptDatabase::AddScriptData @ 0x00450220` validates SCPT magic/version, retains up to 32 file buffers, locates a subroutine-offset array after the 0x24-byte header and variable include block, and merges packed names plus relative subroutine headers into an eight-byte-entry lexically sorted table. The first file initializes the table; later files copy and free the previous allocation, insert each new entry before the first greater-or-equal name, and shift later entries. Nonzero include length dispatches virtual slot one on file `+0x24`. The database is the same target-proven polymorphic 0x1098-byte resource owner whose derived Enemy vtable binds slot zero here, slot one to the ANIM/ECLI package loader and slot two to the filename loader. | Complete target owner disassembly and sole direct caller; hash-attested Ghidra decompilation; target vtables `0x0046D0B4`/`0x0046D0F0`; maintained layout assertions and natural source |
| TOOLCHAIN-088 | observed / normal-and-ltcg-nonexact | Pinned VC7.1 `/O2` normal and linked `/GL` builds emit a complete 547-byte `EclVmScriptDatabase::AddScriptData` contribution against the 589-byte target owner. The candidate has exactly two linked fields, `malloc` and `free`, and remains a real mismatch with `acceptance_authority=none`. `/Ot` is identical; `/O1` and `/Os` emit 522 bytes and are worse, so the default speed-optimized profile is retained. The existing seven exact `src/EclVm.cpp` units still replay all 578 target bytes after the database layout and method were added. Repository totals become 1,313 candidates, 298 mappings and 133 exact functions / 18,308 bytes. | `.analysis/gpt-5.6-sol/20260916-ecl-script-loader/final-ltcg-diagnostic.json`; `profile-{O1,Os,Ot}.json`; `full-source-replay-final.json`; pinned VC7.1 SP1 build6030 |
| ECLVM-007 | observed / exact-host-runner | `EclVmHost::Run @ 0x0044FD10-0x0044FDA1` walks the sentinel at host `+0x1030` and every spawned-thread node, publishes each context at host `+0x4`, and calls the generic context runner with one float delta. Failure of the embedded context returns `-1`; failure of a spawned context deletes that context, repairs both neighboring links, clears the removed node links and deletes the node. Normal completion restores host `+0x4` to the embedded context at `+0x8`. The only target callers are the two maintained `EnemyRuntimeUpdate` sites at `0x0040E0FE/0x0040E205`, which keep the host in EDI. | Complete target disassembly and boundary; Factory-native Ghidra decompile/callers/callees/xrefs under passed target attestation; maintained source and exact layout assertions |
| TOOLCHAIN-089 | observed / exact-cross-tu-context | A canonical linked unit uses `src/EclVm.cpp` as the uniquely owned source contribution, `src/Enemy.cpp` as support, and maintained `EnemyRuntimeUpdate` as the real link entry. VC7.1 then naturally emits the target's private EDI host ABI and a complete 146-byte `EclVmHost::Run` contribution. Two cold links with different PE hashes replay all bytes and four REL32 fields exactly: two context-`Run` calls and two operator-delete calls. The full ECL gate closes 8 units / 2 artifacts / 724 bytes and the unchanged Enemy gate closes 20 units / 2 artifacts / 511 bytes. Target-observed instruction reloads, signed iteration and active-context writes improve noncanonical `EclVmStartSubroutine` to 518/550 bytes and 73/534 comparable bytes; it remains non-exact. Repository totals become 1,313 candidates, 299 mappings and 134 exact functions / 18,454 bytes. | `.analysis/gpt-5.6-sol/20260916-ecl-start-subroutine/host-run-canonical-{1,2}.json`; `ecl-full-exact-replay-final.json`; `enemy-full-exact-replay-final.json`; pinned VC7.1 SP1 build6030 |
| ECLVM-008 | observed / source-shape-refined | Target `EclVmContext::Run` expands typed `Pop` paths in each opcode body, keeps typed pushes as calls to the independent exact `EclVmStackView::Push` owner, and embeds formatted-operand evaluation inside the 7,020-byte physical owner. The maintained source now expresses that asymmetric VC7.1 shape, uses the target's one-byte format flag index and unsigned spawn-thread operand-index shift, and removes a target-absent raw-return-pop initializer. The target entry continues to consume context in EAX and one float delta on the stack with `RET 4`. Exactness remains unknown. | Hash-attested target case disassembly; exact stack-owner boundaries; selected real-host linked diagnostic; maintained source |
| TOOLCHAIN-090 | observed / dispatcher-structural-progress | With `EclVmHost::Run` selected as the real caller, pinned VC7.1 `/GL` initially emits a 2,756-byte `EclVmContext::Run` contribution against the 7,020-byte target. Forced inline typed-pop and format helpers plus a scoped `inline_depth(1)` around the typed-push wrappers reproduce the target's dominant expansion/call pattern while preserving the EAX context ABI: the contribution becomes 6,748 bytes, 402 comparable bytes match, and the remaining extent gap is 272 bytes. The candidate remains a mismatch with `acceptance_authority=none`; no exact row is added. `report-ecl-vm-table.py` can now locate the candidate's unique selector/table pair and report per-opcode relative destinations and following-destination gaps. A cold canonical replay proves the already accepted 146-byte host runner and all four fields remain exact after the dispatcher changes. | `.analysis/gpt-5.6-sol/20260916-ecl-run-exact/inline-depth-push.json`; `case-layout.json`; `host-cold-replay-inline-depth-push.json`; pinned VC7.1 SP1 build6030 |
| ECLVM-009 | observed / control-flow-refined | The target physically groups generic VM cases by execution family rather than opcode number: integer arithmetic precedes float arithmetic, integer comparisons precede float comparisons, and the thread, jump, vector and formatted-operand groups each have target-specific order. The maintained switch now emits the same 59-case physical order. RETURN pops the restored instruction and time directly into their context fields. Conditional true/false jumps share the single JUMP body, and jump offsets plus ordinary instruction sizes join at one instruction-writeback tail. These changes reproduce target control flow rather than duplicating equivalent case bodies. | Target and candidate jump-table destination order; target case/tail disassembly; maintained natural switch and shared labels; `report-ecl-vm-table.py` candidate comparison |
| TOOLCHAIN-091 | observed / frame-and-order-closure | In the exact host caller context, the control-flow-refined `/GL` candidate has a 6,648-byte PDB contribution versus the 7,020-byte physical owner and a 6,320-byte pre-table span versus target 6,692. Its `0x108` local frame, saved-register set, EAX receiver ABI and complete physical case order now match the target; 431 of 6,316 comparable bytes match. RETURN is 81 versus 83 bytes, the two conditional jump blocks are each two bytes short, and many thread/frame/vector cases are within zero or one byte. The remaining 372-byte contribution gap is dominated by typed arithmetic conversion branches and an EBX/EBP live-range exchange. The diagnostic remains non-exact with `acceptance_authority=none`. `report-ecl-vm-table.py` now reports the target's 6,690-byte executable-code size, pre-table span delta and whether the complete candidate physical case order matches the target. The full ECL exact gate still closes 8 units / 2 artifacts / 724 bytes. | `.analysis/gpt-5.6-sol/20260916-ecl-run-exact/final-control-flow.json`; `final-control-flow-layout.json`; `ecl-full-replay-control-flow-final.json`; pinned VC7.1 SP1 build6030 |
| ANM-032 | observed / control-flow-corrected | The `ExecuteScript` selector is a direct 94-entry table indexed by signed opcode plus one. Its 94 opcodes `-1..92` resolve to 92 unique destinations because END/DELETE share one body and NOP/INTERRUPT_LABEL share the ordinary instruction-advance body. The physical order begins UNIT_SPEED, child opcodes 88/91/90/92, sprite and basic transforms, then proceeds through control, interpolation, arithmetic, RNG and conditional-jump families before END/DELETE/STATIC. The four child cases call target owners Variant0, Variant2, Variant1 and Variant3 respectively; the earlier maintained 90/91 association was reversed. STOP_HIDE falls through the common STOP body. The executor itself does not invoke VM `positionCallback`; the two manager update owners invoke it before entering the executor, and the target executor proceeds from generated mode-9 geometry directly to the script-timer update. | Hash-attested target dispatch table and complete code disassembly; direct child call targets; manager-update call sites; `scripts/report-anm-execute-table.py` |
| TOOLCHAIN-092 | observed / dispatcher-structural-progress | `report-anm-execute-table.py` now verifies the hash-pinned target's 9,587-byte executable body, one-byte alignment, 94-entry absolute table, 92 unique destinations, complete `-1..92` enum/case coverage and optional linked-candidate layout. Reordering the maintained switch from numeric opcode order to the target-observed physical order, splitting the four child bodies, preserving the direct `GetVm` calls and sharing the instruction-advance label produces a 9,488-byte candidate pre-table span versus target 9,588 including alignment, a `-100` span delta. UNIT_SPEED is `+1`, child entries 88/91/90 are `+1`, child 92 is `+4`, sprite is `+8`, and END/STATIC are `-1` relative to target. All 92 destination groups follow target order except the shared NOP/INTERRUPT advance group, which VC7.1 still places between conditional cases 28 and 29 instead of after POSITION. The linked diagnostic remains non-exact at 379/8,516 comparable bytes and has no acceptance authority. A full cold source replay still closes all 76 canonical `AnmManager.cpp` units across 10 artifacts and 15,739 bytes. | `.analysis/gpt-5.6-sol/20260916-anm-executor-exact/final/{probe,table,replay-anm}.json`; pinned VC7.1 SP1 build6030; complete target/candidate tables and PDB contribution |
| GUI-001 | observed / source-present | `GuiView::UpdateStageElements @ 0x00414900` is the central TH10 stage-HUD update owner and `GuiMessageVmView::Run @ 0x00415E90` is its complete stage-message interpreter. The exact `0x9ED0` GUI view includes the six constructor-proved embedded VM arrays, the separate boss-position VM, managed ids, boss gauge state, four interleaved gauge values, message pointer, stage/front ANM resources, spell-second state and ending counter. The exact `0x90` message view contains three timers, six VM ids, current instruction, two text positions, input cooldown, flags, alternating-line/side state and two colors. Its variable-sized instruction header is `u16 time`, `u8 opcode`, `u8 payloadSize`, then payload. The maintained interpreter covers all opcodes `0..23`, including wait/skip behavior, portrait/text control, target stage-specific sprite and enemy-name maps, stage events and completion. The updater preserves target side-panel and boss-gauge position thresholds, `0.025f` gauge interpolation, four-phase boss thresholds and the distance-based boss marker alpha. Both entries consume one pointer on the stack and return with `RET 4`. | Hash-attested target disassembly and table bytes; Factory-native Ghidra decompile/calls/xrefs; constructors `0x00413810/0x00415B00`; maintained exact-offset layouts and source; `scripts/report-gui-core.py` |
| GUI-002 | observed / physical-table-ownership | The reviewed physical owner for `0x00414900` is `0x00414900-0x004157FB` = 3,836 bytes: code ends at `0x004157DC`, three alignment bytes follow, and the seven-entry boss-gauge stage selector occupies `0x004157E0-0x004157FB`. The reviewed physical owner for `0x00415E90` is `0x00415E90-0x0041700F` = 4,480 bytes: code ends at `0x00416F61`, two alignment bytes follow, and 43 table pointers occupy `0x00416F64-0x0041700F`. The latter split exactly into a 24-entry opcode table, seven enemy-portrait stage cases, seven enemy-name stage cases and five difficulty bonus cases. Every selector points inside its owning code body and every partition has distinct destinations. | Direct canonical target bytes and branch operands; adjacent independent function boundaries; target-bound `scripts/report-gui-core.py --check` |
| TOOLCHAIN-074 | observed / normal-and-ltcg-nonexact | Pinned VC7.1 SP1 build6030 compiles `src/Gui.cpp` under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc`, the same profile plus `/GL`, and `/W4`; the only `/W4` diagnostics are pre-existing anonymous-union extension warnings in `AnmManager.hpp`. Normal `/Gy` COMDATs are 690 bytes for `GuiView::UpdateStageElements` and 1,348 for `GuiMessageVmView::Run`; linked `/GL` PDB contributions are 689 and 1,560 bytes. Both are real target-bound mismatches against the 3,836- and 4,480-byte source-owned target contributions, with `acceptance_authority=none`. This is consistent with substantial target inlining but does not establish the original production LTCG context or exactness. No match row is added. Repository totals become 253 source mappings and remain 110 exact functions / 12,166 bytes. | Fixed normal and linked `/GL` probe reports below `build/gui/`; pinned `/W4` compiler output; canonical target extents; unchanged exact ledger |
| MAIN-022 | observed / source-present | Ten central Main owners now have maintained target-bound source across 5,456 reviewed bytes: `WinMain`, window procedure, present/reset/screenshot, frame rendering, timestamp, window creation, D3D9 initialization, render-state reset, single-instance/startup-path handling and executable checksum. `GameWindowView` is exactly `0x58` bytes; `MainSupervisorView` assertions bind D3D objects, matrices, viewport/presentation data, serialized configuration, gameplay/active viewports, transition/render flags, music owner, capabilities, seven critical sections, render-state cache and frame duration. `MainAllocationRegistryView` is exactly `0xA004`. The source preserves target machine boundaries separately from descriptive C++ ownership. | Hash-attested target extents, epilogues and direct field accesses; Factory-native Ghidra evidence; compile-time layouts; `scripts/report-main-corridor.py --check`; maintained `src/Main.cpp`/`src/Main.hpp` |
| TOOLCHAIN-075 | observed / structural-exact-candidate | Pinned VC7.1 SP1 build6030 compiles `src/Main.cpp` under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc /GS`, the same profile plus `/GL`, and `/W4`. Nine owners remain target-bound mismatches; `GetTimestamp` is 282/282 bytes with 159/162 comparable bytes, and the selected `/GL` `CreateGameWindow` contribution is 341/341 bytes but differs in 11 comparable bytes. `ResetRenderState` is a complete 710/710-byte structural-exact diagnostic in both normal and selected `/GL` lanes: all 574 comparable bytes match and all 34 relocations are accounted for. Both lanes have `acceptance_authority=none`; physical normal-COFF versus LTCG ownership and original TU identity remain unresolved, so no canonical unit or exact row is added. Repository totals become 263 source mappings and remain 110 exact functions / 12,166 bytes. | Strict normal diagnostic batch and selected-entry linked report below `build/main/`; pinned `/W4` compile; unchanged exact ledger |
| FRONTEND-007 | observed / source-present | The contiguous difficulty, character and shot-type selection controllers at `0x00430320`, `0x004306A0` and `0x00430A60` now have maintained source across 3,268 target bytes. All three use the shared five-state opening/active/confirmed/closing protocol. Difficulty selects one of four normal entries or the Extra root, manages the auxiliary ASCII VM and transitions to character selection. Character applies the six Extra unlock bytes at profile `+0x1D888..+0x1D88D` and sends four complementary child interrupts on confirmation. Shot type applies three per-character unlock bytes, hides three child rows whose profile record dwords are zero, and commits the selected character/shot/difficulty path either to stage selection or normal-game startup. Controller VM ids are established through slot `0x97`, with the auxiliary difficulty VM id at `+0x5D0` and saved difficulty at `+0x58F0`; the existing key-config scratch remains at `+0x59CC`. Names for the three owners and the profile records are descriptive where original symbols remain unknown. | Hash-attested target disassembly, direct calls and profile/controller offsets; Factory-native Ghidra decompilation; maintained source/layout assertions; `scripts/report-frontend-selection.py --check` |
| FRONTEND-008 | observed / physical-table-ownership | Each selection owner directly indexes a five-entry absolute state table after its executable body. Difficulty is physically `0x00430320-0x00430697` = 888 bytes: code ends at `0x00430680`, a three-byte alignment instruction follows, and its table begins at `0x00430684`. Character is `0x004306A0-0x00430A5B` = 956 bytes with its table immediately after code ending at `0x00430A47`. Shot type is `0x00430A60-0x00430FEF` = 1,424 bytes: code ends at `0x00430FDA`, one NOP follows, and its table begins at `0x00430FDC`. These reviews add 64 bytes to tracked `.text` union coverage and move three indexed tables from outside to inside their owners. | Direct target branch operands, table entries, tails and neighboring entries; focused report; boundary inventory |
| TOOLCHAIN-076 | observed / normal-and-ltcg-nonexact | Pinned VC7.1 SP1 build6030 compiles the complete maintained `FrontEnd.cpp` after selection recovery under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc`, the same profile plus `/GL`, and `/W4`; the warnings are the existing anonymous-union extension diagnostics. Normal COMDAT sizes are 876 bytes for difficulty, 872 for character and 1,196 for shot type, versus target owners of 888, 956 and 1,424 bytes. Strict target-bound normal diagnostics are mismatches at 40/640, 49/740 and 49/1,120 matched comparable bytes, with 62, 54 and 76 object relocations and `acceptance_authority=none`. No exact row is added. Repository totals become 272 source mappings and remain 110 exact functions / 12,166 bytes. | `.analysis/gpt-5.6-sol/20260915-frontend-selection-core/`; pinned normal and `/GL` compiler; strict COFF diagnostics; unchanged exact ledger |
| FRONTEND-009 | observed / source-present | `FrontEndControllerView::UpdateStage @ 0x00430FF0` and `DrawStageScores @ 0x00431410` now have maintained source across 1,471 target bytes. The update owner implements the five-state six-stage controller, profile availability gate, numeric-key start-stage shortcut, ANM VM lifetime and final stage-record/game-mode transition. The display owner emits six profile-backed score rows, colors unavailable/current/other rows, blinks during confirmation and restores ASCII state. Descriptive identifiers do not claim original symbols or TU ownership. | Hash-attested target disassembly/calls/profile offsets; Factory-native Ghidra decompilation/xrefs; maintained source; `scripts/report-frontend-stage.py --check` |
| FRONTEND-010 | observed / physical-table-and-data-layout | The stage update owner is physically `0x00430FF0-0x0043140B` = 1,052 bytes: executable code ends at `0x004313F6`, one NOP follows, and the directly indexed five-entry state table occupies `0x004313F8-0x0043140B`. The display owner is `0x00431410-0x004315B2` = 419 bytes and is reached from the front-end draw callback at `0x0042D2A8` with a private EDI receiver. Stage profile records are eight bytes, with score at `+0` and availability at `+5`, indexed by stage plus difficulty times six within a `(shot + character*3) * 0x437C` group. The keyboard helper distinguishes DirectInput scan codes `2..10` from Win32 virtual-key codes `0x31..0x39` in one 256-byte buffer. | Direct target table/tail/caller/data accesses and string pointers; Factory-native decompilation/xrefs; focused target report |
| TOOLCHAIN-078 | observed / normal-and-ltcg-nonexact | Pinned VC7.1 SP1 build6030 compiles the expanded `src/FrontEnd.cpp` under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc`, the same profile plus `/GL`, and `/W4`; `/W4` adds only pre-existing anonymous-union extension warnings. Normal COMDATs are 660 bytes for stage update and 378 for stage display versus target owners of 1,052 and 419 bytes. Strict target-bound diagnostics are mismatches at 26/876 and 5/371 matched comparable bytes, with 44 and 12 relocations and `acceptance_authority=none`. No match row is added. Repository totals become 274 source mappings and remain 110 exact functions / 12,166 bytes. | `.analysis/gpt-5.6-sol/20260915-frontend-stage-core/`; pinned normal and `/GL` compiler; strict COFF diagnostics; unchanged exact ledger |
| FRONTEND-011 | observed / source-present | `FrontEndControllerView::UpdateReplay @ 0x004315C0` and `DrawReplay @ 0x00431BA0` now have maintained source across 2,317 target bytes. The six-state updater loads twenty-five numbered and up to twenty-five user replay files, derives a seven-entry stage cursor from replay stage headers, commits replay path/character/shot/difficulty and game mode 12, and releases all fifty replay owners on cancellation. The display emits twenty-five metadata rows and an animated seven-stage score view using the target character, difficulty, completion and stage-name tables. The maintained controller view binds replay fields at `+0x59D8/+0x59DC/+0x59E0` and the pointer array at `+0x59E4`. Descriptive identifiers do not claim original symbols or TU ownership. | Hash-attested target disassembly, direct calls, string/data tables and field accesses; Factory-native Ghidra decompilation; maintained source/layout assertions; `scripts/report-frontend-replay.py --check` |
| FRONTEND-012 | observed / physical-table-ownership | The replay update owner is physically `0x004315C0-0x00431B93` = 1,492 bytes: code ends in `RET 4` at `0x00431B79`, two-byte `MOV EDI,EDI` alignment follows, and the directly indexed six-entry table occupies `0x00431B7C-0x00431B93`. Its destination order is state 0 `0x004315F2`, state 1 `0x0043174D`, state 2 `0x004317B1`, state 3 `0x004319B4`, state 4 `0x004318FD`, and state 5 `0x00431A9E`. The display owner is `0x00431BA0-0x00431ED8` = 825 bytes and receives one stack controller argument from callback `0x0042D275`. Twelve and seven `CC` bytes delimit the two owners. This correction adds 26 tracked bytes and moves the table from the outside-owner queue into its source owner. | Direct canonical target branch/table/tail bytes and neighboring padding; update/draw callback call sites; focused report; boundary inventory |
| TOOLCHAIN-079 | observed / normal-and-ltcg-nonexact | Pinned VC7.1 SP1 build6030 compiles the replay-expanded `src/FrontEnd.cpp` under fixed normal `/TP /MT /O2 /Gy /GF /Oi /DNDEBUG /Isrc`, the same profile plus `/GL`, and `/W4`; only existing anonymous-union warnings remain. Normal `UpdateReplay`/`DrawReplay` COMDATs are 1,588/554 bytes versus target owners 1,492/825 and mismatch at 89/1,184 and 33/753 comparable bytes with 88/18 relocations. With `UpdateReplay` selected as the linked `/GL` entry, contributions are 1,672/554 and mismatch at 102/1,196 and 33/753 comparable bytes. All probes carry `acceptance_authority=none`; no match row is added. Repository totals become 276 source mappings and remain 110 exact functions / 12,166 bytes. | `.analysis/gpt-5.6-sol/20260915-frontend-replay-core/normal-probe.json`; selected-entry `ltcg-probe.json`; pinned `/W4` compile; unchanged exact ledger |
| FRONTEND-013 | observed / source-present | `FrontEndControllerView::UpdatePractice @ 0x00431EE0` and `RefreshPracticeRecords @ 0x00432690` now have maintained source across 2,818 target bytes. The four-state update owner maintains independent six-shot, five-difficulty and page cursors, creates and retires the screen ANM VMs, and cycles a ten-row record page. The refresh owner filters the 110-entry spell-card catalog through the target difficulty map, skips prior pages, pads unlocked names to 42 bytes, displays two profile counters and blanks unused rows. The controller view binds the second and third cursors at `+0x0FC/+0x1D4`, displayed-row count at `+0x2AC`, and ten ASCII VM ids at `+0x5D4..+0x5FB`. Descriptive identifiers do not claim original symbols or TU ownership. | Hash-attested target disassembly, calls, catalog/difficulty/profile offsets and format strings; Factory-native Ghidra decompilation; maintained source/layout assertions; `scripts/report-frontend-practice.py --check` |
| FRONTEND-014 | observed / physical-table-and-secret-input | The practice update owner is physically `0x00431EE0-0x0043268F` = 1,968 bytes: code ends in `RET 4` at `0x0043267A-0x0043267C`, three-byte `LEA ECX,[ECX]` alignment follows, and its four-entry state table occupies `0x00432680-0x0043268F`. The table destinations are `0x00431F06`, `0x00432095`, `0x004320BA`, and `0x00432615`. The target's Extra/third-shot branch copies the complete 256-byte keyboard state, derives rising-edge bytes, accepts a 22-entry scan-code sequence, resets on any other press among the first 57 codes, and expires after 300 frames; completion calls the profile-unlock helper and queues sound 44. The refresh helper ends at `0x004329E1` and is followed by fourteen `CC` bytes. | Direct canonical target table/tail bytes, keyboard tables/buffers and control flow; focused target report; boundary inventory |
| TOOLCHAIN-080 | observed / normal-and-ltcg-nonexact | Pinned VC7.1 SP1 build6030 compiles the practice-expanded `src/FrontEnd.cpp` under normal, `/GL`, and `/W4` profiles. Normal `UpdatePractice`/`RefreshPracticeRecords` COMDATs are 1,692/535 bytes versus target owners 1,968/850, with 70/1,592 and 28/786 matched comparable bytes and 94/16 relocations. In the selected practice-update `/GL` context the PDB contributions are 1,764/543 bytes and mismatch at 65/1,596 and 23/786 comparable bytes. The diagnostics carry `acceptance_authority=none`; no match row or exactness promotion is made. Repository totals become 278 source mappings and remain 110 exact functions / 12,166 bytes. | `.analysis/gpt-5.6-sol/20260915-frontend-practice-core/normal-probe.json`; selected-entry `ltcg-probe.json`; `/W4` linked diagnostic; unchanged exact ledger |
| FRONTEND-015 | observed / source-present | `FrontEndControllerView::Draw @ 0x0042D260` and `DrawPractice @ 0x004329F0` now have maintained source across 805 target bytes. The eight-entry draw table proves that screen discriminator 11 is Practice and 12 is Replay, correcting the reversed maintained enum: discriminator 11 calls `UpdatePractice`/`DrawPractice`, while 12 calls `UpdateReplay`/`DrawReplay`. The practice draw owner emits ten profile-backed score rows on page zero, fading each row's red and green channels by `0x10`; each 0x18-byte record supplies score, stage-name index, suffix digit, ten-byte name, timestamp and slowdown rate. It always emits the selected shot's aggregate value at `+0x4C8`, frame-derived `hours:minutes:seconds` at `+0x4CC`, and the selected difficulty's value at `+0x4D0 + difficulty*4`. `Draw` physically owns its table through `0x0042D2D3`; `DrawPractice` ends at `0x00432CA0` before fifteen `CC` bytes. | Canonical target update/draw dispatch tables, direct calls, profile field accesses, time arithmetic, formats and padding; maintained source; expanded `scripts/report-frontend-practice.py --check` |
| TOOLCHAIN-081 | observed / normal-and-ltcg-nonexact | Pinned VC7.1 SP1 build6030 compiles the draw-complete `src/FrontEnd.cpp` under normal, selected-entry `/GL`, and `/W4` profiles. Normal `Draw`/`DrawPractice` COMDATs are 116/585 bytes versus target owners 116/689 and mismatch at 10/60 and 51/625 comparable bytes with 14/16 relocations. With `DrawPractice` selected as the linked `/GL` entry, contributions are 120/585 bytes and mismatch at 15/64 and 54/625 comparable bytes with 14/16 linkage fields. Equal normal size for `Draw` is not exactness evidence; every diagnostic carries `acceptance_authority=none` and no match row is added. Repository totals become 280 source mappings and remain 110 exact functions / 12,166 bytes. | `.analysis/gpt-5.6-sol/20260915-frontend-practice-draw/normal-probe.json`; selected-entry `ltcg-probe.json`; `/W4` linked diagnostic; unchanged exact ledger |
| FRONTEND-016 | observed / source-present | `FrontEndControllerView::UpdateScoreEntry @ 0x00432CB0` and `DrawScoreEntry @ 0x00433230` now have maintained source across 2,226 target bytes. Screen discriminator 15 owns a four-state high-score insertion and name-entry flow: it selects the character/shot/difficulty score table, inserts one 0x18-byte record, restores the profile's last eight-character name, drives a wrapping 91-entry keyboard in thirteen columns, and commits the name to both the selected record and profile `+0x1D878`. The final three keyboard entries are target-rendered control glyphs for space, backspace and accept. The display draws ten profile score rows, overlays the editable name/caret on the inserted row, and draws the full keyboard. Descriptive identifiers do not claim original symbols or TU ownership. | Hash-attested target disassembly, calls, strings, record accesses and controller offsets; Factory-native Ghidra decompilation; maintained source; `scripts/report-frontend-score-entry.py --check` |
| FRONTEND-017 | observed / physical-table-and-layout | The update owner is physically `0x00432CB0-0x00433227` = 1,400 bytes: executable code ends in `RET 4` at `0x00433213-0x00433215`, two-byte `MOV EDI,EDI` alignment follows, and the four-entry state table occupies `0x00433218-0x00433227`. Its destinations are `0x00432CCC`, `0x00432EDA`, `0x00432F00`, and `0x004331AC`. The draw owner is `0x00433230-0x00433569` = 826 bytes and is followed by six `CC` bytes. Controller fields are name `+0x58DC`, name cursor `+0x58E8`, insertion-failure flag `+0x58EC`, and a complete `FrontEndCursorView` at `+0x58F4`; the target store at `+0x59C4` is that cursor's established `wraps` member. | Direct canonical target table/tail/caller bytes; target field accesses; focused target report; compile-time offset assertions |
| TOOLCHAIN-082 | observed / normal-and-ltcg-nonexact | Pinned VC7.1 SP1 build6030 compiles the score-entry-expanded `src/FrontEnd.cpp` under normal, selected-entry `/GL`, and `/W4` profiles; `/W4` adds only established anonymous-union extension warnings. Normal `UpdateScoreEntry`/`DrawScoreEntry` COMDATs are 1,384/781 bytes versus target owners 1,400/826 and mismatch at 72/1,096 and 22/718 comparable bytes. With `UpdateScoreEntry` selected as the linked `/GL` entry, contributions are 1,392/781 bytes and mismatch at 88/1,100 and 22/718 comparable bytes. All diagnostics carry `acceptance_authority=none`; no match row is added. Repository totals become 282 source mappings and remain 110 exact functions / 12,166 bytes. | `.analysis/gpt-5.6-sol/20260915-frontend-score-entry/`; normal COFF and selected-entry linked `/GL` reports below `build/frontend-score-entry/`; pinned `/W4` compile; unchanged exact ledger |
| INVENTORY-005 | observed / corrected | Eight reviewed owners were recorded one or two bytes short because their ledger spans ended on the `C2` opcode of `RET imm16` and omitted its two-byte immediate. Their corrected complete extents are `0x00425070-0x0042508A`, `0x004296D0-0x004296EA`, `0x00443680-0x004436B3`, `0x00444DC0-0x00444E50`, `0x00444E60-0x00444F9A`, `0x00444FA0-0x004450DA`, `0x00447C80-0x00447EBF`, and `0x00448360-0x00448442`. The boundary audit now classifies a one- or two-byte trailing `C2/CA` suffix as a truncated `RET imm16` and refuses to retain a reviewed state for that malformed span. Corrected tracking covers 340,424 target `.text` bytes; boundary-state counts remain 1,173 reviewed, 88 provisional and 51 needs-review. | Canonical target tail bytes; pinned Capstone dense decode; `scripts/report-boundary-inventory.py --check-ledger`; corrected function/boundary ledgers |
| ANM-023 | observed / exact-five-units | Correcting complete terminal-instruction ownership closes five ANM exact owners. `TranslateRotation @ 0x00443680` is 52 bytes including `RET 24`; `QueueSpriteQuad @ 0x00444DC0` is 145 bytes including `RET 4`; the untextured triangle-strip/fan siblings at `0x00444E60/0x00444FA0` are each 315 bytes including `RET 8`. Their pinned VC7.1 `/GL` PDB contributions reproduce all 827 bytes and 37 declared linked fields exactly in the established renderer context. Independently, normal COFF reproduces `MarkVmForDeletion @ 0x004492A0` across all 66 bytes and its sole `FindVm` REL32 relocation. Two cold aggregate replays reproduce all five owners, 893/893 bytes. | Canonical linked-PE and COFF units; two cold `scripts/replay-exact-units.py` passes; target-bound corrected extents and relocation/linkage manifests |
| TOOLCHAIN-083 | observed / near-exact-retained | Fresh dual-lane ranking after the score-entry checkpoint exposed the malformed `RET imm16` boundaries by placing four ANM candidates exactly two bytes over their recorded target sizes while matching every comparable byte. The same ranking shows `UpdatePulsingRadialTrail @ 0x00445620` at 598/598 bytes with all 18 relocation/linkage targets resolved and 524/526 comparable bytes equal. Its only differences exchange the commutative X87 load/add operand offsets for the Y component of `vm->position + vm->positionOffset`; natural expression, local, inheritance, and component-shape probes did not reproduce the target scheduling. It remains non-exact rather than introducing volatile qualifiers, inline assembly, or byte-shaped source. After the five promotions, ANM has 55 canonical exact owners / 10,232 bytes and 68 source-present authored owners / 31,475 bytes in the exact backlog. | `.analysis/gpt-5.6-sol/20260915-anm-ecl-exact-triage/`; dual normal-COFF and linked `/GL` ranking; bounded natural-source experiments; canonical ledgers |
| TOOLCHAIN-084 | observed / exact-regression-repaired | Full-source cold replay found that the later semantic typing of ASCII manager field `+0x89A4` as non-trivial `AnmVmIdView` had inserted a target-absent four-byte pre-clear into `AsciiManagerView::AsciiManagerView`, changing its linked contribution from the canonical 251 bytes to 255. Globally removing the VM-id default constructor restored that owner but changed exact `AddVmVariant0` from 118 to 119 bytes, proving the constructor is part of that return path's codegen. The field is therefore maintained as its target-used scalar integer storage while general VM-id return values retain `AnmVmIdView`. A focused three-unit cold replay closes the ASCII constructor and both 118-byte AddVm tails; the final full `src/AnmManager.cpp` replay closes all 65 configured units across ten freshly built artifacts and 11,068 compared target bytes. | Target and candidate disassembly; PDB contribution extents; `.analysis/gpt-5.6-sol/20260915-anm-ecl-exact-triage/ascii-constructor-after-pod.json`; `pod-storage-three-unit.json`; `full-anm-replay-final.json` |
| ANM-024 | observed / exact | `AnmRenderManagerView::Draw3D @ 0x00444760-0x00444B00` is now canonical exact across its complete 929-byte linked `/GL` PDB contribution and all 25 code fields. The last 13 non-field differences came from modeling target address `0x00491C30` only as an independent D3D-device pointer. TH10's Main layout and the target instruction schedule establish this access as `MainSupervisorView +0x8`: expressing that one call through a source-local supervisor prefix reproduces the target's push/cache-store/device-load order. The canonical field uses supervisor base `0x00491C28` with addend eight, which also disambiguates the diagnostic harness address shared with a neighboring four-byte anchor. Two cold replays close Draw3D; the second also replays four adjacent renderer owners for 1,756/1,756 bytes. A final full-source gate closes all 66 configured `src/AnmManager.cpp` units across ten cold artifacts and 11,997 target bytes. Repository totals become 116 exact functions / 13,988 bytes; ANM is 56 exact owners / 11,161 bytes with 67 source-present authored owners / 30,546 bytes remaining. | Canonical target disassembly and Main layout; normal and linked `/GL` rank; PDB contribution and exhaustive linkage manifest; `.analysis/gpt-5.6-sol/20260916-anm-ecl-exact-next/` |
| ANM-025 | observed / exact-two-units | `AnmRenderManagerView::Draw2D @ 0x004436C0` and `Draw2DRotatedOrAxisAligned @ 0x00443910` are canonical exact across both complete 591-byte linked `/GL` PDB contributions and 21 code fields per owner. The former near match split each three-term coordinate sum into one assignment and two compound assignments; retaining each sum as one naturally left-associated expression makes VC7.1 complete the horizontal-anchor mask before loading the first Y operand, exactly matching the target's only differing scheduling region. The arithmetic, operand order and behavior are unchanged, and no artificial dependency or padding is used. Two focused cold replays close 1,182/1,182 bytes; the full `src/AnmManager.cpp` gate closes 68 units across ten cold artifacts and 13,179 target bytes. Repository totals become 118 exact functions / 15,170 bytes; ANM is 58 exact owners / 12,343 bytes with 65 source-present authored owners / 29,364 bytes remaining. | Canonical target and candidate disassembly; PDB contribution and exhaustive linkage manifests; two focused and one full-source cold replay below `.analysis/gpt-5.6-sol/20260916-anm-ecl-exact-next/` |
| ANM-026 | observed / exact | `AnmRenderManagerView::SetRenderStateForVm3D @ 0x004423E0` is canonical exact across its complete 440-byte linked `/GL` PDB contribution and all eight code fields. Its last 12 comparable differences were the same source-ownership scheduling class recovered in Draw3D: target address `0x00491C30` is both the standalone device pointer and `MainSupervisorView +0x8`. Expressing only the texture-factor call through the source-local supervisor prefix reproduces the target push/cache-store/device-load sequence, while the other device accesses remain standalone-global fields. The canonical field binds supervisor base `0x00491C28` plus eight. Two focused cold replays close 440/440 bytes; the full `src/AnmManager.cpp` gate closes 69 units across ten artifacts and 13,619 target bytes. Repository totals become 119 exact functions / 15,610 bytes; ANM is 59 exact owners / 12,783 bytes with 64 source-present authored owners / 28,924 bytes remaining. | TH10 Main layout and target/candidate disassembly; PDB contribution and exhaustive linkage manifest; focused and full-source cold replays below `.analysis/gpt-5.6-sol/20260916-anm-ecl-exact-next/` |
| ANM-027 | observed / exact-two-units | `AnmRenderManagerView::DrawGeneratedVertices @ 0x00444CE0` and `DrawTexturedTriangleFan @ 0x004450E0` are canonical exact across their complete 224- and 211-byte linked `/GL` PDB contributions and all 7 and 10 declared code fields. Their last 13 differing bytes were the texture-bind schedule already established by Draw3D and `SetRenderStateForVm3D`: expressing only `SetTexture` through the TH10-proved device field at `MainSupervisorView +0x8` makes VC7.1 push the texture and update renderer cache before loading the device, while later calls retain the standalone device global. Two focused cold replays close 435/435 bytes; the final full `src/AnmManager.cpp` gate closes all 71 configured units across ten artifacts and 14,054 target bytes. Repository totals become 121 exact functions / 16,045 bytes; ANM is 61 exact owners / 13,218 bytes with 62 source-present authored owners / 28,489 bytes remaining. | TH10 Main layout and target/candidate disassembly; PDB contributions and exhaustive linkage manifests; focused and full-source cold replays below `.analysis/gpt-5.6-sol/20260916-anm-ecl-exact-next/` |
| ANM-028 | observed / exact | `AnmVmView::GetFloatVar @ 0x0043EAC0` is canonical exact across its complete 424-byte linked `/GL` PDB contribution and all 44 declared linkage fields. The target physically orders its three RNG bodies as unsigned random, signed random, then signed random multiplied by pi; the maintained switch previously ordered the same mapped cases as angle, unsigned random, signed random. Reordering only those case clauses reproduces the target body while preserving enum values, jump-table destinations and semantics. The exhaustive manifest covers `__ftol2`, six RNG calls and owner globals, both pi references, two three-component global owners with base addends, the table base and all 22 case destinations. Two focused cold replays close 424/424 bytes; the full `src/AnmManager.cpp` gate closes 72 units across ten artifacts and 14,478 target bytes. Repository totals become 122 exact functions / 16,469 bytes; ANM is 62 exact owners / 13,642 bytes with 61 source-present authored owners / 28,065 bytes remaining. | Canonical target and candidate disassembly; PDB contribution and exhaustive linkage manifest; focused and full-source cold replays below `.analysis/gpt-5.6-sol/20260916-anm-ecl-exact-next/` |
| TOOLCHAIN-085 | observed / near-exact-retained | `AnmLoadedView::SetAndExecuteScriptIdx @ 0x0043E8B0` remains a complete 287-byte linked `/GL` near match at 249/271 comparable bytes with all four linkage positions structurally aligned. Every difference comes from exchanging the long-lived `ESI` and `EDI` roles; control flow and instruction selection otherwise coincide. Bounded probes covered parameter naming, declaration order, split/combined guards, direct member flags, standard register/const hints, operand syntax, explicit LTCG entry and `/G7`. None supplied a natural exact form: most reproduce the same register allocation, explicit entry changes the private ABI, and `/G7` changes the body size. The maintained natural source is restored and no volatile, assembly or inert code-shaping dependency is retained. | Target/candidate disassembly; bounded reports named `set-script-*` and `anm-profile-g7.json` below `.analysis/gpt-5.6-sol/20260916-anm-ecl-exact-next/`; unchanged canonical ledger |
| ANM-029 | observed / exact | `AnmRenderManagerView::CreateTextureFromAnm @ 0x00446F40` is canonical exact across its complete 264-byte linked `/GL` PDB contribution and all nine declared fields. The target's two destination/source rectangle `LEA` instructions resolve through their different transient stack offsets to the same local rectangle populated from the embedded header width and height. Passing that rectangle to both `D3DXLoadSurfaceFromMemory` region parameters, rather than passing a null destination region, restores the missing three bytes and all 45 formerly differing comparable bytes while accurately limiting the upload. Two focused cold replays close 264/264 bytes; the full `src/AnmManager.cpp` gate closes 73 units across ten artifacts and 14,742 target bytes. Repository totals become 123 exact functions / 16,733 bytes; ANM is 63 exact owners / 13,906 bytes with 60 source-present authored owners / 27,801 bytes remaining. | Canonical target/candidate stack and call disassembly; PDB contribution and exhaustive linkage manifest; focused and full-source cold replays below `.analysis/gpt-5.6-sol/20260916-anm-ecl-exact-next/` |
| ANM-030 | observed / exact | `AnmRenderManagerView::CaptureToSurface @ 0x00448450-0x004485CA` is canonical exact across its complete 379-byte linked `/GL` PDB contribution and all 11 declared fields. The final instruction is the three-byte `RET 36` at `0x004485C8-0x004485CA`, correcting the former claim that its last two bytes were CC padding. TH10 Main layout identifies device `0x00491C30` and backbuffer format `0x00491D14` as `MainSupervisorView +0x8/+0xEC`; expressing both through their common owner restores the render-target and fallback schedules. Natural locals for the selected secondary surface slot and D3D9 device restore the remaining register allocation through the second surface creation and both D3DX copies. Two focused cold replays close 379/379 bytes; the full `src/AnmManager.cpp` gate closes 74 units across ten artifacts and 15,121 target bytes. Repository totals become 124 exact functions / 17,112 bytes; ANM is 64 exact owners / 14,285 bytes with 59 source-present authored owners / 27,422 bytes remaining. | Main owner layout; complete target/candidate disassembly; PDB contribution and exhaustive linkage manifest; focused and full-source cold replays below `.analysis/gpt-5.6-sol/20260916-anm-ecl-exact-next/` |
| ANM-031 | observed / exact | `AnmRenderManagerView::Draw @ 0x004451C0-0x00445297` is canonical exact across its complete 216-byte linked `/GL` PDB contribution: 173 code bytes, the three-byte alignment instruction at `0x0044526D`, and the ten-entry absolute table at `0x00445270`. TH10 physically orders case blocks as modes `0,1,4,5,6,7,8,9,2,3`; retaining that case-clause order closes the dispatcher body while preserving normal numeric table mappings. The target's two flag failures branch into the alpha-zero rejection block immediately before the switch. A single shared source label inside that real rejection branch reproduces the target CFG without artificial state, padding, assembly, or copied bytes. Two independent cold replays close 216/216 bytes and all 21 fields: table base, ten renderer calls and ten table destinations expressed as offsets within the same owner. The full `src/AnmManager.cpp` gate closes 76 units across ten artifacts and 15,739 target bytes. Repository totals become 126 exact functions / 17,730 bytes; ANM is 66 exact owners / 14,903 bytes with 57 source-present authored owners / 26,804 bytes remaining. | Target/candidate disassembly and CFG; complete PDB contribution; exhaustive self-relative table linkage manifest; focused and full-source cold replays below `.analysis/gpt-5.6-sol/20260916-anm-draw-exact/` |
| ANM-033 | observed / source-shape-corrected | `AnmVmFloat3InterpolationView::Evaluate @ 0x00404610` uses value-producing vector addition in modes 7 and 17: the new initial value is assigned from `initial + final` or `initial + finalTangent`, and the acceleration tangent is assigned from `finalTangent + final`. Its completed-duration path retains separate initial/final copies and returns. In `ExecuteScript`, position interpolation branches before evaluation and consumes the evaluator's returned pointer in both branches; rotation evaluates through a shared local before copying the returned vector; scale likewise consumes its evaluator's returned pointer. These forms reproduce target-observed temporaries and copy paths without changing behavior. | Hash-attested target disassembly; Factory-native Ghidra decompilation; linked VC7.1 target/candidate data flow; maintained source |
| TOOLCHAIN-093 | observed / evaluator-near-and-executor-open | With `ExecuteScript` as the pinned `/GL` entry, the Float3 evaluator improves from a 739-byte candidate with 29/847 comparable matches to 854 bytes with 201/847 matches against the 887-byte target. Its zero register, value-add paths and two completed-duration returns now follow the target; 33 contribution bytes remain open. The executor's position interpolation block now has the target branch/call/copy shape and rotation has the target local-return copy shape. The complete executor remains non-exact: its candidate pre-table span is 9,472 bytes versus target 9,588 (`-116`), and the Float2 evaluator still receives a different private receiver register/temporary slot. The full ANM exact gate remains closed at 76 units / 10 artifacts / 15,739 bytes; repository exact totals remain 134 functions / 18,454 bytes. | `.analysis/gpt-5.6-sol/20260916-anm-interpolation-shape/final/{probe,table,replay-anm}.json`; pinned VC7.1 SP1 build6030; complete target/candidate disassembly |
| ANM-034 | observed / evaluator-core-near | TH10's Float2 scale evaluator uses the same separate completed-duration returns and value-object updates as the Float3 owner. Its mode-7 path assigns `initial + final`; mode 17 assigns `initial + finalTangent`, then `finalTangent + final`; Hermite materializes four weighted two-component values before summing their components. In the maintained `ExecuteScript` `/GL` context this source restores the target private `ESI` receiver and `EDI` output ABI and produces a 656-byte contribution against the 652-byte target, with 190/612 comparable bytes matching. The four-byte extent difference remains a code-generation question, so the owner stays source-present/non-exact. The enclosing executor remains at a 9,472/9,588-byte pre-table span, and all 76 canonical ANM units / 10 artifacts / 15,739 bytes cold-replay exactly. | Hash-attested target disassembly and Ghidra decompilation; `.analysis/gpt-5.6-sol/20260916-anm-float2-core/vector-products/{probe,replay-anm}.json`; pinned VC7.1 SP1 build6030 |
| ECLVM-010 | observed / arithmetic-core-recovered | The nine arithmetic cases in generic `EclVmContext::Run @ 0x0044E1A0` pop right and left operands directly through `EclVmStackView::Pop`, mutate the left typed local in place and push that same local. Integer and raw-float-storage negation likewise reuse the popped local, and post-decrement pushes its preserved original value after writing the decremented destination. These source forms reproduce the target's duplicated typed-conversion paths rather than merging them through scalar wrapper return values. | Hash-attested target/candidate disassembly; maintained source; `.analysis/gpt-5.6-sol/20260916-ecl-typed-arithmetic/final/{probe,layout}.json` |
| TOOLCHAIN-094 | observed / ecl-owner-near | In the selected `EclVmHost::Run` `/GL` context, the recovered typed arithmetic and unary data flow increases generic `EclVmContext::Run` from 6,648 to 6,916 candidate bytes against the 7,020-byte physical target owner. Its pre-table span improves from 6,320 to 6,588 bytes against target 6,692 (`-104`) while retaining the target `0x108` frame and exact physical order of all 59 active opcode bodies. The remaining systematic gap is dominated by an `EBX`/`EBP` live-range exchange and `SUB reg,4; JS` versus target `ADD reg,-4; CMP reg,EBP; JL`; the isolated three-byte post-decrement excess is an LTCG internal-call ABI difference. The owner remains non-exact. A cold full-source gate preserves all 8 canonical ECL units / 2 artifacts / 724 bytes exactly. | `.analysis/gpt-5.6-sol/20260916-ecl-typed-arithmetic/final/{probe,layout,replay-ecl}.json`; complete target/candidate disassembly; pinned VC7.1 SP1 build6030 |
| INVENTORY-006 | observed / core-source-complete | The reviewed authored `AnmManager`, `EclVm`, and `Enemy` modules contain 178 owners / 74,197 bytes, all 178 of which have maintained source mappings. Zero reviewed authored owners in those modules are source-absent. Ninety-four are canonical exact; the remaining 84 owners / 58,059 bytes are source-present/non-exact. This is a source-presence handoff fact, not exactness credit. The ANM, generic ECL and Enemy dispatcher target/source topology gates independently cover all 94, 59 and 124 opcode values respectively. | `scripts/report-source-completeness.py --module AnmManager --module EclVm --module Enemy --require-complete`; `.analysis/gpt-5.6-sol/20260916-anm-ecl-web-handoff/`; target-bound table reports |
| TOOLCHAIN-095 | observed / environment-routing-repaired | On this operator host, `/usr/bin/python3` is Python 3.12 and imports Capstone 4.0.2 from `/usr/lib/python3/dist-packages`; the available Conda Python 3.13 imports Capstone 5.0.6 whose wrapper, x86 bindings/constants and native library match all four hashes in `config/tools.lock.toml`. A Web-style `PATH=/usr/bin:/bin` therefore fails a direct `python3 scripts/verify-toolchain.py --execute` for interpreter selection alone. `scripts/repo-python` checks every candidate against the unchanged decoder lock before executing repository commands and successfully selects the pinned interpreter under that same PATH. The full normal/LTCG/resource/PE32 toolchain smoke and generic ECL table gate then pass. The direct system-Python path still fails closed with the observed version and path, and no exactness state changes. | Local interpreter/package paths and versions; hash-pinned `verify_capstone`; `.analysis/gpt-5.6-sol/20260916-python-selection-toolchain.json`; Web-style PATH replay |
| TOOLCHAIN-096 | observed / ecl-allocator-gap-isolated | Fresh TH10 target/candidate review narrows the remaining `EclVmContext::Run @ 0x0044E1A0` mismatch to VC7.1 LTCG register allocation and a few local case shapes rather than missing opcode semantics. The 7,020-byte target keeps `this` in ESI, the writable instruction slot address in EBX, a preserved zero in EBP, and the current instruction in EDI. The current 6,916-byte candidate keeps the same ESI/EDI roles but exchanges EBX and EBP. Consequently each inlined typed-stack pop uses target `ADD reg,-4; CMP reg,EBP; JL` (7 bytes) versus candidate `SUB reg,4; JS` (5 bytes): binary typed-pop cases are normally four bytes short and unary cases two bytes short. Summing target-minus-candidate case-block gaps in physical order accounts for 93 of the 104 missing pre-table bytes; the remaining 11 bytes are shared/prologue-tail differences. `ECL_VM_EVALUATE_FORMAT_OPERANDS` also proves that EBX/EBP are intentionally reused inside a case and restored at the common advance tail: target uses EBX as flag index and EBP as value-word index, then zeroes EBP and reconstructs the instruction slot in EBX. Cold `/GL` probes with the exact `EnemyRuntimeUpdate` support context, declaration-order changes, register/const hints, pointer/reference aliases, outer-scoped current instruction, direct member reads, and lexical versus forced-inline format bodies did not reproduce the target register coloring without introducing target-absent control flow or moving farther from the target. The maintained natural source is therefore unchanged and exactness remains open. | Canonical target disassembly at `0x0044E1A0-0x0044FBC1`; fresh VC7.1 SP1 `/GL` PDB contributions; current-source case-gap report from `.analysis/gpt-5.6-sol/20260916-ecl-typed-arithmetic/final/layout.json`; bounded scratch findings in `.analysis/gpt-web/20260916-ecl-run-hard/findings.json` |
| ECLVM-011 | observed / exact-stack-read-leaves | Direct state updates recover the original source/codegen seam shared by the stack/read leaf corridor. `EclVmStackView::Pop` tests `stackTop - size`, then performs `stackTop -= size` and copies from the updated member; `LeaveFrame` similarly decrements `stackTop` directly before loading the saved frame base. The standalone exact `Pop` body is unchanged, while LTCG constant-propagated four-byte pops now use the target `ADD reg,-4` form. This closes `ReadFloat @ 0x0044FE40` (183 bytes), `ReadIntValue @ 0x0044FF00` (124), `ReadFloatValue @ 0x0044FF80` (171), and `LeaveFrame @ 0x004506D0` (41) without ABI forcing or byte patches. | Maintained natural source; complete target disassembly; reviewed PDB extents; retained pinned-decoder linkage diagnostics from `.analysis/gpt-5.6-sol/20260916-ecl-vm-read-resolve/` |
| TOOLCHAIN-097 | observed / exact-four-leaf-promotion | Two independent cold VC7.1 SP1 `/GL` builds in the maintained `EclVmContext::Run` entry context reproduce all four new complete PDB extents with zero normalized differences. `LeaveFrame` is raw 41/41 byte-identical; the three read helpers differ raw only at their exhaustive declared float-constant DIR32 and `__ftol2` REL32 fields. Seven previously canonical units sharing the same linked artifact remain zero-difference, and the separately contextualized `EclVmHost::Run` unit remains 146/146 after rebuilding with `src/Enemy.cpp` support. The tracked graph therefore advances from 134 to 138 exact functions and the authored source backlog falls from 161 to 157. Factory accepted-snapshot authority still depends on the required post-commit canonical replay receipt. | `config/match-units.toml`; two cold PDB/function-level replays below ignored `build/`; `scripts/build-match-unit.py --check`; regenerated conservative progress |
| ANM-035 | observed / exact-leaf | `AnmRenderManagerView::GetVmPosition @ 0x004493C0` is a complete 27-byte normal-COFF exact leaf. The maintained ternary had allowed VC7.1 to reuse the null return from `FindVm`; spelling the same semantics as an explicit non-null return followed by `return NULL` reproduces the target `JE null / ADD +0x340 / RET 4 / XOR EAX,EAX / RET 4` shape. Canonical unit `anm-manager-get-vm-position` covers all 27 bytes and its sole REL32 to `FindVm @ 0x004491C0`; two cold replays also keep all six pre-existing normal-COFF `AnmManager.obj` units exact. | Maintained natural source; pinned VC7.1 SP1 normal COFF; complete target extent and relocation replay |
| MAIN-023 | observed / exact | `GameWindowView::ResetRenderState @ 0x00439D20` is canonical normal-COFF exact across the complete 710-byte extent. All 574 non-relocation bytes and all 34 DIR32 fields replay zero-difference twice: 33 fields address `MainSupervisorView +0x8` and the final field addresses `g_AnmRenderManagerView`. This bounded codegen claim remains independent of unresolved production normal-COFF versus LTCG physical ownership. | Maintained source; pinned VC7.1 SP1 normal-COFF cold replay; target-bound complete relocation manifest |
| TOOLCHAIN-098 | observed / exact-two-unit-promotion | The leaf pass promotes `GetVmPosition` (27 bytes) and the previously diagnostic-only `ResetRenderState` (710 bytes) without ABI hacks, volatile forcing, inline assembly, copied bytes, or inert dependencies. Both units pass two independent cold replays; the shared `AnmManager.obj` regression set remains 7/7 exact. Repository totals advance to 141 canonical exact functions / 19,773 authored exact bytes with 154 authored source-present functions remaining. `UpdatePulsingRadialTrail` remains a 598/598-byte two-byte X87 operand-order near match, while `GetTimestamp` remains 282/282 with only three stack-frame-size immediates (`0x10` versus target `0x14`) differing under every tested natural source/profile variant. | Canonical match units and ledgers; cold replay receipts below ignored `build/leaf-experiments/`; current normal-COFF backlog sweep |
| MAIN-024 | observed / fps-owner-recovered | TH10 `0x004134B0-0x004135C7` is a 280-byte half-second FPS sampler and `0x004135D0-0x00413683` is its 180-byte display owner. Target-local control flow proves the sampler reads `GameWindowView::GetTimestamp`, repairs backward time, computes unsigned frame-count FPS, treats `>65` as a clock anomaly, resets the four GameWindow timestamps at anomaly counts two/four, disables QPC on the fourth anomaly, and accumulates lag statistics through a gameplay-state gate at `+0x58`. The display owner calls the sampler, selects three colors, emits `%2.1ffps` through the ASCII manager, and advances the frame count by `frameskip + 1`. Callback bridge `0x00413690` directly calls the display owner at `0x00413693`, correcting its former provisional boundary to reviewed/high. The adjacent TH095 `CalculateFps`/`DrawFpsCounter` family supplies only descriptive naming; all maintained fields and behavior are independently present in TH10. | Hash-attested TH10 disassembly and Ghidra decompilation; callback installation/call chain; maintained `FpsCounterView` layout/source in `src/Main.cpp`; adjacent family used only after target behavior recovery |
| TOOLCHAIN-099 | observed / exact-fps-context | Restoring the target-proved FPS owner family exposes the missing production `/GL` alignment context for `GameWindowView::GetTimestamp @ 0x00439540`. VC7.1 gives `LARGE_INTEGER` size/alignment 8; the real FPS sampler begins `PUSH EBP / MOV EBP,ESP / AND ESP,-8` before calling GetTimestamp. In that graph, GetTimestamp changes only its local frame from `0x10` to the target `0x14` and becomes exact across all 282 bytes and 30 linked fields. The same graph emits `FpsCounterView::CalculateFps @ 0x004134B0` exact across all 280 bytes and 20 linked fields. Two independent cold VC7.1 SP1 `/GL` links using the authored `DrawFpsCounter` owner as entry produce zero normalized differences for both functions. The tracked graph advances to 143 canonical exact functions / 20,335 bytes and 301 source mappings; `DrawFpsCounter` remains source-present/non-exact. The live shell still cannot import the hash-pinned Capstone 5.0.6 decoder, so the repository replay wrapper fails closed and no weaker decoder is substituted. | `config/match-units.toml`; `.analysis/gpt-5.6-sol/20260916-main-fps-exact/findings.json`; two cold outputs under `build/leaf-experiments/fps-tracked-replay{1,2}`; target stack-alignment/caller evidence |
| MAIN-025 | observed / exact-fps-display | `FpsCounterView::DrawFpsCounter @ 0x004135D0` is canonical linked exact across its complete 180-byte owner. Adding `src/AnmManager.cpp` as the real formatter support TU lets VC7.1 recover the target private register contract (`EDI` FPS owner, `ESI` ASCII manager, `EBX` position); retaining `currentFps` as one local `float fps` keeps the x87 value live across both 30/40-FPS comparisons and reuses it directly for the variadic double argument, reproducing the target `FCOM / FCOM / FSTP QWORD` schedule. Two independent cold `/GL` links reproduce all 180 bytes after replaying nine CalculateFps, display-mode, ASCII-manager, threshold, format-string, formatter-call and frameskip fields. The retained 10-byte callback bridge at `0x00413690` is raw byte-identical in the same graph, but its authored-versus-compiler/LTCG provenance remains unresolved, so it is deliberately not promoted. | `main-fps-draw` linked unit; two cold outputs under `build/leaf-experiments/fps-draw-tracked-replay{1,2}`; target callback/Draw bytes; maintained natural locals in `src/Main.cpp` |
| TOOLCHAIN-100 | observed / fps-family-regression-closed | The completed FPS support graph now closes three authored Main owners without trading regressions: CalculateFps 280 bytes, DrawFpsCounter 180 bytes, and GetTimestamp 282 bytes. After the Draw source-shape correction, two additional Main-only cold links still reproduce CalculateFps and GetTimestamp with zero normalized differences in their original canonical context, while two full Main+AnmManager links independently close DrawFpsCounter and emit the target 10-byte callback bridge raw-equal. Repository totals advance to 144 canonical exact functions / 20,515 exact bytes and 153 authored source-present functions remain in the exact backlog. | Canonical match units and ledgers; `build/leaf-experiments/fps-main-context-regress{1,2}`; `fps-draw-tracked-replay{1,2}`; target-independent CI gates |
| ANM-036 | observed / exact-leaf | `AnmInt3View::operator* @ 0x00441E50` is canonical exact across the complete 56-byte linked `/GL` PDB contribution and all three `__ftol2` REL32 fields. Target call topology proves that the old descriptive free `ScaleAnmInt3` helper is value-returning member behavior: the unique color-Hermite caller passes the integer triplet in EDI, hidden result storage in ESI, one float on the stack, and the callee returns with `RET 4`. Restoring the member operator, evaluating the final Hermite term before the initial term, and retaining X/Z/Y constructor argument slots makes VC7.1 naturally evaluate Y, Z, then X and store Z, X, then Y, reproducing every target instruction without ABI annotations, assembly, volatile forcing, or inert dependencies. | Complete target/candidate disassembly; two cold `ExecuteScript`-context links; PDB extent; exhaustive three-field replay; same-context regression gate |
| TOOLCHAIN-101 | observed / exact-scale-regression-closed | Two independent cold VC7.1 SP1 `/GL` links from tracked source reproduce `AnmInt3View::operator*` as the same stable 56-byte contribution; after normalizing its three `__ftol2` calls there are zero ordinary-byte differences from `0x00441E50-0x00441E87`. A focused regression of all 16 pre-existing canonical units sharing the same `ExecuteScript` entry/profile remains zero-difference. A clean HEAD-plus-scale metadata snapshot passes match-graph and tracking validation and advances the repository to 147 canonical exact functions / 20,729 exact bytes with 150 authored source-present functions remaining. | `build/scale-int3-live-{1,2,final}`; `build/scale-canon-clean`; canonical unit `anm-int3-scale`; pinned VC7.1 SP1 build6030 |
| ENEMY-014 | observed / exact | `EnemySetMotionAngle @ 0x00413170` is a complete 20-byte stack-argument `RET 8` helper that normalizes an angle through `EnemyWrapAngle @ 0x0044BC70` and stores the x87 result at motion `+0x1C`. The 94-byte angle wrapper uses signed-pi bounds, two-pi steps, one shared iteration counter, and `RET 4`. Two independent cold normal-COFF builds reproduce both entire target extents and all seven declared call/constant relocations. The `Enemy.cpp` declaration uses the target-proved `__stdcall` ABI; its two pre-existing local-table units remain exact after their compiler label numbers shift. | Hash-attested target; IDA disassembly/decompilation; `enemy-set-motion-angle` and `enemy-wrap-angle` canonical replay; four-source regression |
| GUI-003 | observed / source-present | `GuiSetLivesDisplayCount @ 0x00413790` takes the GUI owner in EAX and count in ECX, sets flag bit one in `count` consecutive `0x3AC`-stride life VM slots, then clears that bit up to slot nine when `count < 9`. The 77-byte target owner and the Player reset call establish the descriptive source mapping. Both GUI and Player `/GL` probes compile. Its private register ABI and exact code generation remain open. | Hash-attested IDA body; reviewed target boundary and Player call chain; maintained `src/Gui.cpp` and `src/Player.cpp`; pinned VC7.1 `/GL` compile |
| MAIN-026 | observed / exact | The 100-byte system-parameter initializer at `0x004392E0`, 62-byte settings/IME restore helper at `0x00439350`, and 152-byte unlocked timestamp helper at `0x00439660` now have maintained source and canonical normal-COFF units. Two independent cold builds reproduce all 314 target bytes and all 27 declared global, import, constant and call fields. IDA omits a function at `0x00439350`, but all 62 target bytes independently decode through RET. Original identifiers and production normal-COFF versus LTCG owners remain unknown; exactness is limited to the declared normal-COFF codegen replay. | Hash-attested target and IDA where available; pinned Capstone dense decode; `main-initialize-system-parameters`, `main-restore-system-parameters`, and `main-get-timestamp-unlocked` cold replays; Main regression |
| COMPRESSION-007 | observed / ABI-corrected | `Lzss::DeleteString @ 0x00436210` consumes its node index in ECX, including the recursive call. The maintained static helper now declares `__fastcall`, matching that target entry contract. Existing five LZSS exact units remain zero-difference after a cold mixed COFF/LTCG replay. `DeleteString` itself is still non-exact, and its production physical owner remains unknown. | Hash-attested target Capstone disassembly; maintained declaration/source; focused LZSS canonical replay |
| ORIGIN-001 | observed / runtime-library-origin | A complete-COFF-symbol scan of the pinned VC7.1 SP1 `libcmt.lib` and `libcpmt.lib` against the hash-attested TH10 image found 242 unique `libcmt.lib` function-body matches covering 49,952 target bytes. Each accepted symbol has a matching complete COFF extent of at least 32 bytes and at least 24 matching non-relocation bytes. All 635 relocation references whose symbols resolve uniquely within the accepted set reach the expected target entry. These 242 entries are classified `library` and excluded from the authored denominator; this is provenance evidence, not a source-exact or build-closure claim. The largest is the 3,627-byte `__input @ 0x00457A7C`. Short or ambiguous matches remain under review. | `scripts/review-crt-origins.py`; pinned target and library hashes; complete COFF symbol extents, relocation masks and target reference resolution; ignored detailed report under `.analysis/gpt-5.6-sol/20260917-origin-review/` |
| BOUNDARY-001 | observed / independent-entry-review | The pinned runtime member extents establish 41 previously provisional or focused, non-overlapping function boundaries. Separately, IDA MCP identified 28 prior provisional entries with matching function extents and entry references. Re-decoding the hash-attested target at every referenced address proved a direct `call` or `jmp` to the candidate entry, and the prior boundary audit had already established complete dense decode and no overlap. These first two campaigns moved the boundary ledger from 1,175 to 1,244 reviewed entries. | `scripts/review-crt-origins.py`, `scripts/review-boundary-entry-refs.py`; IDA MCP target hash and function inventory; pinned Capstone direct-branch replay; existing target boundary audit |
| ORIGIN-002 | observed / runtime-family-origin | Thirteen more candidate bodies match multiple pinned `libcmt.lib` COFF symbols with the same full extent and at least 24 identical non-relocation bytes. Their library origin is recorded with medium confidence; their individual source symbol remains ambiguous. This includes two 829-byte memory-copy bodies: both match `_memcpy` and `_memmove` members across 645 ordinary bytes, and each contains 46 relocated local control targets that all resolve inside its own extent. Repeated C++ template matches from `libcpmt.lib` were left pending because an identical inline instantiation does not prove library ownership. | `scripts/review-crt-origins.py`; pinned target and VC7.1 library hashes; full COFF function bodies and target-local relocation destinations |
| BOUNDARY-002 | observed / full-inventory-review | All 1,313 current candidate boundary rows are reviewed. The former 5,022-byte `0x00463C0D` span was corrected to its 174-byte contiguous entry ending at `0x00463CBA`; IDA shows remote shared exit chunks near `0x00464F9E`, while the old span swallowed independent math CRT functions. Ten library routines contain a small directly called RET helper inside the parent extent, so both parent and nested helper are deliberately retained with documented overlap. The two 829-byte memory-copy owners include local dispatch data and all 46 COFF relocation targets stay inside their extents. Remaining direct, data-pointer, noreturn-tail and shared-prefix entries were replayed from target bytes. Fifteen code islands with no observed independent entry reference were promoted at medium confidence from eleven matching IDA extents or four explicit target-local separators; the missing reference remains stated in each row. Boundary review establishes a physical candidate extent only; authored origin and exact source are separate. | `scripts/review-boundary-entry-refs.py`, `scripts/review-isolated-boundaries.py`, `scripts/review-crt-origins.py`; hash-matched IDA MCP xrefs/extent observations; pinned Capstone; COFF member extents and relocations; tracking ledger |
| ORIGIN-003 | observed / PE-import-and-EH-provenance | Twenty-one six-byte `FF 25` jump thunks resolve to named or ordinal entries in the target's own PE import table and are classified `import_thunk`. Forty-one VC7.1 C++ `FuncInfo` records with magic `0x19930520` point through valid unwind maps to all 62 current `Unwind@` cleanup entries; each action has a complete target RET/JMP body and is classified `compiler_generated`. These conclusions come from target metadata and target instructions, not from IDA's provisional labels alone. | `scripts/review-import-origins.py`, `scripts/review-eh-origins.py`; pinned target PE import descriptors and C++ exception tables; IDA xref spot checks; pinned Capstone |
| ORIGIN-004 | observed / CRT-object-family-provenance | The runtime scan now classifies 39 unique short `libcmt.lib` bodies with medium confidence, 30 candidate entries whose complete COFF body extends beyond the ledger's contiguous local fragment, 30 nested candidate fragments fully inside a matched CRT object's function extent, and 11 same-size lower-information bodies whose provisional inventory name agrees with a matching CRT symbol. Every accepted extended body matches all non-relocation bytes across the complete original COFF extent, including locally split suffix/helper code; 961 uniquely resolvable runtime relocation references agree with target destinations. Source-exact matching and authored ownership are not inferred from these provenance observations. | `scripts/review-crt-origins.py`; pinned `libcmt.lib` hash; complete COFF symbol extents, relocation masks, target-local destination replay; `config/function-origins.csv` |
| ORIGIN-005 | observed / authored-game-provenance | Forty-six previously unclassified candidate bodies totaling 31,234 target bytes are completely decoded and independently show a direct call to an already reviewed authored function plus references to at least two exact game-global addresses each used by at least two previously reviewed authored functions. None of those globals appears in reviewed excluded bodies. IDA spot checks show game display, flow and state operations in representative entries. These bodies are classified authored at medium confidence; their subsystem, source unit, production compiler mode and exactness remain unknown. The evidence script excludes its own rows from the seed set on replay. | `scripts/review-game-global-origins.py`; hash-attested target instructions, direct calls and global references; reviewed origin ledger; IDA MCP decompilation spot checks at `0x00402850`, `0x00409280`, `0x004236F0`, `0x00433EF0`, `0x00422C30` |
| ORIGIN-006 | observed / authored-game-neighbor-provenance | Seventy-seven further complete code bodies totaling 30,505 bytes reference an exact game-global address also used by at least two earlier reviewed authored functions and have an independently decoded direct call to or from one of those earlier authored functions. Reviewed excluded bodies do not reference the selected global. Representative IDA decompilations show game state, callback, rendering and file operations. The 77 entries are authored-origin observations at medium confidence; no subsystem, source unit, normal-COFF/LTCG ownership or exactness follows. Both authored-origin campaigns use the earlier 320 reviewed authored entries as fixed seeds. | `scripts/review-game-global-origins.py`; hash-attested target decode/call/global replay; existing origin ledger; IDA MCP spot checks at `0x00405ED0`, `0x00420470`, `0x0043DDF0`, `0x00449470`, `0x0044B620` |
| ORIGIN-007 | observed / authored-call-and-data-provenance | Eleven more game-region bodies totaling 2,789 bytes are directly called by an earlier reviewed authored function and themselves directly call another earlier reviewed authored function. Eighteen further bodies totaling 7,852 bytes reference a target `.data` address used by at least two earlier authored bodies and no reviewed excluded body, and also have a direct call edge to or from an earlier authored body. The ubiquitous `1.0f` constant at `0x00476F78` is excluded from the data evidence. Each selected body completely decodes, and representative IDA decompilation shows game behavior. These are medium-confidence authored-origin classifications with source, subsystem and exactness still open. | `scripts/review-game-edge-origins.py`; target PE `.data` range, target call instructions, fixed earlier authored origins; IDA MCP spot checks at `0x00404F30`, `0x00412AC0`, `0x00426520`, `0x00438A60` |
| ORIGIN-008 | observed / compiler-EH-handler-provenance | The target's VC7.1 C++ `FuncInfo` records include valid `TryBlockMap` entries whose 16-byte `HandlerType` records directly name the two tracked `Catch@` bodies at `0x00438723` and `0x004387C2`. Both completely decode and are classified compiler generated. The prior 62 `Unwind@` classifications still replay independently from unwind maps. | `scripts/review-eh-origins.py`; pinned target `.rdata` `FuncInfo`/`TryBlockMap`/`HandlerType` fields; IDA MCP data xrefs at `0x00471410` and `0x00471400` |
| ORIGIN-009 | observed / thin-CRT-wrapper-provenance | Eleven individually identified 5–15-byte runtime wrappers completely decode and each transfers to exactly one reviewed CRT library body outside its own extent. The target call destination plus wrapper behavior identifies allocator/free/conversion, thread error, exit-lock/cleanup, security-cookie, floating-point trap or math-dispatch support. They are excluded as library origin at medium confidence; the narrow structural evidence does not prove an exact CRT object symbol or source form. | `scripts/review-crt-wrapper-origins.py`; hash-attested target code and previously reviewed CRT callee origins; IDA MCP semantic spot checks for `_errno`, `__doserrno`, `_lockexit`, `_unlockexit`, `_fptrap`, `_CIfmod` |
| ORIGIN-010 | observed / individually-reviewed-game-behavior | Twenty further complete game-region bodies totaling 14,404 bytes were individually inspected in IDA. Their visible behaviors include ANM command/VM state, stage and score transitions, player option state, the TH10 replay header, game sound cues and DirectInput/keyboard transitions. Each has a direct target call edge to or from an already reviewed authored body. Two callers with embedded switch tables have their exact direct `call` sites separately decoded at `0x00432DB9` and `0x0042F6B6`. The 20 entries are classified authored at medium confidence; IDA behavior descriptions are provisional and no source unit, compiler mode or exactness is inferred. | `scripts/review-game-behavior-origins.py`; hash-attested target code, direct call-site replay, existing origin ledger; IDA MCP decompilation of each listed entry |
| ORIGIN-011 | observed / runtime-special-provenance | Five more complete runtime-region entries have individually reviewed CRT behavior: SSE `floor` with calls/jumps to reviewed CRT math bodies, a six-slot CRT floating-conversion pointer initializer, the `__NLG_Notify1` and `strcpy` prefixes that jump into reviewed library bodies, and an x87 status-word helper. They are classified library at medium confidence from target instructions plus IDA semantic inspection; their exact object member/source identity remains unknown. | `scripts/review-crt-wrapper-origins.py`; hash-attested target instructions and previously reviewed CRT bodies; IDA MCP decompilation of the five entries |
| ORIGIN-012 | observed / C++-runtime-family-provenance | Nineteen complete bodies in the late runtime cluster implement `std::string`, standard exception class constructors/destructors, type-info deletion or standard string throw helpers. Target vtable writes, calls and exception strings agree with IDA's type interpretation; several bodies also have matching candidate code in pinned VC7.1 C++ runtime archives, while duplicate template bodies leave exact object membership ambiguous. Three identical seven-byte member-call adapters use `pop eax; pop ecx; xchg [esp],eax; jmp eax` and are directly called from already reviewed CRT EH bodies at three independently decoded call sites. These 22 entries are excluded as library origin at medium confidence without claiming exact CRT symbols or TU ownership. | `scripts/review-cxx-runtime-origins.py`; target instructions, vtable/call/string behavior, pinned CRT candidate report, IDA MCP C++ type and xref review |
| ORIGIN-013 | observed / CRT-callback-helper-provenance | Three final short runtime-region entries are target-bound to reviewed CRT owners: a nine-byte wrapper calls the target `KERNEL32!TlsAlloc` IAT slot and is installed by CRT `__mtinit`; a one-byte RET is the initial value of two CRT thread callbacks consumed by reviewed thread-start/end bodies; and a three-byte zero-result function is the initial CRT math-error callback and is called directly by two reviewed math bodies. Each complete body is classified library origin at medium confidence, leaving exact object provenance unknown. No currently tracked runtime-region origin remains pending. | `scripts/review-crt-wrapper-origins.py`; target PE import table, target `.data` callback slots, target CRT caller instructions and reviewed origin ledger |
| ORIGIN-014 | observed / early-game-behavior-provenance | Thirty-two further complete entries from `0x00401000-0x0040FFFF`, totaling 6,650 bytes, were individually reviewed in IDA for game manager, ANM VM, projectile pool, playfield, scheduler and resource-path behavior. Each has a target-decoded direct call edge to or from an earlier reviewed authored body. They are classified authored at medium confidence; several nearby math helpers and constructors without an independent reviewed call edge remain pending. This origin claim does not identify original TU/source ownership or exact codegen. | `scripts/review-game-behavior-origins.py`; hash-attested target body and direct-call replay; IDA MCP decompilation of all 32 entries |
| ORIGIN-015 | observed / middle-game-behavior-provenance | Twenty-two complete entries from `0x00410000-0x0041FFFF`, totaling 2,732 bytes, were individually reviewed for stage/game objects, scheduler, projectile pool, playfield and resource behavior. Each has a target-decoded direct call edge to or from an authored owner outside the current manual behavior cohorts. They are classified authored at medium confidence; nearby generic string, constructor and interpolation bodies without that independent edge remain pending. `0x00412A10` remains origin-unknown because its ECL integer-lvalue helper could be source-written or outlined by the compiler; a dispatcher call does not resolve that ambiguity. No source unit, build mode or exactness is inferred. | `scripts/review-game-behavior-origins.py`; hash-attested target direct-call replay; IDA MCP decompilation of the 22 accepted entries; prior target-bound Enemy ECL context seam review |
| ORIGIN-016 | observed / late-game-behavior-provenance | Twenty-five complete entries from `0x00420000-0x0042FFFF`, totaling 3,316 bytes, were individually inspected for resource paths, stage transitions, player option/position, playfield and game controller behavior. Each has a target-decoded direct call edge to or from an authored owner outside the current four manual behavior cohorts. They are classified authored at medium confidence. The source-present Player callback bodies at `0x00427960` and `0x00427AE0` remain origin-unknown because their authored-adapter versus compiler-wrapper provenance requires separate evidence. No source/exact or production TU claim follows. | `scripts/review-game-behavior-origins.py`; hash-attested target direct-call replay; IDA MCP decompilation of each accepted entry; Player callback provenance notes |
| ORIGIN-017 | observed / standard-string-provenance | Nine complete entries at `0x004381F0-0x004387C1` implement VC7.1 `std::basic_string<char>` storage reset, assignment, erase, size update and growth. The target uses the 0xF-byte small-string capacity and calls already reviewed standard string throw helpers at `0x00462428` and `0x004624E5`. Entry `0x0043874C` is the normal continuation after the out-of-line EH catch at `0x00438723`, so its independent candidate boundary does not imply a separate source function. These nine entries are classified library at medium confidence; exact library object membership remains unknown. | `scripts/review-cxx-runtime-origins.py`; complete hash-attested target decode and call anchors; IDA MCP standard-string body and EH-continuation review |
| ORIGIN-018 | observed / audio-render-game-provenance | Twenty-one complete entries totaling 3,690 bytes were individually reviewed for GDI text rendering, game window setup, MIDI output, D3D drawing, ANM script random state and the TH10 sound cue/stream manager. Each has a decoded direct call edge to or from an earlier reviewed authored owner outside all five manual behavior cohorts. They are classified authored at medium confidence. In particular, the MIDI and GDI helpers are not assigned a source unit or original identifier merely from adjacency; normal COFF versus LTCG production ownership and exactness remain open. | `scripts/review-game-behavior-origins.py`; hash-attested target call-edge replay; IDA MCP behavior inspection of the 21 entries |
| ORIGIN-019 | observed / game-component-chain-provenance | Eighteen more complete entries totaling 5,902 bytes were individually reviewed for game text token parsing, render object state, GDI glyph bitmap handling, MIDI track/event processing, sprite state and sound filename lookup. A fixed acyclic sequence of target-decoded direct call edges ties each entry to an earlier reviewed authored owner, either from the existing ledger or an earlier entry in this explicit cohort. The author classification is medium confidence and does not infer original source unit or codegen. The DirectInput initializer at `0x0043B8D0` was deliberately left pending because its observed direct callee `0x00421D20` is still origin-unknown. | `scripts/review-game-component-origins.py`; hash-attested complete target decode and fixed call anchors; individual IDA MCP behavior review |
| ORIGIN-020 | observed / game-utility-and-audio-provenance | Forty further complete entries totaling 7,014 bytes have fixed direct target call edges to independently reviewed authored owners and were individually inspected in IDA. They implement scheduler/resource nodes, joystick/keyboard state, logging, game timer and moving-object helpers, plus sound stream construction, playback, volume, cursor and sample-copy operations. They are authored-origin observations at medium confidence, without source unit or codegen claims. Short destructor wrappers at `0x0044C100/0x0044C130` and several sound object lifecycle entries still require separate compiler-versus-source provenance review. | `scripts/review-game-component-origins.py`; hash-attested complete target decode and fixed call anchors; IDA MCP body review of the 40 new entries |
| BOUNDARY-003 | observed / provisional-inventory-gap | The hash-attested IDA MCP lists 2,052 function starts. Before the current expansion, the repository tracked 1,313 candidates and 892 IDA starts were outside the ledger; most were under 64 bytes. Four of those starts, `0x004201B0`, `0x0043BA90`, `0x0043BB30` and `0x0043CCF0`, now have target-replayed immediate function-pointer references, complete dense extents ending in RET, no tracked overlap and reviewed boundary rows totaling 424 bytes. The remaining 888 IDA starts are provisional and require separate entry/extent triage; the current 1,317/1,317 statistic is not an exhaustive `.text` inventory claim. | `scripts/review-ida-missed-entries.py`; hash-attested IDA MCP inventory and behavior; target immediate-pointer and Capstone extent replay; tracking ledger |
| ORIGIN-021 | observed / game-input-and-worker-provenance | Two sound worker callback entries at `0x0043CCF0` and `0x0043E3A0` are passed as function pointers by previously reviewed authored owners. An acyclic four-entry input chain starts at target game initialization `0x004201B0`, directly calls a reviewed authored game owner, passes `0x0043BA90` as its polling thread, which directly calls DirectInput initialization `0x0043B8D0`, which passes `0x0043BB30` as a device callback. Three sound setup/WAV helpers at `0x0043CD30`, `0x0043D390` and `0x0043D250` follow independently decoded direct calls from the reviewed sound setup thread. All nine complete entries, 2,593 bytes, were individually reviewed in IDA and classified authored at medium confidence; original source units and exact codegen remain unknown. | `scripts/review-game-pointer-origins.py`, `scripts/review-game-input-chain-origins.py`, `scripts/review-game-component-origins.py`; target pointer/call replay; IDA MCP behavior review |
| ORIGIN-022 | observed / additional-game-component-provenance | Forty-six further complete game entries totaling 3,997 bytes were individually reviewed for ANM command/interpolation helpers, score and timer updates, hint-file parsing, stage linked lists, input/worker setup, sound buffers and TH10 replay header/payload initialization. Each has a fixed independently decoded direct call edge to a previously reviewed authored owner, and the reviewer excludes its own entire cohort from the initial anchor set before replaying the acyclic chain. They are authored at medium confidence without source unit, production build mode or exactness claims. Constructor-only and source-versus-LTCG-outlined fragments remain under review. | `scripts/review-game-component-origins.py`; hash-attested target complete decode and fixed direct-call anchors; individual IDA MCP body review |
| ORIGIN-023 | observed / compiler-deleting-destructor-provenance | Three complete bodies at `0x0044C100`, `0x0044CF00` and `0x0044D710` follow the VC7.1 scalar deleting-destructor pattern: call the object destructor, test the deletion flag at `[esp+8]`, conditionally call the target free routine and return with `RET 4`. They are compiler-generated wrappers excluded from authored origin at high confidence. The underlying destructor bodies remain separately unresolved. | `scripts/review-deleting-destructor-origins.py`; hash-attested target instructions, call destinations and deletion-flag control flow; prior VC7.1 deleting-destructor precedents in the ledger |
| ORIGIN-024 | observed / target-call-backed-game-provenance | Four more complete game helpers, 196 bytes total, are authored at medium confidence: `0x0040ACE0` tests collision category masks, `0x0044B010` acquires keyboard state, `0x004501B0` updates a polar vector and `0x00450700` updates a renderer state link. Each has a target-decoded direct call site within a previously reviewed authored game owner. The original source unit and exact codegen remain unknown. | `scripts/review-game-component-origins.py`; hash-attested target and direct call sites; IDA MCP behavior inspection |
| ORIGIN-025 | observed / reviewed-attribution-unknown | Ninety remaining tracked entries were individually checked against reviewed physical boundaries and IDA body/xrefs and recorded as `indeterminate`, with an address-specific observation in the function ledger. C++ lifecycle and adapter shapes do not by themselves establish source authorship; some substantive game behavior also lacks independent source/linker provenance. Four source-present Player adapters remain unknown, with no canonical exact claim. The tracked-origin queue is now 709 authored, 518 excluded, 90 reviewed-indeterminate and zero unreviewed. The authored exact denominator remains provisional. | `scripts/review-indeterminate-origins.py`; hash-attested target extent checks; IDA MCP body/xref review; target-bound origin/function ledgers |
| ENEMY-015 | observed / linked-case-layout | The selected `/GL /GS` diagnostic for `EnemyRuntimeView::DispatchEclInstruction @ 0x0040E770` has a 108-entry jump table adjacent to a 181-byte selector that is **181/181 byte-identical** to the original. The target and candidate each have 43 bytes after the selector inside the reported contribution. The target pre-table span is 13,760 bytes; the current candidate is 12,456, exactly 1,304 bytes shorter. Jump destination physical order already differs at its first group, although selector slot numbering and all 124 active opcode values agree. This isolates the present total extent gap to executable case/prologue layout, not missing top-level selector coverage. No byte-exact claim follows. | `scripts/report-ecl-dispatch-table.py --candidate ...`; target-verified PE table reads; `.analysis/gpt-5.6-sol/20260918-enemy-exact/final-{probe,layout}.json` |
| TOOLCHAIN-102 | observed / focused-profile-diagnostic | The target Enemy dispatcher prologue has a VC7.1 security cookie and a `0x2C4` stack frame. A focused linked `/GL` candidate without explicit `/GS` lacks the cookie; explicit `/GS` emits the matching cookie sequence but retains a `0x2BC` frame, so the original production profile and local layout remain open. Removing a redundant source range check before the `switch` leaves the switch default semantics intact and changes the focused `/GL /GS` candidate from 13,128 bytes / 421 of 11,692 comparable bytes equal to 13,112 bytes / 486 equal. The target is 14,416 bytes; normalization is incomplete and the diagnostic has `acceptance_authority=none`. Splitting the spell name buffer into a separate local raised the candidate frame to `0x304` and was reverted. | Pinned VC7.1 SP1 normal and `/GL` focused source probes; target and linked PE prologue disassembly; `scripts/report-ecl-dispatch-table.py --check`; `.analysis/gpt-5.6-sol/20260918-enemy-exact/` |
| ANM-037 | observed / physical-case-order-recovered | In the current `ExecuteScript @ 0x0043EE30` selected `/GL` entry context, the target's 94-entry table resolves to 92 physical case groups. The earlier maintained shared `advanceInstruction` label caused the NOP/interrupt group to appear at physical index 77 rather than target index 21. Giving NOP/interrupt its own ordinary advance body and writing the common switch-exit advance after the switch yields **92/92 groups in target physical order**, with NOP/interrupt at index 21. The candidate pre-table span becomes 9,408 bytes versus the target 9,588 (`-180`); size is worse than the previous `-116`, and exactness remains unknown. The 9,784-byte PDB contribution includes the separate 376-byte absolute table, whereas the target's 9,587-byte ledger extent ends before its table. | `scripts/report-anm-execute-table.py --candidate ...`; target/candidate linked PE table reads; `.analysis/gpt-5.6-sol/20260918-anm-exact/{current,split-advance}-{probe,layout}.json` |
| TOOLCHAIN-103 | observed / exact-manifest-field-correction | A focused cold replay exposed two stale `anm-int3-scale` manifest REL32 offsets: the actual `__ftol2` displacement fields begin at `+0x0A/+0x18/+0x25`, while the first two manifest entries had named the CALL opcodes at `+0x09/+0x17`. Direct target and current candidate disassembly show the same 56-byte instruction schedule. Correcting only those two linkage offsets restores the complete 56-byte canonical replay. With unchanged baseline ANM source, all 79 `AnmManager.cpp` exact units / 16,420 bytes across ten cold artifacts pass; after the new ANM control flow, all 17 exact units / 2,746 bytes in the directly affected `AnmExecutor` artifact pass. Other ANM entry contexts were not cold replayed after that source edit. | `config/match-units.toml`; target/candidate disassembly; `scripts/replay-exact-units.py --unit anm-int3-scale`, `--source src/AnmManager.cpp`, and 17-unit `AnmExecutor` replay; ignored `.analysis/gpt-5.6-sol/20260918-anm-exact/` |
| ECLVM-012 | observed / current-linked-case-layout | A fresh selected `EclVmHost::Run` `/GL` diagnostic updates the generic runner frontier: the then-current candidate pre-table span was 6,716 bytes versus target 6,692 (`+24`), rather than the prior `-104` recorded before later source changes. Expressing the format conversion selector as a natural `switch` shortens the candidate by four bytes to a 6,712-byte pre-table span (`+20`) and a 7,040-byte total contribution against 7,020 target. All 59 active opcode groups retain target physical order. The `EVALUATE_FORMAT_OPERANDS` next-destination gap improves from candidate 267 to 262 versus target 247; source and target still differ in register allocation and conversion-branch shape. This is non-exact with incomplete normalization and no acceptance authority. | `scripts/report-ecl-vm-table.py --candidate ...`; current source/linked PE/target case disassembly; `.analysis/gpt-5.6-sol/20260918-ecl-vm-exact/` |
| ECLVM-013 | observed / ABI-probe-negative | IDA shows `EclVmStartSubroutine @ 0x0044DF70` has exactly two direct call sites in the maintained generic VM source and consumes destination in EAX plus two callee-popped stack arguments. In the selected linked diagnostic, changing the maintained global declaration to internal linkage leaves the 143-byte `SpawnThread` caller unchanged and makes the start-subroutine symbol unresolved to the current diagnostic. A natural `__stdcall` declaration removes caller stack cleanup but passes destination in ESI, omits the target `mov eax, esi`, and grows the start-subroutine candidate from 518 to 522 bytes; the generic runner match also worsens. Removing the loop null guard from the runner grows its pre-table span from 6,712 to 6,736 bytes and retains the wrong EBX/EBP assignment. All three experiments were reverted; the private ABI and register coloring remain unknown. | Hash-attested target/IDA disassembly and callers; focused VC7.1 linked probes under `.analysis/gpt-5.6-sol/20260918-ecl-vm-exact/` |
| TOOLCHAIN-104 | observed / focused-ECL-regression-gate | Following the format-conversion source change, a focused cold replay of `src/EclVm.cpp` rebuilds all 13 configured exact units over two artifacts and confirms 1,301/1,301 bytes plus their declared fields. No generic runner match row is added. | `scripts/replay-exact-units.py --source src/EclVm.cpp`; `.analysis/gpt-5.6-sol/20260918-ecl-vm-exact/exact-replay.json` |
| ENEMY-016 | observed / linked-case-layout-candidate | IDA-attested helper wrappers at `0x00412A00`, `0x00412A60`, `0x00412A70`, and `0x00412AA0` read the active ECL context through the Enemy owner. Natural wrapper definitions and five `memset` calls for the 16-word spawn scratch make the focused `/GL /GS` candidate emit five matching `rep stosd` clears. Reordering semantic case blocks according to the target's physical jump destinations makes all 108 physical groups agree in order, while the 181-byte selector and 43-byte suffix remain identical. With `src/EclVm.cpp` linked as support, the current complete candidate is 14,532 bytes versus 14,416 target (`+116`), with 692/11,684 comparable bytes equal and complete normalization. The target still allocates `0x2C4` stack bytes versus candidate `0x2BC`; wrapper receiver registers and many case bodies differ. This is a non-exact candidate, and the helper wrapper addresses do not establish original source authorship. | Canonical target and IDA wrapper disassembly; focused VC7.1 `/GL /GS` linked replay; `scripts/report-ecl-dispatch-table.py --candidate ...`; `.analysis/gpt-5.6-sol/20260918-enemy-order/memset-laser-order-{probe,layout}.json` |
| ANM-038 | observed / child-case-control-flow | Target `ExecuteScript` child opcodes 88/90 share the tail placed after opcode 90, and 91/92 share the tail placed after opcode 92. The maintained source now uses explicit common tails and the candidate's first two child-case gaps become 44 and 47 bytes, equal to target, while all 92 physical case groups retain target order. The selected `/GL` contribution remains 9,784 bytes including the separate 376-byte table; the pre-table span remains 9,408 versus target 9,588 (`-180`). No exact claim follows. | Canonical target and candidate disassembly; focused selected-entry linked replay with `src/RandomMath.cpp` support; `.analysis/gpt-5.6-sol/20260918-enemy-order/anm-shared-child-tail-{probe,layout}.json` |
| ECLVM-014 | observed / runner-entry-negative | Directly selecting `EclVmContext::Run` as the `/GL` diagnostic link entry still produces a 7,040-byte candidate for the 7,020-byte target, the same span as the prior host-entry context. The target prologue receives the context in EAX and reserves `0x108` stack bytes; the selected candidate receives it in ECX with the same frame. Changing the format branch's index from byte to `int` left the 20-byte span excess unchanged and reduced byte agreement, so it was reverted. The private LTCG call context and register allocation remain unresolved; no normal-COFF or exact claim follows. | Canonical target/candidate disassembly, IDA target boundary, focused linked probes `.analysis/gpt-5.6-sol/20260918-enemy-order/ecl-{run-entry,int-format-index}-probe.json` |
| TOOLCHAIN-105 | observed / focused-exact-regression-gate | After the Enemy dispatcher and ANM executor source changes, a focused cold replay of their two sources passed all 81 configured exact units across 11 artifacts: 16,534/16,534 bytes and every declared linkage field. The Enemy source contributed two units / 114 bytes; ANM contributed 79 units / 16,420 bytes. The two large edited owners remain outside the exact ledger. | `scripts/replay-exact-units.py --source src/EnemyEclDispatcher.cpp --source src/AnmManager.cpp`; `.analysis/gpt-5.6-sol/20260918-enemy-order/focused-exact-replay.json` |
| ECLVM-015 | observed / private-receiver-context | IDA shows only two direct callers of `EclVmContext::Run @ 0x0044E1A0`, both inside `EclVmHost::Run @ 0x0044FD10`. Each leaves the context in EAX and pushes the time delta before the call. Selecting the real host as the focused `/GL` link entry makes the maintained candidate use EAX as the runner receiver too; selecting the runner itself as an external entry makes the candidate use ECX. Both selected contributions are 7,040 bytes versus 7,020 target. The real host context therefore recovers this private receiver convention, while the EBX/EBP coloring and body differences remain unresolved. | IDA direct callers and host disassembly; target/candidate PE disassembly; `.analysis/gpt-5.6-sol/20260918-enemy-order/ecl-host-current-probe.json` |
| ENEMY-017 | observed / projection-call-shape | Target `DispatchEclInstruction` calls the imported `D3DXVec3Project @ 0x0045218E` with six stack arguments and no caller cleanup; the maintained declaration had accidentally used the caller-cleanup convention. Declaring `__stdcall` removes that cleanup in the focused `/GL /GS` candidate. Target projection input X/Y/Z are stored before `EnemyPrepareProjection`, so the source now builds `projectionInput` before the call. Together these changes reduce the candidate PDB contribution from 14,532 to 14,508 bytes against 14,416 target; the entire 92-byte excess lies before the jump table. The 108 case groups remain in target order, the 181-byte selector and 43-byte suffix agree, and the frame stays `0x2BC` against target `0x2C4`. Linked-field normalization is incomplete. No exact claim follows. | Canonical target import thunk and spawn-case disassembly; focused linked probes and case report `.analysis/gpt-5.6-sol/20260918-enemy-order/enemy-project-{stdcall-gs,input-order}-probe.json`, `enemy-project-input-order-layout.json` |
| ANM-039 | observed / position-construction-shape | In target `ExecuteScript`, opcode 48 evaluates position operands Z, Y, X before copying the completed three-word vector to primary or alternate position. Direct X/Y/Z field assignments in the maintained source emitted the opposite evaluation order and split the position branch differently. Constructing `AnmFloat3View(GET_FLOAT_VAR(0), GET_FLOAT_VAR(1), GET_FLOAT_VAR(2))` in each branch reproduces the target's local right-to-left evaluation and aggregate-copy instruction pattern. The selected candidate's pre-table code span becomes 9,584 bytes versus target 9,588 (`-4`, formerly `-180`), while its PDB contribution is 9,960 bytes including a separate 376-byte table. The constructor also raises the candidate stack frame from `0xD4` to the target's `0xFC`. The saved game-speed local still lands at candidate `[esp+0x60]` versus target `[esp+0x98]`, and the alternate-position branch is placed before the interrupt-label block instead of after it. Thus the owner is non-exact despite matching frame size and close total code span. | Canonical target/candidate opcode-48 and prologue disassembly; focused `/GL` probe and case report `.analysis/gpt-5.6-sol/20260918-enemy-order/anm-position-constructor-{probe,layout}.json` |
| ECLVM-016 | observed / loop-and-format-negative | In the real `EclVmHost::Run` link context, carrying the current instruction pointer across iterations changes the runner candidate from 7,040 to 7,028 bytes but alters the frame from `0x108` to `0x10C` and leaves one undecoded suffix byte; the target remains 7,020 bytes with a `0x108` frame. An explicit `continue` in the percent-conversion case grows the candidate to 7,048 bytes and slightly reduces comparable-byte agreement. Both source experiments were reverted. The target's EAX receiver is already recovered, while its EBX/EBP allocation and format-case shape remain unresolved. | IDA target loop and format-case disassembly; focused linked probes `.analysis/gpt-5.6-sol/20260918-enemy-order/ecl-{carried-current,format-percent-continue}-probe.json` |
| TOOLCHAIN-106 | observed / focused-exact-regression-gate | After the Enemy projection ABI and ANM position-construction source corrections, a cold replay of the two edited source files passed all 81 configured exact units across 11 artifacts: 16,534/16,534 bytes and every declared linkage field. This gate protects previously accepted small units; it does not promote either large edited owner. | `scripts/replay-exact-units.py --source src/EnemyEclDispatcher.cpp --source src/AnmManager.cpp`; `.analysis/gpt-5.6-sol/20260918-enemy-order/current-focused-exact-replay.json` |
| ECLVM-017 | observed / format-case-source-shape | In target `EclVmContext::Run`, the format opcode compares metadata type before loading the raw operand and reloads the current instruction through the context. Moving the raw load inside each type branch and using `context->instruction` at each operand read restores the target's EBX instruction-cursor and EBP zero-register assignment in the selected real-host `/GL` diagnostic. Passing only the initial format cursor from the outer `current` pointer makes the case start with target-shaped `push 0x400; add edi,0x14`. The candidate now has the target's full 7,020-byte contribution, 6,692-byte pre-table span, and all 59 physical groups in order; its format-case gap is 250 versus target 247 (formerly 262). The candidate frame is `0x104` versus target `0x108`, linked-field normalization is incomplete, and many body bytes still differ, so the function is not exact. Changing the percent case to an explicit `continue` grows the contribution to 7,028 bytes and was reverted. | IDA target format-case disassembly; focused real-host linked probe and selector report `.analysis/gpt-5.6-sol/20260918-enemy-order/ecl-format-{cursor-argument-probe,cursor-argument-layout,final-percent-continue-probe}.json` |
| TOOLCHAIN-107 | observed / focused-exact-regression-gate | After the ECL format operand source change, a focused cold replay of `src/EclVm.cpp` passes all 13 configured exact units over two artifacts: 1,301/1,301 bytes and all declared linkage fields. This does not promote the 7,020-byte generic runner, whose byte and field comparison still fails. | `scripts/replay-exact-units.py --source src/EclVm.cpp`; `.analysis/gpt-5.6-sol/20260918-enemy-order/ecl-current-focused-exact-replay.json` |
| ECLVM-018 | observed / equal-span-float-stack-slots | In the current equal-length runner candidate, the opcode 0x3E `NOT_EQUAL_FLOAT` physical gap is 165 versus target 177, while opcode 0x44 `GREATER_FLOAT` is 175 versus target 163. Target 0x3E stores its first float temporary at `[esp+0x100]`; candidate uses `[esp+0x4C]`, saving 12 bytes through short displacements. For 0x44 the target uses short-offset slots `+0x7C/+0x74`, while candidate's first slot `+0x80` requires long displacements and adds 12 bytes. These opposite gaps cancel in the total extent and do not imply matching code. Moving one comparison temporary to function scope or reversing the commutative `!=` variable names produced byte-identical candidates and was reverted. The stack-slot assignment remains open. | Canonical target and selected linked candidate float-case disassembly; `ecl-format-cursor-argument-layout.json` and negative probes below `.analysis/gpt-5.6-sol/20260918-enemy-order/` |
| ECLVM-019 | observed / loop-entry-source-shape | Target `EclVmContext::Run` checks the instruction pointer once on entry, then tests instruction time without a repeated null guard on each loop iteration. Removing the extra guard from the maintained source makes the selected real-host `/GL` candidate's entry through the first case selector instruction match the target instruction sequence and branch layout except for the unresolved `0x104` versus `0x108` frame constant and linked addresses. The candidate's return-to-loop path now uses `FILD/FCOMP` as the target does, rather than a pointer-null comparison. Its contribution grows from 7,020 to 7,036 bytes against target 7,020, but the sum of absolute physical case-gap differences improves from 68 to 60 bytes and raw comparable-byte agreement improves from 591 to 694 of 6,268. The target's shared advance computes `ECX = EAX + offset` then stores the pointer; the candidate adds the offset directly in memory and reloads it. Changing only the tail spelling or offset signedness compiled identically; reloading jump operands through the cursor changed the whole candidate to 6,972 bytes and was reverted. This remains non-exact. | IDA `0x0044E1A0` entry/tail and canonical target disassembly; focused linked probes and layouts `ecl-loop-{no-null,final}-{probe,layout}.json`, `ecl-loop-final.exe`, and negative `ecl-loop-{direct-advance,jump-reload,jump-local,unsigned-offset,advance-local}-probe.json` below `.analysis/gpt-5.6-sol/20260918-enemy-order/` |
| ANM-040 | observed / position-advance-negative | Target `ExecuteScript` places the NOP/interrupt-label advance at `0x0043F532` immediately after the primary POSITION branch, before the alternate-position block. The current 9,960-byte candidate places a separate NOP advance after the alternate branch. Two source variants joining NOP and POSITION through an explicit label retained the physical opcode order but produced 9,848-byte candidates, 9,472 versus 9,588 pre-table bytes, and still placed NOP after the alternate branch. Both variants were reverted. Omitting the redundant switch-exit `continue` had no case-layout effect. The 9,960-byte source remains selected and non-exact. | IDA target and candidate position/advance disassembly; focused linked `anm-position-{shared-nop,branch-island}-probe.json`, `anm-position-shared-nop-layout.json`, and `anm-fallthrough-advance-{probe,layout}.json` below `.analysis/gpt-5.6-sol/20260918-enemy-order/` |
| TOOLCHAIN-108 | observed / focused-exact-regression-gate | After removing the generic runner's loop null guard, a focused cold replay of `src/EclVm.cpp` passes all 13 configured exact units in two artifacts, 1,301/1,301 bytes and their declared linkage fields. This protects existing exact units only; the 7,020-byte target runner remains non-exact. | `scripts/replay-exact-units.py --source src/EclVm.cpp`; `.analysis/gpt-5.6-sol/20260918-enemy-order/ecl-loop-focused-exact-replay.json` |
| ECLVM-020 | observed / direct-store-operand-shape | Target `Run` opcodes 0x2B `STORE_INT` and 0x2D `STORE_FLOAT` resolve the destination first, then inline `EclVmStackView::Pop` so the popped value is copied directly to that destination before tagged-type conversion. The previous source used a temporary return from `PopInt`/`PopFloat` and assigned it afterward, changing both order and underflow behavior. Replacing those two cases with direct `stack.Pop(type, sizeof(value), ResolveX(0))` makes their selected candidate physical spans exactly target-sized: 80/80 and 86/86 bytes, with matching local instruction shapes apart from linked destinations. The selected whole runner is now 7,032 versus 7,020 target bytes, all 59 opcode groups remain in order, and the sum of absolute case-gap differences falls from 60 to 52. The frame moves to `0xFC` versus target `0x108`; cases 0x34/0x3F acquire offsetting `+12/-12` gaps, and the complete owner is non-exact. Target `ReadInt @ 0x0044FDB0` takes an EDX receiver plus stack argument while the probe-private candidate helper currently takes EDX plus ECX; this explains part of the still-open 0x14 call-shape difference and must not be disguised as an exact ABI. | IDA target `Run` and `ReadInt` disassembly; canonical target/candidate STORE disassembly; `ecl-direct-store-{probe,layout}.json` and `ecl-direct-store.exe` below `.analysis/gpt-5.6-sol/20260918-enemy-order/` |
| TOOLCHAIN-109 | observed / focused-exact-regression-gate | After correcting the generic runner's two STORE cases, a focused cold replay of `src/EclVm.cpp` again passes all 13 configured exact units across two artifacts, 1,301/1,301 bytes and every declared linked field. The edited 7,020-byte target runner is still non-exact. | `scripts/replay-exact-units.py --source src/EclVm.cpp`; `.analysis/gpt-5.6-sol/20260918-enemy-order/ecl-direct-store-focused-exact-replay.json` |
| ECLVM-021 | observed / exact-polar-helper-and-runner-shape | The runner's opcode 0x51 calls a separate 30-byte `__thiscall` polar-vector helper at `0x004501B0`; its x87 `FSINCOS` writes cosine and sine times magnitude to two floats. The complete helper is byte-identical to the target's ANM `AnmFloat3View::FromAngleMagnitude @ 0x004458B0`. A maintained `EclVmPolarVectorView::FromAngleMagnitude` compiled in the real `EclVmHost::Run` `/GL` link context reproduces all 30 PDB-owned bytes raw-equal in two cold builds, with no linkage fields. Opcodes 0x51 and 0x52 both use the separately exact `EnemyWrapAngle @ 0x0044BC70`; expressing the 0x51 call with nested argument evaluation gives target-sized 97/97 and 36/36 physical case spans. The complete runner remains non-exact: 7,032 candidate versus 7,020 target bytes, `0x104` versus `0x108` frame, although all 59 physical opcode groups retain target order. | IDA target helper/call disassembly; target-bound `ecl-polar-canonical-{1,2}.json`; `ecl-polar-nested-call-{probe,layout}.json` under `.analysis/gpt-5.6-sol/20260918-enemy-order/`; exact ledger unit `ecl-vm-polar-from-angle-magnitude` |
| TOOLCHAIN-110 | observed / focused-exact-regression-gate | With the polar helper and angle-call source changes, a focused cold replay of `src/EclVm.cpp` passes all 14 configured exact units across three artifacts, 1,331/1,331 bytes and every declared linkage field. This validates the new 30-byte helper alongside the 13 prior units; the 7,020-byte generic runner remains non-exact. | `scripts/replay-exact-units.py --source src/EclVm.cpp`; `.analysis/gpt-5.6-sol/20260918-enemy-order/ecl-polar-focused-exact-replay.json` |
| ECLVM-022 | observed / polar-local-stack-shape | The target runner's 0x51 call addresses its temporary vector's X/Y at `[esp+0x10C]` and `[esp+0x110]` after the helper returns. Its 30-byte helper has the same complete instruction body as the ANM 12-byte `AnmFloat3View::FromAngleMagnitude`; the target caller reserves a `0x108` frame. Modeling the ECL local as a three-float vector raises the candidate frame from `0x104` to `0x108` while preserving the complete 30-byte helper exact match, the 97/97 polar case span, all 59 opcode-group order, and the 7,032-byte candidate owner size. In the anchored linked diagnostic, the first raw difference moves from offset `0x2` (frame immediate) to `0x32` (branch displacement). The third float's semantic use remains unproven, and this local improvement does not make the whole runner exact. | IDA target 0x51 caller and helper disassembly; target and candidate x86 decoder; `ecl-polar-three-component-{probe,layout}.json` and `ecl-polar-three-component-helper-replay.json` under `.analysis/gpt-5.6-sol/20260918-enemy-order/` |
| TOOLCHAIN-111 | observed / focused-exact-regression-gate | With the ECL polar local widened to three floats, a focused cold replay of `src/EclVm.cpp` again passes all 14 configured exact units across three artifacts, 1,331/1,331 bytes and every declared linkage field. The generic runner remains non-exact. | `scripts/replay-exact-units.py --source src/EclVm.cpp`; `.analysis/gpt-5.6-sol/20260918-enemy-order/ecl-polar-three-component-focused-replay.json` |
| ECLVM-023 | observed / read-int-underflow-return | Target `EclVmContext::ReadInt @ 0x0044FDB0` loads its stack index into EAX on entry and leaves EAX unchanged when the typed stack has fewer than four bytes, so that path returns the input index. The maintained source now initializes the pop destination from that index. A real-host linked `/GL` probe makes the candidate helper 117 bytes versus the target's 144, down from 122; its private ABI remains EDX receiver plus ECX index versus target EDX receiver plus stack index. The complete runner remains 7,032 versus 7,020 bytes, with all 59 physical opcode groups in target order. This corrects the underflow behavior but establishes no new exact claim. | IDA target `0x0044FDB0-0x0044FE3F` disassembly; candidate disassembly; `ecl-readint-init-index-{probe,layout}.json` under `.analysis/gpt-5.6-sol/20260918-enemy-order/` |
| TOOLCHAIN-112 | observed / focused-exact-regression-gate | Following the `ReadInt` underflow correction, the `src/EclVm.cpp` focused cold replay passes all 14 exact units across three artifacts, 1,331/1,331 bytes and every declared linkage field. `ReadInt` and the generic runner remain non-exact. | `scripts/replay-exact-units.py --source src/EclVm.cpp`; `.analysis/gpt-5.6-sol/20260918-enemy-order/ecl-readint-init-index-focused-replay.json` |
| ECLVM-024 | observed / private-read-int-abi-gap | Runner opcode 0x14 occupies 51 target bytes but 57 candidate bytes in the selected host `/GL` link. Target calls `ReadInt` with `push ebp` for index zero and `push 1` for index one, then retains the found thread context in EDI across the second call. Candidate passes those indices in ECX and moves the first lookup result to EDI before the null check. Omitting Enemy support from the host-entry probe leaves the runner at 7,032 bytes and `ReadInt` at 117 bytes; selecting the Enemy dispatcher as entry with ECL support also leaves the ECL contributions at 7,032 and 117 bytes. The target `ReadInt` remains 144 bytes and takes an EDX receiver plus stack index. These link-context probes do not establish the production private ABI. | IDA `ReadInt @ 0x0044FDB0` and target opcode-0x14 disassembly; linked probes `ecl-host-without-enemy-probe.json` and `enemy-entry-with-ecl-support-probe.json` under `.analysis/gpt-5.6-sol/20260918-ecl-abi/` |
| ENEMY-018 | observed / security-cookie-profile-and-entry-gap | The target Enemy dispatcher begins with an EBP-aligned `0x2C4` frame, reads the security cookie at `0x00473660`, and XORs it with EBP. Pinned `/GS` with the real dispatcher entry and ECL support reproduces the prior 14,508-byte candidate against the 14,416-byte target; its physical case order matches and the 181-byte opcode selector is equal. Omitting `/GS` yields a 12,988-byte diagnostic with ECL support or 12,692 bytes without it; these shorter bodies are the wrong codegen profile. The `/GS` candidate uses a `0x2BC` frame, so its remaining 92-byte owner gap cannot be treated as case-body exactness. Marking `ReadInt` noinline leaves the `/GS` dispatcher and helper at 14,508/117 bytes; storing opcode in a 32-bit temporary grows the dispatcher to 14,516 bytes and both probes were reverted. | Target and candidate entry disassembly; `enemy-entry-{gs-with-ecl,with-ecl-support,without-ecl,readint-noinline}-probe.json` and `enemy-opcode-word-int-probe.json` under `.analysis/gpt-5.6-sol/20260918-ecl-abi/` |
| ANM-041 | observed / executor-tail-placement-and-child-abi | In target `ExecuteScript`, opcode 39's 105-byte float-jump body flows to the END/DELETE return at `0x00440DFC`; the script-stop update block begins after that return at `0x00440E2B`. The candidate float-jump instructions have the same local structure, but its stop block begins at `0x00407EA6` before the END return at `0x00407EFD`, making the physical interval 192 bytes. The candidate remains a 9,960-byte contribution with a 9,584-byte pre-table span versus target 9,588, and all 92 physical groups retain target order. Moving the saved-speed declaration to the top compiles to the exact same 9,960 candidate bytes; inverting the POSITION condition changes only three bytes, including a branch sense that then disagrees with target, so both source variants were reverted. Nearby private ABIs remain open: `FindVm` is 65 versus target 73 bytes with EAX+EDX rather than EDX+stack, `GetVm` is 19 versus 21, and `CreateVmVariant3` is 74 versus 73 with EBX rather than EDI hidden return storage. | Target/candidate x86 disassembly and linked probe `anm-position-final-layout.json`; `anm-saved-speed-first-probe.json` and `anm-position-branch-inversion-probe.json` under `.analysis/gpt-5.6-sol/20260918-ecl-abi/` |
| ANM-042 | observed / float-jump-stack-slots | The four ANM float-jump cases use different stack temporaries despite their shared source expression shape. Target/candidate first-operand slots are `+0x84/+0x4C` for opcode 33, `+0x8C/+0x54` for opcode 35, `+0x94/+0x5C` for opcode 37, and `+0x2C/+0x24` for opcode 39. The first three differ by `0x38`, the same difference as the saved game-speed slot, while the fourth differs by eight. The candidate and target opcode-39 comparison/branch sequence otherwise follows the same instruction pattern before the misplaced stop block. This is a local-allocation discrepancy, not a four-byte whole-owner match. | Complete target and selected candidate case disassembly; `anm-position-final-layout.json` in `.analysis/gpt-5.6-sol/20260918-enemy-order/` |
| ENEMY-019 | observed / argument-resolver-codegen | Target dispatcher `0x0040E770` calls private `EnemyRuntimeView::ResolveIntArgument @ 0x00412A10` nine times. The complete 75-byte target helper loads the active ECL context and implements the same operand-mask/local-slot/virtual-fallback path as `EclVmContext::ResolveInt`. A natural maintained forwarding method in `src/EnemyEclDispatcher.cpp` emits one complete 75-byte PDB-owned LTCG contribution at candidate `0x004010F0`, raw-equal to target in two independent selected-entry `/GS` cold links. The target's game-source versus compiler-outlined origin remains indeterminate, so this is source presence and diagnostic byte equality, **not** a canonical `matches.csv` exact unit. The neighboring `ReadIntArgument @ 0x00412A00` is mapped to its maintained source, but the candidate is 14 versus target 16 bytes because the selected private `ReadInt` ABI still differs. The dispatcher grows from 14,508 to 14,512 bytes against target 14,416 and remains non-exact. | IDA target `0x00412A00-0x00412A5A` and dispatcher call sites; `enemy-resolve-int-wrapper-{probe,second-probe}.json`, `enemy-resolve-int-linked-inventory.json`, and `enemy-resolve-int-raw-second.json` under `.analysis/gpt-5.6-sol/20260918-ecl-abi/` |
| TOOLCHAIN-113 | observed / focused-exact-regression-gate | After adding the Enemy argument resolver, focused cold replay of both exact units sourced from `EnemyEclDispatcher.cpp` and the sole ECL polar unit using that file as support passes all three canonical units across two artifacts, 144/144 bytes and every declared linkage field. The large dispatcher still fails exact. | `scripts/replay-exact-units.py --unit enemy-set-motion-angle --unit enemy-wrap-angle --unit ecl-vm-polar-from-angle-magnitude`; `.analysis/gpt-5.6-sol/20260918-ecl-abi/enemy-resolver-focused-replay.json` |
| ECLVM-025 | observed / index-parameter-pop-abi | The target `ReadInt @ 0x0044FDB0` uses EDX for its ECL context, reads the index from the stack, and returns with `RET 4`; its typed pop can overwrite that stack argument. Writing the pop result directly into the maintained `index` parameter makes selected EclVmHost and Enemy-supported `/GS` LTCG candidates use the same private ABI and the full 144-byte extent. Against the hash-attested target, the Enemy-supported candidate differs at raw offsets `0x51,0x52,0x55,0x5C` because it uses `LEA ECX,[ESI-4]` and subsequent ECX references where target uses `ADD ESI,-4`; raw offsets `0x6A-0x6C` are the expected linked `__ftol2` call displacement. The function is still non-exact. Changing only the index parameter's signedness gave the same 144-byte body and was reverted. The EclVmHost-entry runner becomes 7,016 versus target 7,020 bytes, with all 59 physical opcode groups in target order and a four-byte pre-table deficit. | IDA `ReadInt` disassembly; `ecl-readint-pop-index-{probe,linked-inventory,layout}.json`, `ecl-readint-signed-index-{probe,linked-inventory}.json`, and `ecl-readint-pop-index-raw-diagnostic.json` under `.analysis/gpt-5.6-sol/20260918-ecl-abi/` |
| ENEMY-020 | observed / integer-reader-abi-propagation | With the `ReadInt` index on the stack, the selected Enemy `/GS` plus ECL-support link emits a 16-byte `ReadIntArgument` against the target's 16 bytes. The wrapper still uses candidate ECX receiver/EAX index where target uses EAX receiver/ECX index, so equal length is not exactness. The 75-byte `ResolveIntArgument` remains raw-equal. The full dispatcher becomes 14,284 against target 14,416 bytes; its 181-byte selector and physical case order still match, while its pre-table code is 132 bytes short. This demonstrates that the private helper ABI materially changes the large owner, without resolving its case bodies or register allocation. | IDA `0x00412A00-0x00412A5A`; `enemy-readint-pop-index-{gs-probe,linked-inventory,layout}.json` under `.analysis/gpt-5.6-sol/20260918-ecl-abi/` |
| TOOLCHAIN-114 | observed / focused-exact-regression-gate | After the `ReadInt` parameter-pop change, a focused cold replay of all 14 `src/EclVm.cpp` canonical units passes across three artifacts, 1,331/1,331 bytes and all declared linkage fields. `ReadInt`, `Run`, and the Enemy dispatcher remain non-exact. | `scripts/replay-exact-units.py --source src/EclVm.cpp`; `.analysis/gpt-5.6-sol/20260918-ecl-abi/ecl-readint-pop-index-focused-replay.json` |
| ECLVM-026 | observed / target-sized-runner-with-open-byte-gaps | Target `Run` opcode 0x14 loads the selected thread's context before evaluating its second integer operand. A separate `threadContext` source local makes the selected candidate's complete 51-byte case instruction sequence match the target apart from linked addresses; its earlier 53-byte case performed that load after `ReadInt(1)`. Explicitly advancing the cursor and continuing for a literal `%%` conversion makes the format parser retain the target's three-way `'%'/ 'd'/ 'f'` decision. In the selected EclVmHost `/GL` link with Enemy support, the full runner is now 7,020/7,020 bytes, its pre-table span is 6,692/6,692, and all 59 physical opcode groups are in target order. This is **not exact**: the diagnostic matches only 795/6,264 comparable bytes; the format case is 253 versus target 247 bytes, and several arithmetic cases have offsetting 12- and 15-byte stack-slot gaps. An explicit null guard made the runner 7,040 bytes; an outer guarded do-while made it 7,024, so both were reverted. Rewriting `ReadInt`'s return path or `Pop`'s tagged-top local did not eliminate its three register-choice fields and was reverted. | IDA target `Run @ 0x0044E1A0` and `ReadInt @ 0x0044FDB0`; selected `ecl-thread-format-enemy-support-{probe,inventory,layout}.json`, `ecl-thread-control-temp-layout.json`, `ecl-format-percent-continue-layout.json`, and negative `ecl-format-{early-null,do-while}-{probe,layout}.json` under `.analysis/gpt-5.6-sol/20260918-ecl-abi/` |
| TOOLCHAIN-115 | observed / focused-exact-regression-gate | After the ECL thread-control and format-cursor source changes, focused cold replay of all 14 `src/EclVm.cpp` canonical units passes across three artifacts, 1,331/1,331 bytes with all declared linkage fields. The target-sized 7,020-byte runner and 144-byte `ReadInt` remain non-exact. | `scripts/replay-exact-units.py --source src/EclVm.cpp`; `.analysis/gpt-5.6-sol/20260918-ecl-abi/ecl-thread-format-focused-replay.json` |
| ANM-043 | observed / executor-exit-order-negative | The target ANM float-jump case 39 is followed physically by the script END/DELETE return and then the stop-update block, while the candidate has stop before END. Moving the END/STATIC return to a label outside the loop, chaining `CreateVmVariant3(...).GetVm()`, and reordering independent `CreateVmVariant3` field writes did not recover the target order or hidden-return EDI convention. The outside-loop label and field-write variants generated the same complete 9,960-byte candidate; the chained temporary moved its stack object from `+0x34` to `+0x114` and changed 142 bytes without closing the gap. All variants were reverted. | Hash-attested IDA target `0x00440D93-0x00440E2B`; selected `/GL` probes under `.analysis/gpt-5.6-sol/20260919-anm-tail/` |
| ECLVM-027 | observed / arithmetic-stack-order | The target runner's float ADD/SUB/MUL/DIV arithmetic cases use first-pop stack slots `+0x60/+0xB8/+0x68/+0xF8`; the maintained candidate uses `+0xE0/+0x60/+0xB8/+0x68`. The resulting 8-bit versus 32-bit stack displacements account for several offsetting 15-byte physical-case gaps despite the 7,020/7,020 complete owner spans. Interleaving integer and float source cases raised byte agreement from 795 to 1,080 of 6,264 comparable bytes but broke the target's physical case order; changing only ADD_FLOAT declaration order left codegen unchanged. Both experiments were reverted. `ReadInt` specialization of its typed pop also compiled to the same 144-byte non-exact body. | Target IDA runner arithmetic disassembly; `ecl-{readint-specialized-pop,arithmetic-interleaved,add-float-declaration-order}-probe.json` and `ecl-arithmetic-interleaved-layout.json` under `.analysis/gpt-5.6-sol/20260919-anm-tail/` |
| ENEMY-021 | observed / difficulty-count-call-shape | In Enemy dispatcher opcode `0x1B4`, the target selects difficulty-dependent operand indices with small branches and joins at one `ReadInt` call for each count field. The maintained source formerly selected an index then called `ReadIntArgument`, causing the selected `/GL /GS` candidate to duplicate the second call and return epilogue in several branches. Placing `ReadIntArgument` in each source branch gives the compiler a closer common-call shape: the 0x1B4 physical interval falls from 254 to 216 bytes against target 151, the whole candidate from 14,284 to 14,244 against target 14,416, and comparable-byte agreement rises from 562 to 595. The 108 case groups remain in target physical order and the 181-byte selector remains equal. The dispatcher is still non-exact; ternary index selection left its span at 14,284 and was reverted. | Target bytes/IDA-attested SHA; selected `/GL /GS` `enemy-count-{baseline,ternary,branch-call}-probe.json` and `enemy-count-branch-call-layout.json` under `.analysis/gpt-5.6-sol/20260919-anm-tail/` |
| TOOLCHAIN-116 | observed / focused-exact-regression-gate | After the Enemy 0x1B4 source change, a focused cold replay passes both exact units from `src/EnemyEclDispatcher.cpp` and the ECL polar unit that links it as support: three units across two artifacts, 144/144 bytes and all declared linkage fields. The Enemy dispatcher remains non-exact. | `scripts/replay-exact-units.py --unit enemy-set-motion-angle --unit enemy-wrap-angle --unit ecl-vm-polar-from-angle-magnitude`; `.analysis/gpt-5.6-sol/20260919-anm-tail/enemy-count-focused-replay.json` |
| ENEMY-022 | observed / float-difficulty-index-codegen | Target dispatcher opcode `0x1B3` selects an integer operand index in ECX before each float read. The prior source carried that integer through a bit-encoded `float`, forcing candidate stack stores and reloads. A case-local `int speedIndex` removes those artificial conversions: the physical interval becomes 169 bytes versus target 170, down from 211, while the complete `/GL /GS` candidate changes from 14,244 to 14,204 bytes versus target 14,416. All 108 case groups retain target physical order and the 181-byte selector is equal. The candidate still scales the bullet index before its first float read where target saves the index in ESI and scales it after the call, so neither this case nor the owner is exact. Seven other variants were checked: a case-local bullet index and separate float-result locals generated the same candidate; an explicit difficulty-2 branch shortened the case to 161 bytes and was reverted; changing the neighboring 0x1B4 selector to a local or preloaded context worsened its whole-owner diagnostic and was reverted. A `switch` raised broad comparable-byte agreement but used the wrong local `sub/dec` branch shape and was reverted. Replacing the shared rank/difficulty float selector with an integer improved one rank case but collapsed another target 169-byte interval from candidate 174 to 110; it too was reverted. | SHA-attested target bytes and IDA `ReadInt` caller evidence; selected `speed-retained-final-{probe,layout}.json`, `speed-int-index-{probe,layout}.json`, and negative `local-index`, `switch-second`, `context-local`, `speed-local-bullet-index`, `speed-explicit-difficulty`, `speed-float-lifetime`, `shared-float-index` probes under `.analysis/gpt-5.6-sol/20260919-enemy-count/` |
| TOOLCHAIN-117 | observed / focused-exact-regression-gate | Following the Enemy 0x1B3 index-source change, the two exact units sourced from `src/EnemyEclDispatcher.cpp` and the ECL polar unit linking it as support cold-replay at 144/144 bytes across two artifacts, with every declared linkage field matched. The 14,204-byte Enemy dispatcher remains non-exact against target 14,416. | `scripts/replay-exact-units.py --unit enemy-set-motion-angle --unit enemy-wrap-angle --unit ecl-vm-polar-from-angle-magnitude`; `.analysis/gpt-5.6-sol/20260919-enemy-count/focused-exact-replay.json` |
| ENEMY-023 | observed / speed-index-register-allocation-negative | Six additional natural source variants around dispatcher opcode `0x1B3` did not recover the target's raw-index ESI lifetime. Assigning the raw index before the float read, assigning `ReadIntArgument` directly to the later address variable, or reusing the function-level integer temporary all compiled to the retained 14,204-byte candidate with 596/11,660 comparable-byte agreement. Caching difficulty produced a 164-byte case and 579/11,664 agreement; an explicit 0x1B4 call-join label produced a 14,252-byte owner; direct float calls produced 14,304 bytes. All variants were reverted. The remaining one-byte 0x1B3 interval gap is coupled to whole-function private register allocation and cannot be promoted independently. | Target/candidate x86 disassembly; `raw-index-before-call`, `cached-difficulty`, `count-explicit-join`, `reuse-function-int`, `direct-float-calls`, and `direct-index-result` probes under `.analysis/gpt-5.6-sol/20260919-enemy-speed/` |
| ECLVM-028 | observed / format-register-allocation-negative | In runner opcode `0x1E`, target carries the format metadata offset in EDX, leaving EDI available for `MOV cursor; ADD 2` at the loop tail. The selected candidate carries metadata offset in EDI and therefore uses ECX plus `LEA` and `TEST`; its loop header also contains three alignment bytes. Splitting both cursor updates into assignment plus increment, explicitly materializing `inlineBytes + metadataOffset`, and advancing a metadata pointer in two statements each compiled to the unchanged 7,020-byte candidate with 795/6,264 comparable-byte agreement and a 253-byte format case versus target 247. Swapping the two Enemy support-source positions also left the entry address and candidate bytes unchanged. All source variants were reverted. | Target/candidate complete 0x1E disassembly; `baseline-{probe,layout}.json`, `cursor-mov-add-probe.json`, `argument-offset-local-probe.json`, `metadata-pointer-probe.json`, and `support-order-probe.json` under `.analysis/gpt-5.6-sol/20260919-ecl-format/` |
| ANM-044 | observed / explicit-stop-edge-negative | Replacing the ANM executor's conditional `while` with an infinite loop and explicit `goto stop` preserves semantics but does not recover the target's END-before-stop physical layout. The selected contribution grows from 9,960 to 9,996 bytes and matches only 317/8,611 comparable bytes. The experiment was reverted; the maintained candidate remains non-exact at 9,960 bytes including its 376-byte table. | Hash-attested target and selected `/GL` probe `.analysis/gpt-5.6-sol/20260919-anm-loop/explicit-stop-edge-probe.json` |
| ENEMY-024 | observed + inferred / scratch-layout-and-count-join | IDA target bytes place the 0x1F8-byte laser request scratch at dispatcher `ESP+0x50` and the 0x84-byte spawn request at `ESP+0x248`, immediately below the security cookie. Modeling the laser request as an array union caused `/GS` to reverse those objects: the selected candidate used `ESP+0xC8` for the laser request and `ESP+0x40` for the spawn request. Modeling the still-semantically-unknown laser request as a 0x1F8-byte scalar-field struct restores the target object order; candidate offsets become `ESP+0x40` and `ESP+0x238`, each uniformly 0x10 below target. This raises comparable-byte agreement from 596/11,660 to 639/11,660. In the same source checkpoint, selecting opcode 0x1B4's two difficulty operands through a case-local integer gives one reader call per field and shrinks its physical interval from 216 to 142 bytes against target 151; combined agreement rises to 671/11,676. The complete owner becomes 14,080 versus 14,416 and remains non-exact; 0x1B3 remains 169/170, all 108 physical groups retain target order, and the 181-byte selector remains equal. The struct field meanings remain unknown and are not semantic claims. | IDA target bytes at `0x0040E770`, target/candidate x86 disassembly, `laser-struct-{probe,layout}.json`, and `checkpoint-{probe,layout}.json` under `.analysis/gpt-5.6-sol/20260919-enemy-exact/` |
